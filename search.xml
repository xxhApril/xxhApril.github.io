<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>《Objective-C高级编程》三篇总结之二：Block篇</title>
    <url>/2019/12/11/%E3%80%8AObjective-C%E9%AB%98%E7%BA%A7%E7%BC%96%E7%A8%8B%E3%80%8B%E4%B8%89%E7%AF%87%E6%80%BB%E7%BB%93%E4%B9%8B%E4%BA%8C%EF%BC%9ABlock%E7%AF%87/</url>
    <content><![CDATA[<p>这本书作者几乎通篇都在用 C、C++ 语言分析讲解 Block 的实现，初次看真的很吃力。这里推荐一篇文章<a id="more"></a>：<a href="https://juejin.im/post/58f40c0a8d6d810064879aaf" target="_blank" rel="noopener">《Objective-C 高级编程》干货三部曲（二）：Blocks篇</a>。总结的非常精简到位。这篇博客作者有三篇文章分别是：</p>
<ul>
<li><a href="https://juejin.im/post/58ead8c344d904006d4f17af" target="_blank" rel="noopener">《Objective-C 高级编程》干货三部曲（一）：引用计数篇</a></li>
<li><a href="https://juejin.im/post/58f40c0a8d6d810064879aaf" target="_blank" rel="noopener">《Objective-C 高级编程》干货三部曲（二）：Blocks篇</a></li>
<li><a href="https://juejin.im/post/58fd55c08d6d81005898df46" target="_blank" rel="noopener">《Objective-C 高级编程》干货三部曲（三）：GCD篇</a></li>
</ul>
<p>我读这本书时，也大量参考了这几篇文章的讲解。</p>
<p>另外，我个人也对 <a href="[https://www.jianshu.com/p/93e5d674c56c](https://www.jianshu.com/p/93e5d674c56c">引用计数</a><br>)  以及 <a href="">Grand Central Dispatch</a> 做了总结。</p>
<h3 id="C-语言相关"><a href="#C-语言相关" class="headerlink" title="C 语言相关"></a>C 语言相关</h3><h4 id="Objective-C-转-C-的方法"><a href="#Objective-C-转-C-的方法" class="headerlink" title="Objective-C 转 C++ 的方法"></a>Objective-C 转 C++ 的方法</h4><p>Block 是带有自动变量的匿名函数，实质上就是对 C 语言的扩充，最终通过支持 C 语言的编辑器，将目标代码转化为 C 语言代码被编译，可通过下面命令行转化：<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">clang -rewrite-objc 源代码文件名</span><br></pre></td></tr></table></figure></p>
<p>例如：</p>
<ol>
<li>创建 OC 源文件 block.m 并编辑代码。</li>
<li>打开终端，cd 到 block.m 所在的文件夹。</li>
<li>输入 <code>clang -rewrite-objc block.m</code>，就会在当前文件夹生成 block.cpp 文件。</li>
</ol>
<h4 id="C-语言几种变量特点"><a href="#C-语言几种变量特点" class="headerlink" title="C 语言几种变量特点"></a>C 语言几种变量特点</h4><p>C 语言函数可能使用的变量：</p>
<ul>
<li>自动变量（局部变量）</li>
<li>函数的参数</li>
<li>静态变量（局部静态变量）</li>
<li>静态全局变量</li>
<li>全局变量</li>
</ul>
<p>其中，在函数的多次调用之间能够传递值的变量有：</p>
<ul>
<li>静态变量（局部静态变量）</li>
<li>静态全局变量</li>
<li>全局变量</li>
</ul>
<h3 id="Block-实质"><a href="#Block-实质" class="headerlink" title="Block 实质"></a>Block 实质</h3><p>先说结论：Block 即为 Objective-C 的对象。</p>
<h4 id="Block-的构成"><a href="#Block-的构成" class="headerlink" title="Block 的构成"></a>Block 的构成</h4><p>以下是 block 语法的 BN 范式：</p>
<p>Blokc_literal_expression ::= ^block_decl compound_statement_body</p>
<p>block_decl ::=</p>
<p>block_decl ::= parameter_list</p>
<p>block_decl ::= type_expression</p>
<p>如下所示：</p>
<p><strong><code>^</code></strong> <strong><code>返回值类型</code></strong> <strong><code>参数列表</code></strong> <strong><code>表达式</code></strong></p>
<h4 id="表层分析-Block-实质：它是一个类型"><a href="#表层分析-Block-实质：它是一个类型" class="headerlink" title="表层分析 Block 实质：它是一个类型"></a>表层分析 Block 实质：它是一个类型</h4><p>Block 是一种类型，一旦使用了 Block 就相当于生成了可以赋值给 Block 类型变量的类型。举个例子：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> (^blk)(<span class="keyword">int</span>) = ^(<span class="keyword">int</span> count) &#123;</span><br><span class="line">    <span class="keyword">return</span> count + <span class="number">1</span>;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></p>
<ul>
<li>等号左侧代码定义这个 Block 类型：它接受一个 int 参数，返回一个 int 值。</li>
<li>等号右侧代码表示这个 Block 的值：它是左侧定义的 Block 的一种实现。</li>
</ul>
<p>如果我们项目中经常使用某个类型的 Block，可以通过 <code>typeof</code> 来抽象出这种类型的 Block，如下：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 抽象成一种类型</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="keyword">int</span> (^AddOneBlock)(<span class="keyword">int</span> count);</span><br><span class="line"></span><br><span class="line">AddOneBlock block = ^(<span class="keyword">int</span> count) &#123;</span><br><span class="line">    <span class="comment">// 具体实现代码</span></span><br><span class="line">    <span class="keyword">return</span>  count + <span class="number">1</span>;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></p>
<p>这样一来，Block 的赋值和传递就和普通类型一样方便了。</p>
<h4 id="深层分析-Block-的实质：它是-Objective-C-对象"><a href="#深层分析-Block-的实质：它是-Objective-C-对象" class="headerlink" title="深层分析 Block 的实质：它是 Objective-C 对象"></a>深层分析 Block 的实质：它是 Objective-C 对象</h4><p>Block 其实就是 Objective-C 对象。它的结构体内含有 ISA 指针。</p>
<p>下面将 Objective-C 的代码转化为 C++ 来看下实现，这里我把方法命名为 test：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="meta">#include <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="keyword">int</span> test()</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">void</span> (^blk)(<span class="keyword">void</span>) = ^&#123;</span><br><span class="line">        printf(<span class="string">"Block\n"</span>);</span><br><span class="line">    &#125;;</span><br><span class="line">    blk();</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>C++ 核心代码，这里并不是全部代码，抽取了关键部分来说：<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> __<span class="title">block_impl</span> &#123;</span></span><br><span class="line">  <span class="keyword">void</span> *isa;</span><br><span class="line">  <span class="keyword">int</span> Flags;</span><br><span class="line">  <span class="keyword">int</span> Reserved;</span><br><span class="line">  <span class="keyword">void</span> *FuncPtr;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// block 结构体</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> __<span class="title">test_block_impl_0</span> &#123;</span></span><br><span class="line">  <span class="class"><span class="keyword">struct</span> __<span class="title">block_impl</span> <span class="title">impl</span>;</span></span><br><span class="line">  <span class="class"><span class="keyword">struct</span> __<span class="title">test_block_desc_0</span>* <span class="title">Desc</span>;</span></span><br><span class="line">  __test_block_impl_0(<span class="keyword">void</span> *fp, struct __test_block_desc_0 *desc, <span class="keyword">int</span> flags=<span class="number">0</span>) &#123;</span><br><span class="line">    impl.isa = &amp;_NSConcreteStackBlock;</span><br><span class="line">    impl.Flags = flags;</span><br><span class="line">    impl.FuncPtr = fp;</span><br><span class="line">    Desc = desc;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Block 被调用的代码，也是Block被转化为 C 语言的代码</span></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">void</span> __test_block_func_0(struct __test_block_impl_0 *__cself) &#123;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"Block\n"</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 方法的描述</span></span><br><span class="line"><span class="keyword">static</span> <span class="class"><span class="keyword">struct</span> __<span class="title">test_block_desc_0</span> &#123;</span></span><br><span class="line">  <span class="keyword">size_t</span> reserved;</span><br><span class="line">  <span class="keyword">size_t</span> Block_size;</span><br><span class="line">&#125; __test_block_desc_0_DATA = &#123; <span class="number">0</span>, <span class="keyword">sizeof</span>(struct __test_block_impl_0)&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 自己定义的 test 方法</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">test</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">void</span> (*blk)(<span class="keyword">void</span>) = ((<span class="keyword">void</span> (*)())&amp;__test_block_impl_0((<span class="keyword">void</span> *)__test_block_func_0, &amp;__test_block_desc_0_DATA));</span><br><span class="line">    ((<span class="keyword">void</span> (*)(__block_impl *))((__block_impl *)blk)-&gt;FuncPtr)((__block_impl *)blk);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>先来看下 OC 中源代码 block 语法 <code>^{printf(&quot;Block\n&quot;)};</code> 变化成 C++ 后：<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">static</span> <span class="keyword">void</span> __test_block_func_0(struct __test_block_impl_0 *__cself) &#123;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"Block\n"</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>该函数的参数 <strong>cself 相当于 C++ 实例方法中指向自身实例方法的 this，或是 OC 实例方法中指向对象自身的变量 self，即参数 </strong>cself 为指向 Block 值的变量，也是 __test_block_impl_0 结构体的指针。</p>
<p>结合这个方法，可知 <strong>test_block_impl_0 即为这个 Block 的结构体。也就是说 blk 这个 Block就是通过 </strong>test_block_impl_0 构造出来的。</p>
<p>下面看下这个方法：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// block 结构体</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> __<span class="title">test_block_impl_0</span> &#123;</span></span><br><span class="line">  <span class="class"><span class="keyword">struct</span> __<span class="title">block_impl</span> <span class="title">impl</span>;</span></span><br><span class="line">  <span class="class"><span class="keyword">struct</span> __<span class="title">test_block_desc_0</span>* <span class="title">Desc</span>;</span></span><br><span class="line">  <span class="comment">// 构造函数</span></span><br><span class="line">  __test_block_impl_0(<span class="keyword">void</span> *fp, struct __test_block_desc_0 *desc, <span class="keyword">int</span> flags=<span class="number">0</span>) &#123;</span><br><span class="line">    impl.isa = &amp;_NSConcreteStackBlock;</span><br><span class="line">    impl.Flags = flags;</span><br><span class="line">    impl.FuncPtr = fp;</span><br><span class="line">    Desc = desc;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>可以看到，该结构体由三部分组成：</p>
<p>第一个成员变量是 impl，其 __block_impl 结构体声明为：<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> __<span class="title">block_impl</span> &#123;</span></span><br><span class="line">  <span class="keyword">void</span> *isa;            <span class="comment">// 指针</span></span><br><span class="line">  <span class="keyword">int</span> Flags;            <span class="comment">// 标志</span></span><br><span class="line">  <span class="keyword">int</span> Reserved;         <span class="comment">// 今后版本升级所需要的区域</span></span><br><span class="line">  <span class="keyword">void</span> *FuncPtr;        <span class="comment">// 函数指针</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></p>
<p>第二个成员变量时 Desc 指针，用来描述这个 block 的附加信息，__test_block_desc_0 结构体如下：<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 方法的描述</span></span><br><span class="line"><span class="keyword">static</span> <span class="class"><span class="keyword">struct</span> __<span class="title">test_block_desc_0</span> &#123;</span></span><br><span class="line">  <span class="keyword">size_t</span> reserved;          <span class="comment">// 今后版本升级所需要的区域</span></span><br><span class="line">  <span class="keyword">size_t</span> Block_size;        <span class="comment">// block 大小</span></span><br><span class="line">&#125; __test_block_desc_0_DATA = &#123; <span class="number">0</span>, <span class="keyword">sizeof</span>(struct __test_block_impl_0)&#125;;</span><br></pre></td></tr></table></figure></p>
<p>第三部分是 __test_block_impl_0 结构体的构造函数：<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">__test_block_impl_0(<span class="keyword">void</span> *fp, struct __test_block_desc_0 *desc, <span class="keyword">int</span> flags=<span class="number">0</span>) &#123;</span><br><span class="line">  impl.isa = &amp;_NSConcreteStackBlock;</span><br><span class="line">  impl.Flags = flags;</span><br><span class="line">  impl.FuncPtr = fp;</span><br><span class="line">  Desc = desc;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>在这个结构体的构造函数里，ISA 指针保持着所属类的结构体的实例指针。__test_block_impl_0 结构体相当于 Objective-C 类对象的结构体，这里 _NSConcreteStackBlock 相当于 block 结构体的实例。所以说 Block 的实质即为 Objective-C 的对象。</p>
<h4 id="Objective-C-类和对象的实质"><a href="#Objective-C-类和对象的实质" class="headerlink" title="Objective-C 类和对象的实质"></a>Objective-C 类和对象的实质</h4><p><strong>id</strong> 这一变量用于存储 Objective-C 对象。源码中面对 id 类型的声明为：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="comment">/// Represents an instance of a class.</span></span><br><span class="line"><span class="keyword">struct</span> objc_object &#123;</span><br><span class="line">    Class _Nonnull isa  OBJC_ISA_AVAILABILITY;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">/// A pointer to an instance of a class.</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="keyword">struct</span> objc_object *<span class="keyword">id</span>;</span><br></pre></td></tr></table></figure></p>
<p>id 为 objc_object 结构体的指针类型。我们再看下 Class:<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="comment">/// usr/include/objc/objc.h</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="keyword">struct</span> objc_class *Class;</span><br></pre></td></tr></table></figure></p>
<figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="comment">/// usr/include/objc/runtime.h</span></span><br><span class="line"><span class="keyword">struct</span> objc_class &#123;</span><br><span class="line">    Class isa;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>各类的结构体就是基于 objc_class 的结构体的 class_t 结构体。class_t 在 objc4 运行时库的 runtime/objc-runtime-new.h 中声明如下：<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">typedef struct class_t &#123;</span><br><span class="line">    struct class_t *isa;</span><br><span class="line">    struct class_t *superclass;</span><br><span class="line">    Cache cache;</span><br><span class="line">    IMP *vtable;</span><br><span class="line">    class_rw_t *data;</span><br><span class="line">&#125; class_t;</span><br></pre></td></tr></table></figure></p>
<p>这里要说一下，objc4-437 还有这段代码，但是最新的 objc4-756.2 中已经没有了。查看一个 NSObject 对象的 C++ 源码，依然可以找到：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> _class_t &#123;</span><br><span class="line">	<span class="keyword">struct</span> _class_t *isa;</span><br><span class="line">	<span class="keyword">struct</span> _class_t *superclass;</span><br><span class="line">	<span class="keyword">void</span> *cache;</span><br><span class="line">	<span class="keyword">void</span> *vtable;</span><br><span class="line">	<span class="keyword">struct</span> _class_ro_t *ro;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></p>
<p>借用书中的一张图，表达如下：</p>
<p><img src="https://i.loli.net/2019/12/02/WfqAbicCIg9soKy.png" alt="oc类与对象的实质.png"></p>
<h3 id="Block-截获自动变量和对象"><a href="#Block-截获自动变量和对象" class="headerlink" title="Block 截获自动变量和对象"></a>Block 截获自动变量和对象</h3><h4 id="Block-截获自动变量（局部变量）"><a href="#Block-截获自动变量（局部变量）" class="headerlink" title="Block 截获自动变量（局部变量）"></a>Block 截获自动变量（局部变量）</h4><p>使用 Block 时，不仅可以使用其内部参数，还能使用 Block 外部的局部或者全局变量。</p>
<p>当 Block 使用外部的局部变量时，这些变量就会被 Block 保存，即使在 Block 外部修改这些值，存在于 Block 内部的这些变量也不会被修改，如下：<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> a = <span class="number">10</span>;</span><br><span class="line"><span class="keyword">int</span> b = <span class="number">20</span>;</span><br><span class="line"><span class="keyword">void</span> (^printNumbers)(<span class="keyword">void</span>) = ^&#123;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"a = %d\nb = %d\n"</span>, a, b);</span><br><span class="line">&#125;;</span><br><span class="line">printNumbers();</span><br><span class="line"><span class="comment">// a = 10  b = 20</span></span><br><span class="line">a = <span class="number">55</span>;</span><br><span class="line">b = <span class="number">66</span>;</span><br><span class="line">printNumbers();</span><br><span class="line"><span class="comment">// a = 10  b = 20</span></span><br></pre></td></tr></table></figure></p>
<p>如果想要在 Block 内部修改外部局部变量值，会编译错误，提示添加 __block。如果操作外部局部变量，则不会有这问题。如：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="built_in">NSMutableArray</span> *array = [[<span class="built_in">NSMutableArray</span> alloc] init];</span><br><span class="line"><span class="keyword">void</span> (^printNumbers)(<span class="keyword">void</span>) = ^&#123;</span><br><span class="line">    [array addObject:@<span class="number">1</span>];</span><br><span class="line">    <span class="built_in">NSLog</span>(<span class="string">@"array1 = %@\n"</span>, array);</span><br><span class="line">&#125;;</span><br><span class="line">printNumbers();</span><br><span class="line"><span class="built_in">NSLog</span>(<span class="string">@"array2 = %@\n"</span>, array);</span><br><span class="line">[array addObject:@<span class="number">2</span>];</span><br><span class="line">printNumbers();</span><br><span class="line"></span><br><span class="line"><span class="comment">// 输出：</span></span><br><span class="line">array1 = [<span class="number">1</span>]</span><br><span class="line">array2 = [<span class="number">1</span>]</span><br><span class="line">arrar1 = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">1</span>]</span><br></pre></td></tr></table></figure></p>
<p>如果 Block 内部执行 <code>array = [NSMutableArray array];</code> 则会编译错误，提示加上 __block。</p>
<p>根据以上例子，可以暂时总结三点：</p>
<ul>
<li>Block 可以截获局部变量。</li>
<li>修改 Block 外部的局部变量，Block 内部被截获的值不会改变。</li>
<li>直接修改 Block 内部的值，编译错误。但是可以操作局部变量。</li>
</ul>
<p>下面通过 C++ 代码分析下为什么会出现这种现象, C 代码：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">int</span> dmy = <span class="number">256</span>;</span><br><span class="line">    <span class="keyword">int</span> val = <span class="number">10</span>;</span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">char</span> *fmt = <span class="string">"val = %d\n"</span>;</span><br><span class="line">    <span class="keyword">void</span> (^blk)(<span class="keyword">void</span>) = ^ &#123;</span><br><span class="line">        <span class="built_in">printf</span>(fmt, val);</span><br><span class="line">    &#125;;</span><br><span class="line">    val = <span class="number">2</span>;</span><br><span class="line">    fmt = <span class="string">"Value changed. Val = %d\n"</span>;</span><br><span class="line">    blk();      <span class="comment">// 输出： val = 10</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>通过 Clang 将其转化为 C++ 代码，主要代码如下：<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// block 结构体</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> __<span class="title">main_block_impl_0</span> &#123;</span></span><br><span class="line">  <span class="class"><span class="keyword">struct</span> __<span class="title">block_impl</span> <span class="title">impl</span>;</span></span><br><span class="line">  <span class="class"><span class="keyword">struct</span> __<span class="title">main_block_desc_0</span>* <span class="title">Desc</span>;</span></span><br><span class="line">  <span class="keyword">const</span> <span class="keyword">char</span> *fmt;</span><br><span class="line">  <span class="keyword">int</span> val;</span><br><span class="line">  __main_block_impl_0(<span class="keyword">void</span> *fp, struct __main_block_desc_0 *desc, <span class="keyword">const</span> <span class="keyword">char</span> *_fmt, <span class="keyword">int</span> _val, <span class="keyword">int</span> flags=<span class="number">0</span>) : fmt(_fmt), val(_val) &#123;</span><br><span class="line">    impl.isa = &amp;_NSConcreteStackBlock;</span><br><span class="line">    impl.Flags = flags;</span><br><span class="line">    impl.FuncPtr = fp;</span><br><span class="line">    Desc = desc;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">/// block 函数体</span></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">void</span> __main_block_func_0(struct __main_block_impl_0 *__cself) &#123;</span><br><span class="line">  <span class="keyword">const</span> <span class="keyword">char</span> *fmt = __cself-&gt;fmt; <span class="comment">// bound by copy</span></span><br><span class="line">  <span class="keyword">int</span> val = __cself-&gt;val; <span class="comment">// bound by copy</span></span><br><span class="line">    <span class="built_in">printf</span>(fmt, val);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/// block 描述</span></span><br><span class="line"><span class="keyword">static</span> <span class="class"><span class="keyword">struct</span> __<span class="title">main_block_desc_0</span> &#123;</span></span><br><span class="line">  <span class="keyword">size_t</span> reserved;</span><br><span class="line">  <span class="keyword">size_t</span> Block_size;</span><br><span class="line">&#125; __main_block_desc_0_DATA = &#123; <span class="number">0</span>, <span class="keyword">sizeof</span>(struct __main_block_impl_0)&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">/// 定义的 main 方法</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">int</span> dmy = <span class="number">256</span>;</span><br><span class="line">    <span class="keyword">int</span> val = <span class="number">10</span>;</span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">char</span> *fmt = <span class="string">"val = %d\n"</span>;</span><br><span class="line">    <span class="keyword">void</span> (*blk)(<span class="keyword">void</span>) = ((<span class="keyword">void</span> (*)())&amp;__main_block_impl_0((<span class="keyword">void</span> *)__main_block_func_0, &amp;__main_block_desc_0_DATA, fmt, val));</span><br><span class="line">    val = <span class="number">2</span>;</span><br><span class="line">    fmt = <span class="string">"Value changed. Val = %d\n"</span>;</span><br><span class="line">    ((<span class="keyword">void</span> (*)(__block_impl *))((__block_impl *)blk)-&gt;FuncPtr)((__block_impl *)blk);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>单独看 block 的构成结构体 <code>__main_block_impl_0</code>：</p>
<ul>
<li>可以看到，在 block 内部使用的局部变量 val、fmt 作为成员变量被追加到 __main_block_impl_0 结构体中。而 block 中没有使用的局部变量不会追加，如 dmy。</li>
<li>初始化 block 结构体实例时，即 <code>void (*blk)(void) = ((void (*)())&amp;__main_block_impl_0((void *)__main_block_func_0, &amp;__main_block_desc_0_DATA, fmt, val));</code>，截获了局部变量 val 和 fmt 来初始化 block 的结构体实例。</li>
</ul>
<p>再看一下 block 函数体代码：<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">/// block 函数体</span></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">void</span> __main_block_func_0(struct __main_block_impl_0 *__cself) &#123;</span><br><span class="line">  <span class="keyword">const</span> <span class="keyword">char</span> *fmt = __cself-&gt;fmt; <span class="comment">// bound by copy</span></span><br><span class="line">  <span class="keyword">int</span> val = __cself-&gt;val; <span class="comment">// bound by copy</span></span><br><span class="line">    <span class="built_in">printf</span>(fmt, val);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>可知， val 、fmt 都是从 cself 截获的，而 cself 是指向 block 的对象。所以也能说，val 和 fmt 是属于 block 的。而且从 Clang 生成的注释 <code>bound by copy</code> ，表示对这两个对象做了只读值拷贝，而不是指针传递。所以即使改变了 block 外部局部变量的值，block 内部拷贝的值，也不会发生改变。</p>
<p>不过可以使用 <strong>block 修饰局部变量，来满足 block 内部修改的需求。后面会对 </strong>block 做详细分析。</p>
<h4 id="改变存储于特殊存储区域的变量"><a href="#改变存储于特殊存储区域的变量" class="headerlink" title="改变存储于特殊存储区域的变量"></a>改变存储于特殊存储区域的变量</h4><p>在 Block 内部，下列几种值是可以访问和修改的：</p>
<ul>
<li>全局变量</li>
<li>静态全局变量</li>
<li>静态变量</li>
</ul>
<p>下面通过源码对比来分析下：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="keyword">int</span> global_val = <span class="number">1</span>;     <span class="comment">// 全局变量</span></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">int</span> static_global_val = <span class="number">2</span>;       <span class="comment">// 全局静态变量</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 局部静态变量</span></span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">int</span> static_val = <span class="number">3</span>;</span><br><span class="line">    <span class="keyword">void</span> (^blk)(<span class="keyword">void</span>) = ^ &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">"global_val = %d\nglobal_val = %d\nstatic_val = %d\n"</span>, global_val, static_global_val, static_val);</span><br><span class="line">        global_val = <span class="number">11</span>;</span><br><span class="line">        static_global_val = <span class="number">12</span>;</span><br><span class="line">        static_val = <span class="number">13</span>;</span><br><span class="line">        </span><br><span class="line">    &#125;;</span><br><span class="line">    global_val = <span class="number">21</span>;</span><br><span class="line">    static_global_val = <span class="number">22</span>;</span><br><span class="line">    static_val = <span class="number">23</span>;</span><br><span class="line">    blk();</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"global_val2 = %d\nglobal_val2 = %d\nstatic_val2 = %d\n"</span>, global_val, static_global_val, static_val);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>调用该方法，打印如下：<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">global_val = 21</span><br><span class="line">global_val = 22</span><br><span class="line">static_val = 23</span><br><span class="line">global_val2 = 11</span><br><span class="line">global_val2 = 12</span><br><span class="line">static_val2 = 13</span><br></pre></td></tr></table></figure></p>
<p>可知：</p>
<ul>
<li>这三种类型的值，可以再 Block 内部修该，并且内外修改相互影响。</li>
</ul>
<p>转化成 C++ 代码：<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">/// 定义变量</span></span><br><span class="line"><span class="keyword">int</span> global_val = <span class="number">1</span>;</span><br><span class="line"><span class="keyword">static</span> <span class="keyword">int</span> static_global_val = <span class="number">2</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">/// block 结构体</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> __<span class="title">main_block_impl_0</span> &#123;</span></span><br><span class="line">  <span class="class"><span class="keyword">struct</span> __<span class="title">block_impl</span> <span class="title">impl</span>;</span></span><br><span class="line">  <span class="class"><span class="keyword">struct</span> __<span class="title">main_block_desc_0</span>* <span class="title">Desc</span>;</span></span><br><span class="line">  <span class="keyword">int</span> *static_val;</span><br><span class="line">  __main_block_impl_0(<span class="keyword">void</span> *fp, struct __main_block_desc_0 *desc, <span class="keyword">int</span> *_static_val, <span class="keyword">int</span> flags=<span class="number">0</span>) : static_vtypedefal(_static_val) &#123;</span><br><span class="line">    impl.isa = &amp;_NSConcreteStackBlock;</span><br><span class="line">    impl.Flags = flags;</span><br><span class="line">    impl.FuncPtr = fp;</span><br><span class="line">    Desc = desc;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">/// block  函数体</span></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">void</span> __main_block_func_0(struct __main_block_impl_0 *__cself) &#123;</span><br><span class="line">  <span class="keyword">int</span> *static_val = __cself-&gt;static_val; <span class="comment">// bound by copy</span></span><br><span class="line"></span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">"global_val = %d\nglobal_val = %d\nstatic_val = %d\n"</span>, global_val, static_global_val, (*static_val));</span><br><span class="line">        global_val = <span class="number">11</span>;</span><br><span class="line">        static_global_val = <span class="number">12</span>;</span><br><span class="line">        (*static_val) = <span class="number">13</span>;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// block 描述</span></span><br><span class="line"><span class="keyword">static</span> <span class="class"><span class="keyword">struct</span> __<span class="title">main_block_desc_0</span> &#123;</span></span><br><span class="line">  <span class="keyword">size_t</span> reserved;</span><br><span class="line">  <span class="keyword">size_t</span> Block_size;</span><br><span class="line">&#125; __main_block_desc_0_DATA = &#123; <span class="number">0</span>, <span class="keyword">sizeof</span>(struct __main_block_impl_0)&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">int</span> static_val = <span class="number">3</span>;</span><br><span class="line">    <span class="keyword">void</span> (*blk)(<span class="keyword">void</span>) = ((<span class="keyword">void</span> (*)())&amp;__main_block_impl_0((<span class="keyword">void</span> *)__main_block_func_0, &amp;__main_block_desc_0_DATA, &amp;static_val));</span><br><span class="line">    global_val = <span class="number">21</span>;</span><br><span class="line">    static_global_val = <span class="number">22</span>;</span><br><span class="line">    static_val = <span class="number">23</span>;</span><br><span class="line">    ((<span class="keyword">void</span> (*)(__block_impl *))((__block_impl *)blk)-&gt;FuncPtr)((__block_impl *)blk);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"global_val2 = %d\nglobal_val2 = %d\nstatic_val2 = %d\n"</span>, global_val, static_global_val, static_val);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>通过 <code>__main_block_impl_0</code> 结构体可以看到，全局变量、静态全局变量并没被截获到 block 内部，而局部静态变量 static_val 也是通过指针访问。所以他们在 block 内外的改变都是相互影响的，用的是同一份值。</p>
<h4 id="Block-截获对象"><a href="#Block-截获对象" class="headerlink" title="Block 截获对象"></a>Block 截获对象</h4><p>下面看一下 Block 语法中使用 Array 对象的代码，Array已经超过其作用域：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="keyword">void</span> testBlock() &#123;</span><br><span class="line">    <span class="keyword">typedef</span> <span class="keyword">void</span>(^blk_t)(<span class="keyword">id</span> obj);</span><br><span class="line">    blk_t blk;</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">id</span> array = [[<span class="built_in">NSMutableArray</span> alloc] init];</span><br><span class="line">        blk = [^(<span class="keyword">id</span> obj) &#123;</span><br><span class="line">            [array addObject:obj];</span><br><span class="line">            <span class="built_in">NSLog</span>(<span class="string">@"array count = %ld"</span>, [array count]);</span><br><span class="line">        &#125; <span class="keyword">copy</span>];</span><br><span class="line">    &#125;</span><br><span class="line">    blk([[<span class="built_in">NSObject</span> alloc] init]);</span><br><span class="line">    blk([[<span class="built_in">NSObject</span> alloc] init]);</span><br><span class="line">    blk([[<span class="built_in">NSObject</span> alloc] init]);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>调用 testBlock() 方法，控制台打印：<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">ViewController.m:31	array count = 1</span><br><span class="line">ViewController.m:31	array count = 2</span><br><span class="line">ViewController.m:31	array count = 3</span><br></pre></td></tr></table></figure></p>
<p>array 在超出其作用域后，并没有被彻底销毁，看一下其 C++ 代码实现：<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> __<span class="title">testBlock_block_impl_0</span> &#123;</span></span><br><span class="line">  <span class="class"><span class="keyword">struct</span> __<span class="title">block_impl</span> <span class="title">impl</span>;</span></span><br><span class="line">  <span class="class"><span class="keyword">struct</span> __<span class="title">testBlock_block_desc_0</span>* <span class="title">Desc</span>;</span></span><br><span class="line">  id <span class="built_in">array</span>;     <span class="comment">// 截获的对象</span></span><br><span class="line">  __testBlock_block_impl_0(<span class="keyword">void</span> *fp, struct __testBlock_block_desc_0 *desc, id _array, <span class="keyword">int</span> flags=<span class="number">0</span>) : <span class="built_in">array</span>(_array) &#123;</span><br><span class="line">    impl.isa = &amp;_NSConcreteStackBlock;</span><br><span class="line">    impl.Flags = flags;</span><br><span class="line">    impl.FuncPtr = fp;</span><br><span class="line">    Desc = desc;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></p>
<p>在 OC 中， C 语言的结构体不能含有 __strong 修饰符的变量，因为编译器不知道应该何时进行 C 语言结构体的初始化和废弃操作，不能很好地管理内存。</p>
<p>但是 OC 的运行时库能够准确的把握 Block 从栈复制到堆以及堆上的 Block 函数被废弃的时机。在实现上是通过 <code>__testBlock_block_copy_0</code> 和 <code>__testBlock_block_dispose_0</code> 两个函数进行的：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="keyword">static</span> <span class="keyword">void</span> __testBlock_block_copy_0(<span class="keyword">struct</span> __testBlock_block_impl_0*dst, <span class="keyword">struct</span> __testBlock_block_impl_0*src) &#123;</span><br><span class="line">    _Block_object_assign((<span class="keyword">void</span>*)&amp;dst-&gt;array, (<span class="keyword">void</span>*)src-&gt;array, <span class="number">3</span><span class="comment">/*BLOCK_FIELD_IS_OBJECT*/</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">void</span> __testBlock_block_dispose_0(<span class="keyword">struct</span> __testBlock_block_impl_0*src) &#123;</span><br><span class="line">    _Block_object_dispose((<span class="keyword">void</span>*)src-&gt;array, <span class="number">3</span><span class="comment">/*BLOCK_FIELD_IS_OBJECT*/</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>其中，<code>_Block_object_assign</code> 函数相当于 retain 实例方法的函数，将对象赋值在对象类型的结构体成员变量中。 <code>_Block_object_dispose</code> 函数相当于调用 release 实例方法的函数，释放赋值在对象结类型的构体结构体成员变量中的对象。</p>
<p>这两个函数在源码中并没有被调用，而是在 Block 从栈复制到堆上时以及堆上的 block 被废弃时会调用这些函数，如下表：<strong>调用 copy 函数 和 dispose 函数的时机</strong><br>函数|调用时机<br>:—–|——–|<br>copy 函数|栈上的 Block 复制到堆时<br>dispose 函数 | 堆上的 Block 被废弃时</p>
<p><strong>什么时候栈上的 Block 会复制到堆上呢？</strong></p>
<ul>
<li>调用 Block 的 copy 实例方法时</li>
<li>Block 作为函数的返回值时</li>
<li>将 Block 赋值给附有 __strong 的修饰符 id 类型的类，或 Block 类型成员变量时</li>
<li>在方法名中含有 usingBlock 的 Cocoa 框架方法或者 GCD 的 API 中传递 Block 时</li>
</ul>
<p>上面四种情况，栈上的 Block 都会复制到堆上，但其实本质都可以归结为 block_copy 函数被调用时 Block 从栈复制到堆。</p>
<p><strong>特殊说明</strong>：针对如下修改，书中说程序会强制结束：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="keyword">void</span> testBlock() &#123;</span><br><span class="line">    <span class="keyword">typedef</span> <span class="keyword">void</span>(^blk_t)(<span class="keyword">id</span> obj);</span><br><span class="line">    blk_t blk;</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">id</span> array = [[<span class="built_in">NSMutableArray</span> alloc] init];</span><br><span class="line">        blk = ^(<span class="keyword">id</span> obj) &#123;</span><br><span class="line">            [array addObject:obj];</span><br><span class="line">            <span class="built_in">NSLog</span>(<span class="string">@"array count = %ld"</span>, [array count]);</span><br><span class="line">        &#125;;</span><br><span class="line">    &#125;</span><br><span class="line">    blk([[<span class="built_in">NSObject</span> alloc] init]);</span><br><span class="line">    blk([[<span class="built_in">NSObject</span> alloc] init]);</span><br><span class="line">    blk([[<span class="built_in">NSObject</span> alloc] init]);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>但是我测试时，调用 testBlock 函数依然输出：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">ViewController.m:<span class="number">34</span>	array count = <span class="number">1</span></span><br><span class="line">ViewController.m:<span class="number">34</span>	array count = <span class="number">2</span></span><br><span class="line">ViewController.m:<span class="number">34</span>	array count = <span class="number">3</span></span><br></pre></td></tr></table></figure></p>
<p>不确定是 Apple 之后做了修改，还是我不应该借用 OC 的机制这么写。</p>
<p>另外， <code>__testBlock_block_copy_0</code> 函数是否存在，与是否调用了 copy 实例方法并没有关系。</p>
<p>相对的，在释放复制到堆上的 Block 后，谁都不持有 Block 而被废弃时调用 dispose 函数，相当于对象的 dealloc 方法。比如在使用了 __weak 关键字时：</p>
<figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="keyword">void</span> testBlock() &#123;</span><br><span class="line">    <span class="keyword">typedef</span> <span class="keyword">void</span>(^blk_t)(<span class="keyword">id</span> obj);</span><br><span class="line">    blk_t blk;</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">id</span> array0 = [[<span class="built_in">NSMutableArray</span> alloc] init];</span><br><span class="line">        <span class="keyword">id</span> __<span class="keyword">weak</span> array = array0;</span><br><span class="line">        blk = ^(<span class="keyword">id</span> obj) &#123;</span><br><span class="line">            [array addObject:obj];</span><br><span class="line">            <span class="built_in">NSLog</span>(<span class="string">@"array count = %ld"</span>, [array count]);</span><br><span class="line">        &#125;;</span><br><span class="line">    &#125;</span><br><span class="line">    blk([[<span class="built_in">NSObject</span> alloc] init]);</span><br><span class="line">    blk([[<span class="built_in">NSObject</span> alloc] init]);</span><br><span class="line">    blk([[<span class="built_in">NSObject</span> alloc] init]);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>输出：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">ViewController.m:<span class="number">34</span>	array count = <span class="number">0</span></span><br><span class="line">ViewController.m:<span class="number">34</span>	array count = <span class="number">0</span></span><br><span class="line">ViewController.m:<span class="number">34</span>	array count = <span class="number">0</span></span><br></pre></td></tr></table></figure></p>
<h3 id="block-的实现原理"><a href="#block-的实现原理" class="headerlink" title="__block 的实现原理"></a>__block 的实现原理</h3><h4 id="block-修饰局部变量"><a href="#block-修饰局部变量" class="headerlink" title="__block 修饰局部变量"></a>__block 修饰局部变量</h4><p><code>__block 说明符</code> 更精确的表达方式应该是 <code>__block 存储域类说明符（__block storage-class-specifier）</code>。C 语言有如下存储域类说明符：</p>
<ul>
<li>typedef</li>
<li>extern</li>
<li>static</li>
<li>auto</li>
<li>register</li>
</ul>
<p><strong>block 说明符类似 static、auto和 register 说明符，用于指定将变量值放到哪个存储域中。例如，auto 表示作为自动变量存储在栈中，static 表示作为静态变量存储在数据区中。给自动变量加上 </strong>block 说明符后，就会改变这个自动变量的存储区域。</p>
<p>下面通过给局部变量添加 __block 关键字后效果来分析：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="keyword">void</span>(^PrintNumbers)(<span class="keyword">void</span>);</span><br><span class="line"><span class="keyword">void</span> lineBlock() &#123;</span><br><span class="line">    __block <span class="keyword">int</span> a = <span class="number">10</span>;</span><br><span class="line">    <span class="keyword">int</span> b = <span class="number">20</span>;</span><br><span class="line">    PrintNumbers printBlock = ^() &#123;</span><br><span class="line">        a -= <span class="number">10</span>;</span><br><span class="line">        printf(<span class="string">"a1 = %d, b1 = %d\n"</span>, a, b);</span><br><span class="line">    &#125;;</span><br><span class="line">    printBlock();</span><br><span class="line">    a += <span class="number">20</span>;</span><br><span class="line">    b += <span class="number">20</span>;</span><br><span class="line">    printf(<span class="string">"a2 = %d, b2 = %d\n"</span>, a, b);</span><br><span class="line">    printBlock();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>输出：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">a1 = <span class="number">0</span>, b1 = <span class="number">20</span></span><br><span class="line">a2 = <span class="number">20</span>, b2 = <span class="number">40</span></span><br><span class="line">a1 = <span class="number">10</span>, b1 = <span class="number">20</span></span><br></pre></td></tr></table></figure></p>
<p>附有 <strong>block 修饰符的变量称之为 </strong>block 变量，可以看到，__block 变量在 Block 内部可以被修改，且修改的值被同步到 Block 作用域外。</p>
<p>下面用 Clang 工具看一下 C++ 代码：<br>OC 代码：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="keyword">void</span> lineBlock() &#123;</span><br><span class="line">    __block <span class="keyword">int</span> valNumber= <span class="number">10</span>;</span><br><span class="line">    <span class="keyword">void</span> (^blk)(<span class="keyword">void</span>) = ^&#123;</span><br><span class="line">        valNumber = <span class="number">1</span>;</span><br><span class="line">    &#125;;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>C++ 代码：<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> __<span class="title">Block_byref_valNumber_0</span> &#123;</span></span><br><span class="line">  <span class="keyword">void</span> *__isa;</span><br><span class="line">__Block_byref_valNumber_0 *__forwarding;</span><br><span class="line"> <span class="keyword">int</span> __flags;</span><br><span class="line"> <span class="keyword">int</span> __size;</span><br><span class="line"> <span class="keyword">int</span> valNumber;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> __<span class="title">lineBlock_block_impl_0</span> &#123;</span></span><br><span class="line">  <span class="class"><span class="keyword">struct</span> __<span class="title">block_impl</span> <span class="title">impl</span>;</span></span><br><span class="line">  <span class="class"><span class="keyword">struct</span> __<span class="title">lineBlock_block_desc_0</span>* <span class="title">Desc</span>;</span></span><br><span class="line">  __Block_byref_valNumber_0 *valNumber; <span class="comment">// by ref</span></span><br><span class="line">  __lineBlock_block_impl_0(<span class="keyword">void</span> *fp, struct __lineBlock_block_desc_0 *desc, __Block_byref_valNumber_0 *_valNumber, <span class="keyword">int</span> flags=<span class="number">0</span>) : valNumber(_valNumber-&gt;__forwarding) &#123;</span><br><span class="line">    impl.isa = &amp;_NSConcreteStackBlock;</span><br><span class="line">    impl.Flags = flags;</span><br><span class="line">    impl.FuncPtr = fp;</span><br><span class="line">    Desc = desc;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="keyword">static</span> <span class="keyword">void</span> __lineBlock_block_func_0(struct __lineBlock_block_impl_0 *__cself) &#123;</span><br><span class="line">  __Block_byref_valNumber_0 *valNumber = __cself-&gt;valNumber; <span class="comment">// bound by ref</span></span><br><span class="line"></span><br><span class="line">        (valNumber-&gt;__forwarding-&gt;valNumber) = <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line"><span class="keyword">static</span> <span class="keyword">void</span> __lineBlock_block_copy_0(struct __lineBlock_block_impl_0*dst, struct __lineBlock_block_impl_0*src) &#123;_Block_object_assign((<span class="keyword">void</span>*)&amp;dst-&gt;valNumber, (<span class="keyword">void</span>*)src-&gt;valNumber, <span class="number">8</span><span class="comment">/*BLOCK_FIELD_IS_BYREF*/</span>);&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">void</span> __lineBlock_block_dispose_0(struct __lineBlock_block_impl_0*src) &#123;_Block_object_dispose((<span class="keyword">void</span>*)src-&gt;valNumber, <span class="number">8</span><span class="comment">/*BLOCK_FIELD_IS_BYREF*/</span>);&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">static</span> <span class="class"><span class="keyword">struct</span> __<span class="title">lineBlock_block_desc_0</span> &#123;</span></span><br><span class="line">  <span class="keyword">size_t</span> reserved;</span><br><span class="line">  <span class="keyword">size_t</span> Block_size;</span><br><span class="line">  <span class="keyword">void</span> (*copy)(struct __lineBlock_block_impl_0*, struct __lineBlock_block_impl_0*);</span><br><span class="line">  <span class="keyword">void</span> (*dispose)(struct __lineBlock_block_impl_0*);</span><br><span class="line">&#125; __lineBlock_block_desc_0_DATA = &#123; <span class="number">0</span>, <span class="keyword">sizeof</span>(struct __lineBlock_block_impl_0), __lineBlock_block_copy_0, __lineBlock_block_dispose_0&#125;;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">lineBlock</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    __attribute__((__blocks__(byref))) __Block_byref_valNumber_0 valNumber = &#123;(<span class="keyword">void</span>*)<span class="number">0</span>,(__Block_byref_valNumber_0 *)&amp;valNumber, <span class="number">0</span>, <span class="keyword">sizeof</span>(__Block_byref_valNumber_0), <span class="number">10</span>&#125;;</span><br><span class="line">    <span class="keyword">void</span> (*blk)(<span class="keyword">void</span>) = ((<span class="keyword">void</span> (*)())&amp;__lineBlock_block_impl_0((<span class="keyword">void</span> *)__lineBlock_block_func_0, &amp;__lineBlock_block_desc_0_DATA, (__Block_byref_valNumber_0 *)&amp;valNumber, <span class="number">570425344</span>));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>可以看到：</p>
<ul>
<li>被 <strong>block 修饰的自动变量，被转变为 </strong>Block_byref_valNumber_0 结构体存在，即栈上生成 __Block_byref_valNumber_0 结构体实例。</li>
<li>block 的构造结构体 <code>__lineBlock_block_impl_0</code> 中可以看到，通过 __block 变量的结构体指针访问该变量。</li>
<li>block 函数实现方法 <code>__lineBlock_block_func_0</code> 中看到，block 内部通过 <code>valNumber-&gt;__forwarding-&gt;valNumbe</code> 指针修改 __block 变量。</li>
</ul>
<p>关于 __block 变量的结构体实现：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> __Block_byref_valNumber_0 &#123;</span><br><span class="line">  <span class="keyword">void</span> *__isa;</span><br><span class="line">__Block_byref_valNumber_0 *__forwarding;</span><br><span class="line"> <span class="keyword">int</span> __flags;</span><br><span class="line"> <span class="keyword">int</span> __size;</span><br><span class="line"> <span class="keyword">int</span> valNumber;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></p>
<ul>
<li>结构体的成员变量 valNumber 持有原本变量的字面值。</li>
<li><strong>forwarding: 结构体实例的成员变量 </strong>forwarding 持有指向该实例自身的指针，可以通过成员变量 <strong>forwarding 访问成员变量 valNumber，即 `valNumber-&gt;</strong>forwarding-&gt;valNumbe`。如下图：</li>
</ul>
<p><img src="https://i.loli.net/2019/12/09/xe4bmBUv7k6yLcp.png" alt="访问__block变量.png"></p>
<p>而 <code>__Block_byref_valNumber_0</code> 结构体之所以不在 <code>__lineBlock_block_impl_0</code> 结构体内部定义，是为了在多个 block 中共用一个 __block 变量结构体。</p>
<p>另外，当 Block 从栈复制到堆时，被 Block 持有的 <strong>block 变量也会复制到堆上，注意，这里会增加 </strong>block 变量的引用计数。同时，如果 Block 被废弃，持有的 __block 变量也会被释放。</p>
<p><strong>不管 __block 变量配置在栈上还是堆上，都能够正确的访问该变量</strong>。通过下面这个图可以更深刻的理解这句话：</p>
<p><img src="https://i.loli.net/2019/12/09/Tx1u68lUoPiXCbz.png" alt="复制__block变量.png"></p>
<h4 id="block-修饰对象"><a href="#block-修饰对象" class="headerlink" title="__block 修饰对象"></a>__block 修饰对象</h4><p>__block 说明符可以用来指定任何类型的自动变量，下面用来指定 id 类型的自动变量：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="keyword">void</span> testBlock() &#123;</span><br><span class="line">    __block <span class="keyword">id</span> obj = [[<span class="built_in">NSObject</span> alloc] init];</span><br><span class="line">    <span class="keyword">void</span> (^blk)(<span class="keyword">void</span>) = ^&#123;</span><br><span class="line">        obj = [<span class="built_in">NSObject</span> new];</span><br><span class="line">    &#125;;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>ARC 有效时，id 类型以及对象类型变量必定附加所有权修饰符,缺省为附有 <strong>strong 修饰符的变量。改代码通过 clang 转换。 </strong>block 变量的结构体如下：<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> __<span class="title">Block_byref_obj_0</span> &#123;</span></span><br><span class="line">  <span class="keyword">void</span> *__isa;</span><br><span class="line">__Block_byref_obj_0 *__forwarding;</span><br><span class="line"> <span class="keyword">int</span> __flags;</span><br><span class="line"> <span class="keyword">int</span> __size;</span><br><span class="line"> <span class="keyword">void</span> (*__Block_byref_id_object_copy)(<span class="keyword">void</span>*, <span class="keyword">void</span>*);</span><br><span class="line"> <span class="keyword">void</span> (*__Block_byref_id_object_dispose)(<span class="keyword">void</span>*);</span><br><span class="line"> id obj;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></p>
<p>被 __strong 修饰的 id  类型或对象类型，生成了 copy 和 dispose 两个方法:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">static</span> <span class="keyword">void</span> __testBlock_block_copy_0(struct __testBlock_block_impl_0*dst, struct __testBlock_block_impl_0*src) &#123;_Block_object_assign((<span class="keyword">void</span>*)&amp;dst-&gt;obj, (<span class="keyword">void</span>*)src-&gt;obj, <span class="number">8</span><span class="comment">/*BLOCK_FIELD_IS_BYREF*/</span>);&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">void</span> __testBlock_block_dispose_0(struct __testBlock_block_impl_0*src) &#123;_Block_object_dispose((<span class="keyword">void</span>*)src-&gt;obj, <span class="number">8</span><span class="comment">/*BLOCK_FIELD_IS_BYREF*/</span>);&#125;</span><br></pre></td></tr></table></figure></p>
<ul>
<li>如果 __block 对象变量从栈复制到堆，使用 _Block_object_assign 方法。</li>
<li>当堆上的 __block 对象变量被废弃时，使用 _Block_object_dispose 方法。</li>
</ul>
<p>另外，<strong>weak 与 </strong>block 同时使用，clang 时报错 <code>cannot create __weak reference because the current deployment target
      does not support weak references</code>。</p>
<p>说明不支持 __weak。</p>
<h3 id="Block-存储域，即三种-Block"><a href="#Block-存储域，即三种-Block" class="headerlink" title="Block 存储域，即三种 Block"></a>Block 存储域，即三种 Block</h3><p>通过前面说明可知， Block 转换为 Block 结构体类型的自动变量， <strong>block 变量转换为 </strong>block 变量的结构体类型的自动变量。而所谓 <strong>结构体类型的自动变量</strong>，即栈上生成的该结构体的实例。</p>
<p><strong>Block</strong> 与 <strong>__block</strong> 变量的实质：<br>名称|实质<br>:——–|———|<br>Block|栈上 Block 的结构体实例<br><strong>block变量|栈上 </strong>block 变量的结构体实例</p>
<p>而 Block 根据存储域的不同，可以归结为三类：<br>Block 的类|存储域|拷贝效果<br>:——-|——–|———-|<br>_NSConcreteStackBlock|栈|从栈拷贝到堆<br>_NSConcreteGlobalBlock|程序的数据区域(.data)|什么也不做<br>_NSConcreteMallocBlock|堆|引用计数增加</p>
<h4 id="全局-Block-NSConcreteGlobalBlock"><a href="#全局-Block-NSConcreteGlobalBlock" class="headerlink" title="全局 Block: _NSConcreteGlobalBlock"></a>全局 Block: _NSConcreteGlobalBlock</h4><p>因为全局 Block 的结构体实例设置在程序的数据存储区，可以在程序任意位置通知指针来访问，只有两种情况下会产生全局 Block：</p>
<ul>
<li>记述全局变量的地方有 Block 语法时。</li>
<li>Block 语法的表达式中不截获自动变量时。</li>
</ul>
<p>关于情况一，如下：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">void</span>(^<span class="keyword">blk_t</span>)(<span class="keyword">void</span>) = ^&#123;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"Global Block.\n"</span>);</span><br><span class="line">&#125;;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">test</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">blk_t</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>通过 Clang 分析代码，获取 Block 的结构体实例如下，可知确实是全局 Block:<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">struct __blk_t_block_impl_0 &#123;</span><br><span class="line">  struct __block_impl impl;</span><br><span class="line">  struct __blk_t_block_desc_0* Desc;</span><br><span class="line">  __blk_t_block_impl_0(void *fp, struct __blk_t_block_desc_0 *desc, int flags=0) &#123;</span><br><span class="line">    impl.isa = &amp;_NSConcreteGlobalBlock;</span><br><span class="line">    impl.Flags = flags;</span><br><span class="line">    impl.FuncPtr = fp;</span><br><span class="line">    Desc = desc;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></p>
<p><strong>特别提示</strong>： 第二种情况我按照书中测试，即：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="function"><span class="keyword">typedef</span> <span class="title">int</span> <span class="params">(^BlockNum)</span> <span class="params">(<span class="keyword">int</span>)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">test</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> rate = <span class="number">0</span>; rate &lt; <span class="number">5</span>; ++rate) &#123;</span><br><span class="line">        BlockNum num = ^(<span class="keyword">int</span> count) &#123;</span><br><span class="line">            <span class="keyword">return</span> count;</span><br><span class="line">        &#125;;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>该 Block 的结构体如下<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">typedef</span> <span class="title">int</span> <span class="params">(*BlockNum)</span> <span class="params">(<span class="keyword">int</span>)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> __<span class="title">test_block_impl_0</span> &#123;</span></span><br><span class="line">  <span class="class"><span class="keyword">struct</span> __<span class="title">block_impl</span> <span class="title">impl</span>;</span></span><br><span class="line">  <span class="class"><span class="keyword">struct</span> __<span class="title">test_block_desc_0</span>* <span class="title">Desc</span>;</span></span><br><span class="line">  __test_block_impl_0(<span class="keyword">void</span> *fp, struct __test_block_desc_0 *desc, <span class="keyword">int</span> flags=<span class="number">0</span>) &#123;</span><br><span class="line">    impl.isa = &amp;_NSConcreteStackBlock;</span><br><span class="line">    impl.Flags = flags;</span><br><span class="line">    impl.FuncPtr = fp;</span><br><span class="line">    Desc = desc;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></p>
<p>依然被认为存储在栈上，这里暂时不确定原因。</p>
<h4 id="栈-Block：-NSConcreteStackBlock"><a href="#栈-Block：-NSConcreteStackBlock" class="headerlink" title="栈 Block：_NSConcreteStackBlock"></a>栈 Block：_NSConcreteStackBlock</h4><p>在生成 Block 后，如果这个 Block 不是全局 Block，那么它就是栈 Block，即 _NSConcreteStackBlock 对象。</p>
<p>配置在全局变量上的 Block，从变量作用域外也可通过指针安全的使用。但设置在栈上的 Block，如果其所属变量作用域结束，该 Block 就被废弃。由于 <strong>block 变量也配置在栈上，同样，其所属的变量作用域结束，则该 </strong>block 变量也被废弃。</p>
<p>Blocks 机制提供了将 Block 和 __block 变量从栈上复制到堆上的方法来解决这个问题。将配置在栈上的 Block 复制到堆上，这样即使 Block 语法记述的变量作用域结束，堆上的 Block 还可以继续存在。</p>
<h4 id="堆-Block：-NSConcreteMallocBlock"><a href="#堆-Block：-NSConcreteMallocBlock" class="headerlink" title="堆 Block：_NSConcreteMallocBlock"></a>堆 Block：_NSConcreteMallocBlock</h4><p>将栈 Block 复制到堆后，block结构体的 ISA 成员变量变成了 _NSConcreteMallocBlock。</p>
<p>在对 Block 使用 copy 实例方法时:<br>Block 的类|副本源的配置存储域|复制（copy）效果<br>:——-|——–|——–|<br>_NSConcreteStackBlock|栈|从栈拷贝到堆<br>_NSConcreteGlobalBlock|程序的数据区域|什么也不做<br>_NSConcreteMallocBlock|堆|引用计数增加</p>
<blockquote>
<p>不管 Block 配置之在何处，用 copy 方法都不会引起任何问题，在不确定时调用 copy 方法即可。</p>
</blockquote>
<p>在 ARC 中不能显示调用 release，多次调用 copy 方法依然没有问题，如下：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="keyword">int</span> (^BlockNum) (<span class="keyword">int</span>);</span><br><span class="line"><span class="keyword">void</span> test(<span class="keyword">int</span> rate) &#123;</span><br><span class="line">    BlockNum num = ^(<span class="keyword">int</span> count) &#123; <span class="keyword">return</span> count * rate;&#125;;</span><br><span class="line">    num = [[[[num <span class="keyword">copy</span>] <span class="keyword">copy</span>] <span class="keyword">copy</span>] <span class="keyword">copy</span>];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>该源代码可以解释为：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">    BlockNum tmp = [num <span class="keyword">copy</span>];</span><br><span class="line">    num = tmp;</span><br><span class="line">&#125;</span><br><span class="line">&#123;</span><br><span class="line">    BlockNum tmp = [num <span class="keyword">copy</span>];</span><br><span class="line">    num = tmp;</span><br><span class="line">&#125;</span><br><span class="line">&#123;</span><br><span class="line">    BlockNum tmp = [num <span class="keyword">copy</span>];</span><br><span class="line">    num = tmp;</span><br><span class="line">&#125;</span><br><span class="line">&#123;</span><br><span class="line">    BlockNum tmp = [num <span class="keyword">copy</span>];</span><br><span class="line">    num = tmp;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>内存管理并不会存在问题。</p>
<p>大多数情况下，编译器会自行判断将 Block 从栈上复制到堆上：</p>
<ul>
<li>block 作为函数返回值的时候。</li>
<li>部分情况下向方法或函数中传递 Block 时。部分情况主要是下面两种情况：<ul>
<li>Cocoa 框架的方法且方法名中含有 usingBlock 时。</li>
<li>GCD 中的 API。</li>
</ul>
</li>
</ul>
<p>除了以上两种情况，我们基本是都要手动 copy Block，才能将其从栈复制到堆。</p>
<p>那么 __block 变量在 Block 执行 copy 操作后会发生什么呢？</p>
<ul>
<li>任何一个 Block 被复制到堆上时，使用的 __block 变量也会复制到堆上，并被该 Block 持有。</li>
<li>如果接着有其他 Block 复制到堆上，被复制的 Block 也持有 <strong>block 变量，则会增加 </strong>block 变量的引用计数。反过来如果堆上 Block 被废弃，它所持有的 __block 变量也会被释放。</li>
</ul>
<h3 id="Block-循环引用"><a href="#Block-循环引用" class="headerlink" title="Block 循环引用"></a>Block 循环引用</h3><p>如果在 Block 内部使用附有 __strong 修饰符的对象类型自动变量，那么当 Block 从栈复制到堆时，该对象为 Block 所持有。这样容易引起循环引用。如下：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="keyword">void</span>(^blk_t)(<span class="keyword">void</span>);</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">@interface</span> <span class="title">Person</span> : <span class="title">NSObject</span></span></span><br><span class="line">&#123;</span><br><span class="line">    blk_t blk_;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">@implementation</span> <span class="title">Person</span></span></span><br><span class="line"></span><br><span class="line">- (<span class="keyword">instancetype</span>)init</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">self</span> = [<span class="keyword">super</span> init];</span><br><span class="line">    blk_ = ^&#123;</span><br><span class="line">        <span class="built_in">NSLog</span>(<span class="string">@"self = %@"</span>,<span class="keyword">self</span>);</span><br><span class="line">    &#125;;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">self</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">@end</span></span><br></pre></td></tr></table></figure></p>
<p>Block blk_t持有self，而self也同时持有作为成员变量的blk_t，导致循环引用，MyObject 的对象实例会无法释放，造成内存泄漏。</p>
<p>另外，编译器有时候能够及时的查处循环引用，进行编译警告 <code>Capturing &#39;self&#39; strongly in this block is likely to lead to a retain cycle</code> 。</p>
<h4 id="weak-修饰符"><a href="#weak-修饰符" class="headerlink" title="__weak 修饰符"></a>__weak 修饰符</h4><p>为避免循环引用，可声明附有 __weak 修饰符的变量，并将 self 赋值使用：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">- (<span class="keyword">instancetype</span>)init</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">self</span> = [<span class="keyword">super</span> init];</span><br><span class="line">    <span class="keyword">if</span> (<span class="keyword">self</span>) &#123;</span><br><span class="line">        <span class="keyword">id</span> __<span class="keyword">weak</span> tmp = <span class="keyword">self</span>;</span><br><span class="line">        blk_ = ^ &#123;</span><br><span class="line">            <span class="built_in">NSLog</span>(<span class="string">@"self = %@"</span>, tmp);</span><br><span class="line">        &#125;;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">self</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>在低于 iOS4 时，使用 <strong>unsafe_unretained 修饰符代替 </strong>weak。</p>
<p>另外，以下源代码中 Block 中没有 self 但是同样截获了 self，引起循环引用：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="keyword">void</span>(^blk_t) (<span class="keyword">void</span>);</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">@interface</span> <span class="title">MyObject</span> : <span class="title">NSObject</span></span></span><br><span class="line">&#123;</span><br><span class="line">    blk_t blk_;</span><br><span class="line">    <span class="keyword">id</span> obj_;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">@end</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">@implementation</span> <span class="title">MyObject</span></span></span><br><span class="line"></span><br><span class="line">- (<span class="keyword">instancetype</span>)init</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">self</span> = [<span class="keyword">super</span> init];</span><br><span class="line">    <span class="keyword">if</span> (<span class="keyword">self</span>) &#123;</span><br><span class="line">        blk_ = ^ &#123;</span><br><span class="line">            <span class="built_in">NSLog</span>(<span class="string">@"obj_ = %@"</span>, obj_);</span><br><span class="line">        &#125;;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">self</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">@end</span></span><br></pre></td></tr></table></figure></p>
<p>Block 语法内部使用的 obj_ 实际上截获了 self。对编译器来说，obj_ 只是对象用结构体的成员变量，即：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">blk_ = ^ &#123;</span><br><span class="line">            <span class="built_in">NSLog</span>(<span class="string">@"obj_ = %@"</span>, <span class="keyword">self</span>-&gt;obj_);</span><br><span class="line">        &#125;;</span><br></pre></td></tr></table></figure></p>
<p>这里说个容易出错的情况，如果某个属性用 weak 关键字呢？<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">@interface</span> <span class="title">Person</span>()</span></span><br><span class="line"><span class="keyword">@property</span> (<span class="keyword">nonatomic</span>, <span class="keyword">weak</span>) <span class="built_in">NSArray</span> *array;</span><br><span class="line"><span class="keyword">@end</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">@implementation</span> <span class="title">Person</span></span></span><br><span class="line"></span><br><span class="line">- (<span class="keyword">instancetype</span>)init</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">self</span> = [<span class="keyword">super</span> init];</span><br><span class="line">    blk_ = ^&#123;</span><br><span class="line">        <span class="built_in">NSLog</span>(<span class="string">@"array = %@"</span>,_array);<span class="comment">//循环引用警告</span></span><br><span class="line">    &#125;;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">self</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>这里依然会有循环引用的警告，因为循环引用是 self 和 block 之间的事，和被这个 Block 持有的成员变量时strong、weak都没有关系，即使是基本数据类型 assgin 一样会有警告。</p>
<h4 id="block-修饰符"><a href="#block-修饰符" class="headerlink" title="__block 修饰符"></a>__block 修饰符</h4><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">- (<span class="keyword">instancetype</span>)init</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">self</span> = [<span class="keyword">super</span> init];</span><br><span class="line">    __block <span class="keyword">id</span> temp = <span class="keyword">self</span>;<span class="comment">//temp持有self</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment">//self持有blk_</span></span><br><span class="line">    blk_ = ^&#123;</span><br><span class="line">        <span class="built_in">NSLog</span>(<span class="string">@"self = %@"</span>,temp);<span class="comment">//blk_持有temp</span></span><br><span class="line">        temp = <span class="literal">nil</span>;</span><br><span class="line">    &#125;;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">self</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">- (<span class="keyword">void</span>)execBlc</span><br><span class="line">&#123;</span><br><span class="line">    blk_();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>如果不调用 execBlc 实例方法，即不给成员变量 blk_ 赋值，便会循环引用并造成内存泄漏。</p>
<p>使用 __block 避免循环引用的优点：</p>
<ul>
<li>通过 __block 变量可控制对象的持有时间。</li>
<li>为避免循环引用必须执行 Block，否则循环引用一直存在。</li>
</ul>
<p>说一下，使用 __block 来避免循环引用，感觉并不方便。不过也可以更具实际情况去具体选择。</p>
<h3 id="参考文档"><a href="#参考文档" class="headerlink" title="参考文档"></a>参考文档</h3><p><a href="https://juejin.im/post/5b4cd5aae51d455b5d3efa2c" target="_blank" rel="noopener">2018年 iOS 面试心得</a></p>
]]></content>
  </entry>
  <entry>
    <title>《Objective-C高级编程》三篇总结之一：引用计数篇</title>
    <url>/2019/11/29/%E3%80%8AObjective-C%E9%AB%98%E7%BA%A7%E7%BC%96%E7%A8%8B%E3%80%8B%E4%B8%89%E7%AF%87%E6%80%BB%E7%BB%93%E4%B9%8B%E4%B8%80%EF%BC%9A%E5%BC%95%E7%94%A8%E8%AE%A1%E6%95%B0%E7%AF%87/</url>
    <content><![CDATA[<h2 id="手动内存管理"><a href="#手动内存管理" class="headerlink" title="手动内存管理"></a>手动内存管理</h2><p>在 Xcode4.2 版本以后，自动引用计数 ARC 已经是默认有效了。但是这里还是先分析一下手动内存管理 MRC，方便我们对 iOS 开发的内存管理有更清晰的认识。</p>
<a id="more"></a>
<p>参考：<a href="https://juejin.im/post/58ead8c344d904006d4f17af" target="_blank" rel="noopener">《Objective-C 高级编程》干货三部曲（一）：引用计数篇</a></p>
<p>放在前面：</p>
<p>可通过下面两个方法查看汇编输出：<br>参考：<a href="https://www.jianshu.com/p/2760ccacb968" target="_blank" rel="noopener">iOS 获取汇编输出方法</a></p>
<ol>
<li>Xcode -&gt; Product -&gt; Perform Action -&gt; Assemble “*.m”即可获得汇编输出。</li>
<li>clang获取oc代码汇编输出。在文件目录下，执行命令行：clang -S -fobjc-arc 文件名.m -o output.s</li>
</ol>
<p>或者查看文件转成 C++ 后的源码。在文件目录下执行命令行：</p>
<p><code>$ clang -rewrite-objc MyClass.m</code></p>
<p>然后在同一目录下会多出一个 MyClass.cpp 文件，双击打开即可。</p>
<h3 id="内存管理的思考方式"><a href="#内存管理的思考方式" class="headerlink" title="内存管理的思考方式"></a>内存管理的思考方式</h3><p>内存管理更加客观、正确的思考方式是：</p>
<ul>
<li>自己生成的对象，自己持有。</li>
<li>非自己生成的对象，自己也能持有。</li>
<li>不再需要自己持有的对象时释放。</li>
<li>非自己持有的对象，无法释放。</li>
</ul>
<p>iOS 内存管理经常用到的词有：<strong>生成</strong>、<strong>持有</strong>、<strong>释放</strong>、<strong>销毁</strong>。如下：</p>
<table>
<thead>
<tr>
<th style="text-align:left">对象操作</th>
<th>Objective-C 方法</th>
<th>引用计数变化</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left">生成并持有对象</td>
<td>alloc/new/copy/mutableCopy 等方法</td>
<td>+1</td>
</tr>
<tr>
<td style="text-align:left">持有对象</td>
<td>retain方法</td>
<td>+1</td>
</tr>
<tr>
<td style="text-align:left">释放对象</td>
<td>release方法</td>
<td>-1</td>
</tr>
<tr>
<td style="text-align:left">废气对象</td>
<td>dealloc方法</td>
<td>无</td>
</tr>
</tbody>
</table>
<p>借用书中图，直观感受如下：</p>
<p><img src="https://i.loli.net/2019/11/26/Q5MlqOYjPV4AXGy.png" alt="引用计数的内存管理.png"></p>
<p>下面分条列举说明。</p>
<h4 id="自己生成的对象，自己所持有"><a href="#自己生成的对象，自己所持有" class="headerlink" title="自己生成的对象，自己所持有"></a>自己生成的对象，自己所持有</h4><p>使用以下名称开头的方法名，意味着自己生成的方法，只有自己持有：</p>
<ul>
<li>alloc</li>
<li>new</li>
<li>copy</li>
<li>mutableCopy</li>
</ul>
<p>根据上述 <strong>使用以下名称开头的方法名</strong>，下列名称也意味着自己生成并持有对象：</p>
<ul>
<li>allocMyObject</li>
<li>newTheObject</li>
<li>copyThis</li>
<li>mutableCopyYourObject</li>
</ul>
<p>但是对于一下名称，即使使用了 alloc/new/copy/mutableCopy 开头，也并不属于同一类别的方法：</p>
<ul>
<li>allocate</li>
<li>newer</li>
<li>copying</li>
<li>mutableCopyyed</li>
</ul>
<p>这里用 <strong>驼峰（<code>CameCase</code>）</strong> 命名法来区分。</p>
<h4 id="非自己生成的对象，自己也能持有"><a href="#非自己生成的对象，自己也能持有" class="headerlink" title="非自己生成的对象，自己也能持有"></a>非自己生成的对象，自己也能持有</h4><p>在 alloc/new/copy/mutableCopy 类型以外取得的对象，默认并不持有对象，可以通过 <code>retain</code> 来持有对象：</p>
<figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="comment">/// 取得非自己生成并持有的对象。此时，obj 取得对象的存在，但自己并不持有</span></span><br><span class="line"><span class="keyword">id</span> obj = [<span class="built_in">NSMutableArray</span> array];</span><br><span class="line"><span class="comment">/// 自己持有对象</span></span><br><span class="line">[obj <span class="keyword">retain</span>];</span><br></pre></td></tr></table></figure>
<h4 id="不再需要自己持有的对象时释放"><a href="#不再需要自己持有的对象时释放" class="headerlink" title="不再需要自己持有的对象时释放"></a>不再需要自己持有的对象时释放</h4><p>自己持有的对象，一旦不需要，持有者有义务释放该对象。释放使用 <code>release</code> 方法。</p>
<figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="comment">/// 自己生成并持有对象</span></span><br><span class="line"><span class="keyword">id</span> obj = [[<span class="built_in">NSObject</span> alloc] init];</span><br><span class="line"><span class="comment">/// 释放对象。指向该对象的指针仍然保留在 obj 中，貌似可以访问。</span></span><br><span class="line"><span class="comment">/// 但对象一经释放，决不可访问，否则会发生崩溃。</span></span><br><span class="line">[obj release];</span><br></pre></td></tr></table></figure>
<p>通过 <code>retain</code> 持有非自己生成的对象时，也需要使用 <code>release</code> 释放:<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="comment">/// 生成</span></span><br><span class="line"><span class="keyword">id</span> obj = [<span class="built_in">NSMutableArray</span> array];</span><br><span class="line"><span class="comment">/// 持有</span></span><br><span class="line">[obj <span class="keyword">retain</span>];</span><br><span class="line"><span class="comment">/// 释放</span></span><br><span class="line">[obj release];</span><br></pre></td></tr></table></figure></p>
<p>生成并持有对象，方法实现入下，注意 <code>allocObject</code> 符合前面生成并持有对象的命名规范：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">-(<span class="keyword">id</span>)allocObject &#123;</span><br><span class="line">    <span class="comment">/// 生成并持有对象</span></span><br><span class="line">    <span class="keyword">id</span> obj = [[<span class="built_in">NSObject</span> alloc] init];</span><br><span class="line">    <span class="comment">/// 让自己持有对象</span></span><br><span class="line">    <span class="keyword">return</span> obj;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">/// 此时，不用调用 retain。obj1 已经持有该对象</span></span><br><span class="line"><span class="keyword">id</span> obj1 = [obj0 allocObject];</span><br></pre></td></tr></table></figure></p>
<p>生成对象，默认不持有，需要手动持有，实现如下：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">-(<span class="keyword">id</span>)object &#123;</span><br><span class="line">    <span class="comment">/// 生成并持有对象</span></span><br><span class="line">    <span class="keyword">id</span> obj = [[<span class="built_in">NSObject</span> alloc] init];</span><br><span class="line">    <span class="comment">/// 取得对象的存在。但是放弃对对象的持有。即加入自动释放池。</span></span><br><span class="line">    [obj autorelease];</span><br><span class="line">    <span class="comment">/// 此时，obj 并不能持有该对象了</span></span><br><span class="line">    <span class="keyword">return</span> obj;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/// 获取对象，并不持有</span></span><br><span class="line"><span class="keyword">id</span> obj1 = [obj0 object];</span><br><span class="line"><span class="comment">/// 持有对象。即引用计数 +1</span></span><br><span class="line">[obj1 <span class="keyword">retain</span>];</span><br></pre></td></tr></table></figure></p>
<p>使用 <code>autorelease</code> 方法，可以使取得对象的存在，但是自己不持有对象。 <code>autorelease</code> 提供这样的功能：使对象在超出指定的生存范围时能够自动并正确的释放（调用 release 方法）。如下图所示：</p>
<p><img src="https://i.loli.net/2019/11/26/TWtXfSbpMdDlCU4.png" alt="release与autorelease区别.png"></p>
<h4 id="无法释放非自己持有的对象"><a href="#无法释放非自己持有的对象" class="headerlink" title="无法释放非自己持有的对象"></a>无法释放非自己持有的对象</h4><p>对于用  alloc/new/copy/mutableCopy 生成并持有的对象，或者是 retain 持有的对象，由于持有者是自己，所以在不需要该对象时自己需要将其释放。而由此之外得到的对象绝对不能释放，若在程序中释放了非自己持有的对象，会引发崩溃。例如过度释放对象：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="comment">/// 自己生成并持有对象</span></span><br><span class="line"><span class="keyword">id</span> obj = [[<span class="built_in">NSObject</span> alloc] init];</span><br><span class="line"><span class="comment">/// 释放对象</span></span><br><span class="line">[obj release];</span><br><span class="line"><span class="comment">/// 对象已经释放后再次释放</span></span><br><span class="line">[obj release];</span><br><span class="line"><span class="comment">/// 此时，程序崩溃。</span></span><br><span class="line"><span class="comment">/// 崩溃分析：对象已废弃，访问废弃对象时崩溃。野指针</span></span><br></pre></td></tr></table></figure></p>
<p>或者在 <strong>取得的对象已存在，但自己不持有该对象</strong> 时释放，也会引发崩溃：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="keyword">id</span> obj = [<span class="built_in">NSMutableArray</span> array];</span><br><span class="line">[obj release];</span><br><span class="line"><span class="comment">///释放非自己持有的对象，崩溃</span></span><br></pre></td></tr></table></figure></p>
<h3 id="alloc-retain-release-dealloc-实现"><a href="#alloc-retain-release-dealloc-实现" class="headerlink" title="alloc/retain/release/dealloc 实现"></a>alloc/retain/release/dealloc 实现</h3><p>NSObject 源码并没有公开，这里参考开源的 GNUstep 源码来推测 NSObject 内部的实现细节。主要是要从实现的角度来理解内存管理的方式。为了明确重点，部分引用做了修改。</p>
<h4 id="alloc-方法"><a href="#alloc-方法" class="headerlink" title="alloc 方法"></a>alloc 方法</h4><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="keyword">id</span> obj = [<span class="built_in">NSObject</span> alloc];</span><br></pre></td></tr></table></figure>
<p>调用该方法，源码实现如下：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="comment">/// GNUstep/modules/core/base/Source/NSObject.m alloc</span></span><br><span class="line">+(<span class="keyword">id</span>) alloc &#123;</span><br><span class="line">    <span class="keyword">return</span> [<span class="keyword">self</span> allocWithZone: <span class="built_in">NSDefaultMallocZone</span>()];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">+(<span class="keyword">id</span>)allocWithZone: (<span class="built_in">NSZone</span> *)z &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">NSAllocateObject</span>(<span class="keyword">self</span>, <span class="number">0</span>, z);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>通过 allocWithZone 类方法调用 NSAllocateObject 函数分配了对象，下面看下 NSAllocateObject h函数：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="comment">/// GNUstep/modules/core/base/Source/NSObject.m NSAllocateObject</span></span><br><span class="line"><span class="keyword">struct</span> obj_layout &#123;</span><br><span class="line">    <span class="built_in">NSUInteger</span> retained;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="keyword">inline</span> <span class="keyword">id</span></span><br><span class="line"><span class="built_in">NSAllocateObject</span>(Class aClass, <span class="built_in">NSUInteger</span> extraBytes, <span class="built_in">NSZone</span> *zone) &#123;</span><br><span class="line">    <span class="keyword">int</span> size = 计算容纳对象所需要的内存大小</span><br><span class="line">    <span class="keyword">id</span> new = <span class="built_in">NSZoneMalloc</span>(zone, size);</span><br><span class="line">    memset(new, <span class="number">0</span>, size);</span><br><span class="line">    new = (<span class="keyword">id</span>) &amp; ((<span class="keyword">struct</span> obj_layout *) new)[<span class="number">1</span>];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>NSAllocateObject 函数通过调用 NSZoneMalloc 函数来分配存放对象所需要的内存空间。之后将该空间置为 0，最后返回作为对象而使用的指针。</p>
<blockquote>
<p>NSZone 是为防止内存碎片化而引入的数据结构。目前该接口效率低，且使源代码更加复杂。</p>
</blockquote>
<p>下面是去掉 <code>NSZone</code> 之后简化的源代码：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> obj_layout &#123;</span><br><span class="line">    <span class="built_in">NSUInteger</span> retained;</span><br><span class="line">&#125;;</span><br><span class="line">+(<span class="keyword">id</span>) alloc &#123;</span><br><span class="line">    <span class="keyword">int</span> size = <span class="keyword">sizeof</span>(<span class="keyword">struct</span> obj_layout) + 对象大小;</span><br><span class="line">    <span class="keyword">struct</span> obj_layout *p = (<span class="keyword">struct</span> obj_layout *)calloc(<span class="number">1</span>, size);</span><br><span class="line">    <span class="keyword">return</span> (<span class="keyword">id</span>)(p + <span class="number">1</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>alloc 类方法使用 struct obj_layout 中的 retained 证书来保存引用计数，并将其写入对象内存头部，该对象内存块全部置为 0 后返回。下图为 GNUstep 实现 alloc 类方法返回对象示意图：</p>
<p><img src="https://i.loli.net/2019/11/26/QTgZ1nkXEHKSDw4.png" alt="alloc返回对象示意图.png"></p>
<p>对象的引用计数可以通过 retainCount 实例方法获取。源码实现如下：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="comment">/// GNUstep/modules/core/base/Source/NSObject.m  retainCount</span></span><br><span class="line">-(<span class="built_in">NSUInteger</span>) retainCount &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">NSExtraRefCount</span>(<span class="keyword">self</span>) + <span class="number">1</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">inline</span> <span class="built_in">NSUInteger</span></span><br><span class="line"><span class="built_in">NSExtraRefCount</span>(<span class="keyword">id</span> anObject) &#123;</span><br><span class="line">    <span class="keyword">return</span> ((<span class="keyword">struct</span> obj_layout *)anObject)[<span class="number">-1</span>].retained;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>可以看到，给NSExtraRefCount传入anObject以后，通过访问对象内存头部的.retained变量，来获取引用计数。</p>
<h4 id="retain-方法"><a href="#retain-方法" class="headerlink" title="retain 方法"></a>retain 方法</h4><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="comment">/// GNUstep/modules/core/base/Source/NSObject.m  retain</span></span><br><span class="line">-(<span class="keyword">id</span>) <span class="keyword">retain</span> &#123;</span><br><span class="line">    <span class="built_in">NSIncrementExtraRefCount</span>(<span class="keyword">self</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">self</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">inline</span> <span class="keyword">void</span></span><br><span class="line"><span class="built_in">NSIncrementExtraRefCount</span>(<span class="keyword">id</span> anObject) &#123;</span><br><span class="line">    <span class="keyword">if</span> (((<span class="keyword">struct</span> obj_layout *)anObject)[<span class="number">-1</span>].retained == <span class="built_in">UINT_MAX</span> - <span class="number">1</span>)</span><br><span class="line">        [<span class="built_in">NSException</span> raise: <span class="built_in">NSInternalInconsistencyException</span> format: <span class="string">@"NSIncrementExtraRefCount() asked to increment too far"</span>];</span><br><span class="line">    ((<span class="keyword">struct</span> obj_layout *)anObject)[<span class="number">-1</span>].retained++;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>可以看出，如果已有的引用计数过大，会执行异常代码。正常情况下，只运行了使 retained 变量加 1 的 retained++ 代码。</p>
<h4 id="release-方法"><a href="#release-方法" class="headerlink" title="release 方法"></a>release 方法</h4><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="comment">/// GNUstep/modules/core/base/Source/NSObject.m release</span></span><br><span class="line">-(<span class="keyword">void</span>) release &#123;</span><br><span class="line">    <span class="keyword">if</span> (<span class="built_in">NSDecrementExtraRefCountWasZero</span>(<span class="keyword">self</span>))</span><br><span class="line">        [<span class="keyword">self</span> dealloc];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="built_in">BOOL</span></span><br><span class="line"><span class="built_in">NSDecrementExtraRefCountWasZero</span>(<span class="keyword">id</span> anObject) &#123;</span><br><span class="line">    <span class="keyword">if</span> (((<span class="keyword">struct</span> obj_layout *)anObject)[<span class="number">-1</span>].retained == <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">YES</span>;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        ((<span class="keyword">struct</span> obj_layout*)anObject)[<span class="number">-1</span>].retained--;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">NO</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>当 retained 变量大于 0 时减 1，等于 0 时调用 dealloc 实例方法，废弃对象。</p>
<h4 id="dealloc-方法"><a href="#dealloc-方法" class="headerlink" title="dealloc 方法"></a>dealloc 方法</h4><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="comment">/// GNUstep/modules/core/base/Source/NSObject.m dealloc</span></span><br><span class="line">-(<span class="keyword">void</span>) dealloc &#123;</span><br><span class="line">    <span class="built_in">NSDeallocateObject</span>(<span class="keyword">self</span>);</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">inline</span> <span class="keyword">void</span></span><br><span class="line"><span class="built_in">NSDeallocateObject</span>(<span class="keyword">id</span> anObject) &#123;</span><br><span class="line">    <span class="keyword">struct</span> obj_layout *o = &amp;((<span class="keyword">struct</span> obj_layout *)anObject)[<span class="number">-1</span>];</span><br><span class="line">    free(o);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>上述代码废弃由 alloc 分配的内存块。</p>
<p>以上便是 GNUstep 中 alloc/retain/release/dealloc 的实现，具体总结如下：</p>
<ul>
<li>在 Objective-C 的对象中存有引用计数这一整数值。</li>
<li>调用 alloc 或 retain 方法后，引用计数 +1。</li>
<li>调用 release 方法后，引用计数 -1。</li>
<li>引用计数为 0 后调用 dealloc 方法废弃对象。</li>
</ul>
<p>好了，下面看一下苹果的实现吧。</p>
<h4 id="苹果的实现"><a href="#苹果的实现" class="headerlink" title="苹果的实现"></a>苹果的实现</h4><p>因为 NSObject 源码并没有公开，这里利用 Xcode 的调试器 lldb 和 iOS 大概追溯其实现过程。在 NSObject 类的 alloc 类方法上设置断点，追踪程序的执行。一下列出执行调用的方法和函数：</p>
<ul>
<li><p>alloc 方法</p>
<ul>
<li>alloc</li>
<li>allocWithZone</li>
<li>class_createInstance</li>
<li>calloc</li>
</ul>
</li>
<li><p>retainCount 方法</p>
<ul>
<li>__CFDoExternRefOperation</li>
<li>CFBasicHashGetCountOfKey</li>
</ul>
</li>
<li><p>retain 方法</p>
<ul>
<li>__CFDoExternRefOperation</li>
<li>CFBasicHashAddValue</li>
</ul>
</li>
<li><p>release 方法</p>
<ul>
<li>__CFDoExternRefOperation</li>
<li>CFBasicHashRemoveValue</li>
</ul>
</li>
<li><p>CFBasicHashRemoveValue 返回 0 时，-release 调用 dealloc</p>
</li>
</ul>
<p>上面频繁出现的 <strong>CFDoExternRefOperation 是开源代码 CFRuntime.c 的 </strong>CFDoExternRefOperation 函数，<br>源码如下：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="built_in">CF_EXPORT</span> uintptr_t __CFDoExternRefOperation(uintptr_t op, <span class="keyword">id</span> obj) &#123;</span><br><span class="line">    <span class="keyword">if</span> (<span class="literal">nil</span> == obj) HALT;</span><br><span class="line">    uintptr_t idx = EXTERN_TABLE_IDX(obj);</span><br><span class="line">    uintptr_t disguised = DISGUISE(obj);</span><br><span class="line">    <span class="built_in">CFSpinLock_t</span> *lock = &amp;__NSRetainCounters[idx].lock;</span><br><span class="line">    <span class="built_in">CFBasicHashRef</span> table = __NSRetainCounters[idx].table;</span><br><span class="line">    uintptr_t count;</span><br><span class="line">    <span class="keyword">switch</span> (op) &#123;</span><br><span class="line">    <span class="keyword">case</span> <span class="number">300</span>:   <span class="comment">// increment</span></span><br><span class="line">    <span class="keyword">case</span> <span class="number">350</span>:   <span class="comment">// increment, no event</span></span><br><span class="line">        __CFSpinLock(lock);</span><br><span class="line">	<span class="built_in">CFBasicHashAddValue</span>(table, disguised, disguised);</span><br><span class="line">        __CFSpinUnlock(lock);</span><br><span class="line">        <span class="keyword">if</span> (__CFOASafe &amp;&amp; op != <span class="number">350</span>) __CFRecordAllocationEvent(__kCFObjectRetainedEvent, obj, <span class="number">0</span>, <span class="number">0</span>, <span class="literal">NULL</span>);</span><br><span class="line">        <span class="keyword">return</span> (uintptr_t)obj;</span><br><span class="line">    <span class="keyword">case</span> <span class="number">400</span>:   <span class="comment">// decrement</span></span><br><span class="line">        <span class="keyword">if</span> (__CFOASafe) __CFRecordAllocationEvent(__kCFObjectReleasedEvent, obj, <span class="number">0</span>, <span class="number">0</span>, <span class="literal">NULL</span>);</span><br><span class="line">    <span class="keyword">case</span> <span class="number">450</span>:   <span class="comment">// decrement, no event</span></span><br><span class="line">        __CFSpinLock(lock);</span><br><span class="line">        count = (uintptr_t)<span class="built_in">CFBasicHashRemoveValue</span>(table, disguised);</span><br><span class="line">        __CFSpinUnlock(lock);</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span> == count;</span><br><span class="line">    <span class="keyword">case</span> <span class="number">500</span>:</span><br><span class="line">        __CFSpinLock(lock);</span><br><span class="line">        count = (uintptr_t)<span class="built_in">CFBasicHashGetCountOfKey</span>(table, disguised);</span><br><span class="line">        __CFSpinUnlock(lock);</span><br><span class="line">        <span class="keyword">return</span> count;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>下面是其简化后的代码实现：</p>
<figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="comment">/// CF/CFRuntime.c __CFDoExternRefOperation</span></span><br><span class="line"><span class="keyword">int</span> ____CFDoExternRefOperation(uintptr_t op, <span class="keyword">id</span> obj) &#123;</span><br><span class="line">    <span class="built_in">CFBasicHashRef</span> table = 取得对象的散列表（obj）;</span><br><span class="line">    <span class="keyword">int</span> count;</span><br><span class="line">    <span class="keyword">switch</span>(op) &#123;</span><br><span class="line">        <span class="keyword">case</span> Operation_retainCount:</span><br><span class="line">            count = <span class="built_in">CFBasicHashGetCountOfKey</span>(table, obj);</span><br><span class="line">            <span class="keyword">return</span> count;</span><br><span class="line">        <span class="keyword">case</span> Operation_retain:</span><br><span class="line">            count = <span class="built_in">CFBasicHashAddValue</span>(table, obj);</span><br><span class="line">            <span class="keyword">return</span> count;</span><br><span class="line">        <span class="keyword">case</span> Operation_release:</span><br><span class="line">            count = <span class="built_in">CFBadicHashRemoveValue</span>(table, obj);</span><br><span class="line">            <span class="keyword">return</span> <span class="number">0</span> == count;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>__CFDoExternRefOperation 函数按照 retainCount/retain/release 操作进行分发，调用不同的函数。猜测， NSObject 类的 retainCount/retain/release 实例方法也许如下面代码表示：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">-(<span class="built_in">NSUInteger</span>) retainCount &#123;</span><br><span class="line">    <span class="keyword">return</span> (<span class="built_in">NSUInteter</span>) __CFDoExternRefOperation(Operation_retainCount, <span class="keyword">self</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">-(<span class="keyword">id</span>)<span class="keyword">retain</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> (<span class="keyword">id</span>)__CFDoExternRefOperation(Operation_retain, <span class="keyword">self</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">-(<span class="keyword">void</span>) release &#123;</span><br><span class="line">    <span class="keyword">return</span> __CFExternRefOperation(Operation_release, <span class="keyword">self</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>从 __CFDoExternRefOperation 函数以及由此函数调用的各个函数名看出，苹果的实现大概是采用散列表（引用技数表）来管理引用计数的，如下图所示：</p>
<p><img src="https://i.loli.net/2019/11/26/bOE3gxYW5aZLlCB.png" alt="散列计数表管理引用计数.png"></p>
<p>GNUstep 将引用计数保存到对象内存块头部的变量中，好处如下：</p>
<ul>
<li>少量代码即可完成。</li>
<li>能够统一管理引用计数内存和对象内存块。</li>
</ul>
<p>苹果很可能采用引用计数表管理引用计数，这样的好处是：</p>
<ul>
<li>对象用内存块的分配无需考虑内存块头部。</li>
<li>引用计数表中存有各个对象的内存块地址，可通过改地址追溯到对象的内存块。</li>
</ul>
<p>尤其是第二，在调试时有着举足轻重的作用，即使出现故障导致对象占用的内存损坏，只要引用计数表没坏，就能够确认各内存块地址。</p>
<h3 id="autorelease"><a href="#autorelease" class="headerlink" title="autorelease"></a>autorelease</h3><h4 id="autorelease-介绍"><a href="#autorelease-介绍" class="headerlink" title="autorelease 介绍"></a>autorelease 介绍</h4><p>当 autorelease 管理的对象超出起作用域后，对象实例的 release 方法会被调用。autorelease 的具体使用方法如下：</p>
<ul>
<li>生成并持有 NSAutoreleasePool 对象。</li>
<li>调用已分配对象的 autorelease 方法。</li>
<li>废弃 NSAutoreleasePool 对象。</li>
</ul>
<p><img src="https://i.loli.net/2019/11/26/L1m62UbDjRYhSP3.png" alt="NSAutoreleasePool的生存周期.png"></p>
<p>对所有调用过 autorelease 实例方法的对象，在废弃 NSAutoreleasePool 时，都将主动调用 release 方法：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="built_in">NSAutoreleasePool</span> *pool = [[<span class="built_in">NSAutoreleasePool</span> alloc] init];</span><br><span class="line"><span class="keyword">id</span> obj = [[<span class="built_in">NSObject</span> alloc] init];</span><br><span class="line">[obj autorelease];</span><br><span class="line">[pool drain];   <span class="comment">/// 等同于 [obj release];</span></span><br></pre></td></tr></table></figure></p>
<p>程序并非一定要使用 NSAutoreleasePool 对象来工作。但是在大量产生 autorelease 的对象时，只要不废弃 NSAutoreleasePool 对象，即 RunLoop 不进入睡眠状态，那么生成的对象就不能被释放，因此有时会产生内存不足的现象。如大量循环中对图片做复杂操作。这种情况下，就会产生大量的 autorelease 对象，内存激增：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> (<span class="keyword">int</span> i  = <span class="number">0</span>; i &lt; 图片数; i++) &#123;</span><br><span class="line">    <span class="comment">/*</span></span><br><span class="line"><span class="comment">    * 读入图像</span></span><br><span class="line"><span class="comment">    * 大量产生 autorelease 对象</span></span><br><span class="line"><span class="comment">    * 由于没有废弃 NSAutoreleasePool 对象，最终导致内存不足。</span></span><br><span class="line"><span class="comment">    */</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p><img src="https://i.loli.net/2019/11/26/G2INzAYZber9OdK.png" alt="大量产生autorelease对象.png"></p>
<p>再次情况下，有必要在适当的地方生成、持有、废弃 NSAutoreleasePool 对象：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; imageArray.count; i++) &#123;</span><br><span class="line">    <span class="comment">// 临时 Pool</span></span><br><span class="line">    <span class="built_in">NSAutoreleasePool</span> *pool = [[<span class="built_in">NSAutoreleasePool</span> alloc] init];</span><br><span class="line">    <span class="built_in">UIImage</span> *image = imageArray[i];</span><br><span class="line">    [image doSomething];</span><br><span class="line">    [pool drain];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p><img src="https://i.loli.net/2019/11/26/9hzXgJP8C4O52WA.png" alt="适当释放autorelease对象.png"></p>
<blockquote>
<p>可能会出的面试题：什么时候会创建自动释放池？<br>答：运行循环检测到事件并启动后，就会创建自动释放池，而且子线程的 runloop 默认是不工作的，无法主动创建，必须手动创建。<br>举个🌰：<br>自定义的 NSOperation 类中的 main 方法里就必须添加自动释放池。否则在出了作用域以后，自动释放对象会因为没有自动释放池去处理自己而造成内存泄露。</p>
</blockquote>
<h4 id="GNUstep-autorelease-实现"><a href="#GNUstep-autorelease-实现" class="headerlink" title="GNUstep autorelease 实现"></a>GNUstep autorelease 实现</h4><p>同上，先看一下 GNUstep 的源码实现：</p>
<figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="comment">/// GNUstep/modules/core/base/Source/NSObject.m autorelease</span></span><br><span class="line">-(<span class="keyword">id</span>) autorelease &#123;</span><br><span class="line">    [<span class="built_in">NSAutoreleasePool</span> addObject: <span class="keyword">self</span>];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>autorelease 方法的本质就是调用 NSAutoreleasePool 对象的 addObject 类方法。下面是作者假想后简化的 NSAutoreleasePool 源代码实现：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="comment">/// GNUstep/modules/core/base/Source/NSAutoreleasePool.m addObject</span></span><br><span class="line">+(<span class="keyword">void</span>) addObjecty: (<span class="keyword">id</span>)anObj &#123;</span><br><span class="line">    <span class="built_in">NSAutoreleasePool</span> *pool = 取得正在使用的 <span class="built_in">NSAutoreleasePool</span> 对象;</span><br><span class="line">    <span class="keyword">if</span> (pool != <span class="literal">nil</span>) &#123;</span><br><span class="line">        [pool addObject: anObj];</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="built_in">NSLog</span>()</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">-(<span class="keyword">void</span>) addObject: (<span class="keyword">id</span>)anObj &#123;</span><br><span class="line">    [array addObject: anObj];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>addObject 类方法就是调用正在使用的 NSAutoreleasePool 对象的 addObject 实例方法，然后这个对象就被追加到正在使用的 NSAutoreleasePool 对象的数组中。</p>
<p>下面看一下使用 drain 实例方法废弃正在使用的 NSAutoreleasePool 对象的过程：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="comment">/// GNUstep/modules/core/base/Source/NSAutoreleasePool.m drain</span></span><br><span class="line">-(<span class="keyword">void</span>) drain &#123;</span><br><span class="line">    [<span class="keyword">self</span> dealloc];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">-(<span class="keyword">void</span>) dealloc &#123;</span><br><span class="line">    [<span class="keyword">self</span> emptyPool];</span><br><span class="line">    [array release];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">-(<span class="keyword">void</span>) emptyPool &#123;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">id</span> obj <span class="keyword">in</span> array) &#123;</span><br><span class="line">        [obj release];</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>虽然调用了好几个方法，可以确定对于数组中的所有对象都调用了 release 实例方法。</p>
<h4 id="苹果的实现-1"><a href="#苹果的实现-1" class="headerlink" title="苹果的实现"></a>苹果的实现</h4><p>可通过 objc 库的 <a href="https://opensource.apple.com/source/objc4/objc4-750.1/runtime/NSObject.mm.auto.html" target="_blank" rel="noopener">https://opensource.apple.com/source/objc4/objc4-750.1/runtime/NSObject.mm.auto.html</a> 来确认苹果中 autorelease 的实现。<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="comment">/// /objc4/objc4-750.1/runtime/NSObject.mm AutoreleasePoolPage</span></span><br><span class="line"></span><br><span class="line"><span class="comment">/// 这里的源码非常长。。。感兴趣的可以自己去看下。</span></span><br><span class="line"><span class="keyword">class</span> AutoreleasePoolPage &#123;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>下面还是结合书中总结的来做分析吧。核心方法是一样的：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> AutoreleasePoolPage &#123;</span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">inline</span> voiod *push() &#123;</span><br><span class="line">        <span class="comment">//  相当于生成或者持有 NSAutoreleasePool 类对象；</span></span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">inline</span> <span class="keyword">void</span> *pop(<span class="keyword">void</span> *token) &#123;</span><br><span class="line">        <span class="comment">// 相当于废弃 NSAutoreleasePool 类对象；</span></span><br><span class="line">        releaseAll();</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">inline</span> <span class="keyword">id</span> autorelease(<span class="keyword">id</span> obj) &#123;</span><br><span class="line">        <span class="comment">/*</span></span><br><span class="line"><span class="comment">        * 相当于 NSAutoreleasePool 类的 addObject 类方法；</span></span><br><span class="line"><span class="comment">        * AutoreleasePoolPage *page = 取得正在使用的 AutoreleasePoolPage 实例；</span></span><br><span class="line"><span class="comment">        * page -&gt; add(obj);</span></span><br><span class="line"><span class="comment">        */</span></span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">id</span> *add(<span class="keyword">id</span> obj) &#123;</span><br><span class="line">        <span class="comment">// 将对象追加到内部数组中；</span></span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">void</span> releaseAll() &#123;</span><br><span class="line">        <span class="comment">// 调用内部数组中对象的 release 方法</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">/// 进栈</span></span><br><span class="line"><span class="keyword">void</span> *objc_autoreleasePoolPush(<span class="keyword">void</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> AutoreleasePoolPage :: push();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/// 出栈</span></span><br><span class="line"><span class="keyword">void</span> *objc_autoreleasePoolPop(<span class="keyword">void</span> *ctxt) &#123;</span><br><span class="line">    AutoreleasePoolPage :: pop(ctxt)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/// 在内部释放</span></span><br><span class="line"><span class="keyword">id</span> *objc_autorelease(<span class="keyword">id</span> obj) &#123;</span><br><span class="line">    <span class="keyword">return</span> AutoreleasePoolPage :: autorelease(obj);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>下面通过外部调用来对比分析：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="built_in">NSAutoreleasePool</span> *pool = [[<span class="built_in">NSAutoreleasePool</span> alloc] init];</span><br><span class="line"><span class="comment">// 等同于  objc_autoreleasePoolPush()</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">id</span> obj = [[<span class="built_in">NSObject</span> alloc] init];</span><br><span class="line">[obj autorelease];</span><br><span class="line"><span class="comment">// 等同于 objc_autorelease(obj)</span></span><br><span class="line"></span><br><span class="line">[<span class="built_in">NSAutoreleasePool</span> showPools];</span><br><span class="line"><span class="comment">/// 非公开类方法。showPools 会将现在 NSAutoreleasePool 的状况输出到控制台</span></span><br><span class="line"></span><br><span class="line">[pool drain];</span><br><span class="line"><span class="comment">// 等同于 objc_autoreleasePoolPop(pool)</span></span><br></pre></td></tr></table></figure></p>
<blockquote>
<p>可能出的面试题： 苹果如何实现 NSAutoreleasePool 的？ 参考答案: NSAutorelease 以一个队列数组的形式实现，主要使用三个方法：objc_autoreleasePoolPush（进栈）、objc_autoreleasePoolPop（出栈）、objc_autorelease（释放内部）。</p>
</blockquote>
<p>另外，如果 autorelease NSAutoreleasePool 对象，回引发崩溃。<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="built_in">NSAutoreleasePool</span> *pool = [[<span class="built_in">NSAutoreleasePool</span> alloc] init];</span><br><span class="line">[pool autorelease];</span><br></pre></td></tr></table></figure></p>
<p>因为对于 NSAutoreleasePool 来说，autorelease 已被重载。</p>
<h2 id="ARC-自动引用计数"><a href="#ARC-自动引用计数" class="headerlink" title="ARC 自动引用计数"></a>ARC 自动引用计数</h2><h3 id="内存管理的思考方式-1"><a href="#内存管理的思考方式-1" class="headerlink" title="内存管理的思考方式"></a>内存管理的思考方式</h3><p>引用计数式内存管理，就是思考 ARC 所引起的变化。</p>
<ul>
<li>自己生成的对象，自己所持有。</li>
<li>非自己生成的对象，自己也能持有。</li>
<li>自己持有的对象不再需要时，释放。</li>
<li>非自己持有的对象无法释放。</li>
</ul>
<p>可以看到，思想和 MRC 是一样的，区别主要是我们不需要在显式的调用，在源代码的记述方法上有所不同。</p>
<h3 id="四种所有权修饰符"><a href="#四种所有权修饰符" class="headerlink" title="四种所有权修饰符"></a>四种所有权修饰符</h3><p>ARC 有效时，id 类型或者对象类型必须附加所有权修饰符。所有权修饰符一共有四种，如下（借鉴的博客中说是引用官方文档，不过我一直找到这份文档…）：</p>
<ul>
<li><strong>__strong</strong>：is the default. An object remains “alive” as long as there is a strong pointer to it.</li>
<li><strong>__weak</strong>：specifies a reference that does not keep the referenced object alive. A weak reference is set to nil when there are no strong references to the object.</li>
<li><strong>__unsafe_unretained</strong>：specifies a reference that does not keep the referenced object alive and is not set to nil when there are no strong references to the object. If the object it references is deallocated, the pointer is left dangling.</li>
<li><strong>__autoreleasing</strong>：is used to denote arguments that are passed by reference (id *) and are autoreleased on return.</li>
</ul>
<p>下面挨个看一下吧。</p>
<h4 id="strong-修饰符"><a href="#strong-修饰符" class="headerlink" title="__strong 修饰符"></a>__strong 修饰符</h4><p>ARC 环境下，__strong 是属性的默认修饰符。</p>
<h5 id="strong-使用方法"><a href="#strong-使用方法" class="headerlink" title="__strong 使用方法"></a>__strong 使用方法</h5><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="keyword">id</span> obj = [[<span class="built_in">NSObject</span> alloc] init];</span><br></pre></td></tr></table></figure>
<p>等同于：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="keyword">id</span> __<span class="keyword">strong</span> obj = [[<span class="built_in">NSObject</span> alloc] init];</span><br></pre></td></tr></table></figure></p>
<p>__strong 修饰符表示对对象的“强引用”，该对象的持有状态如下：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">    <span class="comment">// 自己生成并持有对象。因为强引用，所以持有。</span></span><br><span class="line">    <span class="keyword">id</span> __<span class="keyword">strong</span> obj = [[<span class="built_in">NSObject</span> alloc] init];</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">/// obj 超出作用域，强引用失效。所以自动释放持有的对象。</span></span><br></pre></td></tr></table></figure></p>
<p>对于非自己生成，并持有的对象，亦是如此：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">    <span class="comment">// 取得非自己生成并持有的对象</span></span><br><span class="line">    <span class="keyword">id</span> __<span class="keyword">strong</span> obj = [<span class="built_in">NSMutableArray</span> array];</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">/// 超出作用域，强引用失效</span></span><br></pre></td></tr></table></figure></p>
<p>附有 __strong 修饰符的变量之间也可以相互副职：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 生成对象A。obj0 持有对象 A 的强引用</span></span><br><span class="line"><span class="keyword">id</span> __<span class="keyword">strong</span> obj0 = [[<span class="built_in">NSObject</span> alloc] init];  </span><br><span class="line"><span class="comment">// 生成对象B。obj1 持有对象 B 的强引用</span></span><br><span class="line"><span class="keyword">id</span> __<span class="keyword">strong</span> obj1 = [[<span class="built_in">NSObject</span> alloc] init];</span><br><span class="line"><span class="comment">// obj2 不持有任何对象</span></span><br><span class="line"><span class="keyword">id</span> __<span class="keyword">strong</span> obj2 = <span class="literal">nil</span>;</span><br><span class="line"><span class="comment">// obj0 持有对象 B 的强引用。此时对象 A 因为不再被强引用，被废弃。</span></span><br><span class="line"><span class="comment">// 此时对象 B 被变量 obj0 和 obj1 共同持有。</span></span><br><span class="line">obj0 = obj1;</span><br><span class="line"><span class="comment">// obj2 持有对象 B 的强引用。</span></span><br><span class="line"><span class="comment">// 此时对象 B 被变量 obj0、obj1、obj2 持有。</span></span><br><span class="line">obj2 = obj0;</span><br><span class="line"><span class="comment">// obj1 不再强引用对象 B</span></span><br><span class="line">obj1 = <span class="literal">nil</span>;</span><br><span class="line"><span class="comment">// obj0 不再强引用对象 B</span></span><br><span class="line">obj0 = <span class="literal">nil</span>;</span><br><span class="line"><span class="comment">// obj2 不再强引用对象 B</span></span><br><span class="line">obj2 = <span class="literal">nil</span>;</span><br><span class="line"><span class="comment">// 此时，对象 B 不再被任何变量强引用，被废弃。</span></span><br></pre></td></tr></table></figure></p>
<p>也可以给类的成员变量或方法属性加上 __strong 修饰符：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">@interface</span> <span class="title">Test</span>: <span class="title">NSObject</span> </span>&#123;</span><br><span class="line">    <span class="keyword">id</span> __<span class="keyword">strong</span> obj_;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">-(<span class="keyword">void</span>)setObject: (<span class="keyword">id</span> __<span class="keyword">strong</span>)obj;</span><br></pre></td></tr></table></figure></p>
<p>因为 id 类型和对象类型的所有权修饰符默认为 <strong>strong  修饰符，所以通常不需要写上 </strong>strong。</p>
<h5 id="strong-实现"><a href="#strong-实现" class="headerlink" title="__strong 实现"></a>__strong 实现</h5><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">    <span class="keyword">id</span> __<span class="keyword">strong</span> obj = [[<span class="built_in">NSObject</span> alloc] init];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>通过 clang 获取程序汇编输出，或者 cpp 文件，结合 objc4 库源码，可以分析程序执行的流程。</p>
<blockquote>
<p>其实这部分都是模拟代码，通过总结分析得出的结论。<a href="https://www.jianshu.com/p/df6629ec9a25" target="_blank" rel="noopener">深入解构objc_msgSend函数的实现</a> 这篇文章作者通过把汇编语言转换成 C 语言来具体分析 Objective-C 的消息转发机制。叹为观止。</p>
</blockquote>
<figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="comment">/// 编译器模拟代码</span></span><br><span class="line"><span class="keyword">id</span> obj = objc_msgSend(<span class="built_in">NSObject</span>, <span class="keyword">@selector</span>(alloc));</span><br><span class="line">objc_msgSend(obj, <span class="keyword">@selector</span>(init));</span><br><span class="line">objc_release(obj);</span><br></pre></td></tr></table></figure>
<p>作为对比，看一下转化 C++ 后的执行：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="keyword">id</span> obj = ((<span class="built_in">NSObject</span> *(*)(<span class="keyword">id</span>, SEL))(<span class="keyword">void</span> *)objc_msgSend)((<span class="keyword">id</span>)((<span class="built_in">NSObject</span> *(*)(<span class="keyword">id</span>, SEL))(<span class="keyword">void</span> *)objc_msgSend)((<span class="keyword">id</span>)objc_getClass(<span class="string">"NSObject"</span>), sel_registerName(<span class="string">"alloc"</span>)), sel_registerName(<span class="string">"init"</span>));</span><br></pre></td></tr></table></figure></p>
<p>可以看出，即使 ARC 不支持 release，实际上编辑器还是自动插入了 release。而每个方法都需要执行 objc_msgSend。</p>
<p>而使用 alloc/new/copy/mutableCopy 以外方法生成的对象，又有一些不一样：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">    <span class="keyword">id</span> __<span class="keyword">strong</span> obj = [<span class="built_in">NSMutableArray</span> array];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>编译器的模拟代码：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="keyword">id</span> obj = objc_msgSend(<span class="built_in">NSMutableArray</span>, <span class="keyword">@selector</span>(array));</span><br><span class="line">objc_retainAutoreleaseReturnValue(obj);</span><br><span class="line">objc_release(obj)</span><br></pre></td></tr></table></figure></p>
<blockquote>
<p>objc_retainAutoreleaseReturnValue 函数主要用于程序最优化执行。该函数持有的对象应该是：注册到 autoreleasePool 中对象的方法，或者函数的返回值。</p>
</blockquote>
<p>这种 objc_retainAutoreleaseReturnValue 函数是成对的，与之相对的函数是： objc_autoreleaseReturnValue。来看一下它的使用：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">+(<span class="keyword">id</span>)array &#123;</span><br><span class="line">    <span class="keyword">return</span> [[<span class="built_in">NSMutableArray</span> alloc] init];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>编译器的模拟代码：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">+(<span class="keyword">id</span>)array &#123;</span><br><span class="line">    <span class="keyword">id</span> obj = objc_msgSend(<span class="built_in">NSMutableArray</span>, <span class="keyword">@selector</span>(alloc));</span><br><span class="line">    objc_msgSend(obj, <span class="keyword">@selector</span>(init));</span><br><span class="line">    <span class="keyword">return</span> objc_autoreleaseReturnValue(obj);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<blockquote>
<p>objc_autoreleaseReturnValue: 返回注册到 autoreleasePool 的对象。</p>
</blockquote>
<p><em>书中说，objc_retainAutoreleaseReturnValue 和 objc_autoreleaseReturnValue 方法配合使用可以不将对象注册到 autoreleasePool中</em>，如下图：</p>
<p><img src="https://i.loli.net/2019/11/28/DnAOR2dZCI9Hkx8.png" alt="省略autoreleasePool注册.png"></p>
<h4 id="weak-修饰符"><a href="#weak-修饰符" class="headerlink" title="__weak 修饰符"></a>__weak 修饰符</h4><p>当带有 __strong 修饰符的变量在持有对象时，如果多个对象相互持有，很容易发生循环引用。</p>
<p>循环引用容易发生内存泄漏。所谓内存泄漏就是应当被废弃的对象在超出其生存周期后继续存在。</p>
<h5 id="weak-用法"><a href="#weak-用法" class="headerlink" title="__weak 用法"></a>__weak 用法</h5><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">@interface</span> <span class="title">Test</span>: <span class="title">NSObject</span> </span>&#123;</span><br><span class="line">    <span class="keyword">id</span> __<span class="keyword">strong</span> obj_;</span><br><span class="line">&#125;</span><br><span class="line">-(<span class="keyword">void</span>)setObject:(<span class="keyword">id</span> __<span class="keyword">strong</span>)obj;</span><br><span class="line"><span class="keyword">@end</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">@implementation</span> <span class="title">Test</span></span></span><br><span class="line">-(<span class="keyword">id</span>)init &#123;</span><br><span class="line">    <span class="keyword">self</span> = [<span class="keyword">super</span> init];</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">self</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">-(<span class="keyword">void</span>)setObject:(<span class="keyword">id</span> __<span class="keyword">strong</span>)obj &#123;</span><br><span class="line">    obj_ = obj;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>以下为循环引用：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">    <span class="keyword">id</span> test0 = [[Test alloc] init];     <span class="comment">// test0 强引用对象 A</span></span><br><span class="line">    <span class="keyword">id</span> test1 = [[Test alloc] init];     <span class="comment">// test1 强引用对象 B</span></span><br><span class="line">    [test0 setObject: test1];           <span class="comment">// test0 强引用对象 B</span></span><br><span class="line">    [test1 setObject: test0];           <span class="comment">// test1 强引用对象 A</span></span><br><span class="line">    </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>或者，对象持有自身时，也会发生循环引用：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">    <span class="keyword">id</span> test = [[Test alloc] init];</span><br><span class="line">    [test setObject: test];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p><img src="https://i.loli.net/2019/11/29/A7F8EaODerZHMwk.png" alt="循环引用示意图.png"></p>
<p>使用 <strong>weak 修饰符和避免循环使用。见过检查附有 </strong>weak 修饰符的变量是否为 nil，可以判断被赋值的对象是否已废弃。</p>
<p>__weak 的另一个优点就是，在持有某对象的弱引用时，若该对象被废弃，则此弱引用自动失效且处于 nil 被赋值的状态。</p>
<p>通过下面代码看下 __weak 的特性：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="keyword">id</span> __<span class="keyword">weak</span> obj = [[<span class="built_in">NSObject</span> alloc] init];</span><br></pre></td></tr></table></figure></p>
<p>改代码会出现编译警告，因为 __weak 并不直接持有对象，所以 obj 会被立即释放掉。修改如下即可：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="keyword">id</span> __<span class="keyword">strong</span> obj0 = [[<span class="built_in">NSObject</span> alloc] init];</span><br><span class="line"><span class="keyword">id</span> __<span class="keyword">weak</span> obj1 = obj0;</span><br></pre></td></tr></table></figure></p>
<p>所以，针对上面循环引用的问题，作出修改,即可避免循环引用了：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">@interface</span> <span class="title">Test</span>: <span class="title">NSObject</span> </span>&#123;</span><br><span class="line">    <span class="keyword">id</span> __<span class="keyword">weak</span> obj_;</span><br><span class="line">&#125;</span><br><span class="line">-(<span class="keyword">void</span>)setObject:(<span class="keyword">id</span> __<span class="keyword">strong</span>)obj;</span><br><span class="line"><span class="keyword">@end</span></span><br></pre></td></tr></table></figure></p>
<p><img src="https://i.loli.net/2019/11/29/p9zQtH4rgoKs6eF.png" alt="弱引用避免循环引用.png"></p>
<h5 id="weak-实现"><a href="#weak-实现" class="headerlink" title="__weak 实现"></a>__weak 实现</h5><p>通过前面说明，可以看到 __weak 有如下魔法般的效果：</p>
<ul>
<li>若附有 __weak 修饰符的变量所引用的对象呗废弃，则将 nil 赋值给该变量。</li>
<li>若用附有 __weak 修饰符的的变量，即是使用注册到 autoreleasePool 中的对象。</li>
</ul>
<p>首先，通过研究 __weak 内部实现，再来看下上面的一个问题：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="keyword">id</span> __<span class="keyword">weak</span> obj = [[<span class="built_in">NSObject</span> alloc] init];</span><br></pre></td></tr></table></figure></p>
<p>编译器处理该源码时，模拟如下：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="comment">/// 编译器的模拟代码</span></span><br><span class="line"><span class="keyword">id</span> obj;</span><br><span class="line"><span class="keyword">id</span> tmp = objc_msgSend(<span class="built_in">NSObjct</span>, <span class="keyword">@selector</span>(alloc));</span><br><span class="line">objc_msgSend(tmp, <span class="keyword">@selector</span>(init));</span><br><span class="line">objc_initWeak(&amp;obj, tmp);</span><br><span class="line">objc_release(tmp);</span><br><span class="line">objc_destroyWeak(&amp;obj);</span><br></pre></td></tr></table></figure></p>
<p>假如 <code>[[NSObject alloc] init]</code> 生成了对象 A，因为 __weak 不能持有对象，编译器认为对象 A 没有持有者，就通过 <code>objc_release(tmp);</code> 函数释放和废弃对象 A，所以赋值失败，编译警告。</p>
<p>下面，再来通过合理使用 __weak 分析其内部的实现：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="comment">/// 假设 obj 附加了 __strong 修饰符且对象被赋值</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">id</span> __<span class="keyword">weak</span> obj1 = obj;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>该源代码可转换为如下形式：</p>
<figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="comment">/// 编译器的模拟代码</span></span><br><span class="line"><span class="keyword">id</span> obj1;</span><br><span class="line">objc_initWeak(&amp;obj1, obj);</span><br><span class="line"><span class="keyword">id</span> temp = objc_loadWeakRetained(&amp;obj1);</span><br><span class="line">objc_autorelease(temp);</span><br><span class="line">objc_destroyWeak(&amp;obj1);</span><br></pre></td></tr></table></figure>
<ul>
<li>objc_loadWeakRetaine 函数取出附有 __weak 修饰符的变量所引用的对象，并 retain。</li>
<li>objc_autorelease 函数将对象注册到 autoreleasePool 中。</li>
</ul>
<p>大量的使用 <strong>weak 修饰符修饰的变量，注册到 autoreleasepool 的对象也会大量增加。因此使用附有 </strong>weak 修饰符的变量时，最好先暂时赋值给附有 __strong 修饰符的变量后在使用。如下：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">    <span class="keyword">id</span> __<span class="keyword">weak</span> <span class="number">0</span> = obj;</span><br><span class="line">    <span class="built_in">NSLog</span>(<span class="string">@"1 %@"</span>, o);      <span class="comment">// o 注册到 autoreleasepool 1 次</span></span><br><span class="line">    <span class="built_in">NSLog</span>(<span class="string">@"2 %@"</span>, o);      <span class="comment">// o 注册到 autoreleasepool 2 次</span></span><br><span class="line">    <span class="built_in">NSLog</span>(<span class="string">@"3 %@"</span>, o);      <span class="comment">// o 注册到 autoreleasepool 3 次</span></span><br><span class="line">    <span class="built_in">NSLog</span>(<span class="string">@"4 %@"</span>, o);      <span class="comment">// o 注册到 autoreleasepool 4 次</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>即每次使用，都会注册到 autoreleasepool 中。如果先赋值给 __strong 变量:<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">    <span class="keyword">id</span> __<span class="keyword">weak</span> <span class="number">0</span> = obj;</span><br><span class="line">    <span class="keyword">id</span> __<span class="keyword">strong</span> tmp = o;    <span class="comment">// o 注册到 autoreleasepool 1 次</span></span><br><span class="line">    <span class="built_in">NSLog</span>(<span class="string">@"1 %@"</span>, tmp);      </span><br><span class="line">    <span class="built_in">NSLog</span>(<span class="string">@"2 %@"</span>, tmp);      </span><br><span class="line">    <span class="built_in">NSLog</span>(<span class="string">@"3 %@"</span>, tmp);    </span><br><span class="line">    <span class="built_in">NSLog</span>(<span class="string">@"4 %@"</span>, tmp);    </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<blockquote>
<p>书中原话是： 在 “tmp = o;” 时对象仅登录到 autoreleasepool 中 1 次。</p>
</blockquote>
<p>这里再看下 Swift 中常用的写法，大概也是这个原因：<br><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line">loginVC.loginSuccess = &#123; [<span class="keyword">weak</span> <span class="keyword">self</span>] phoneNum <span class="keyword">in</span></span><br><span class="line">    <span class="keyword">guard</span> <span class="keyword">let</span> strongSelf = <span class="keyword">self</span> <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="keyword">return</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>这里重点说两个方法：</p>
<ul>
<li>objc_initWeak(&amp;obj1, obj): 初始化附有 <strong>weak 修饰符的变量，具体通过执行 <code>objc_storeWeak(&amp;obj1, obj)</code>，将附有 </strong>weak 修饰符的变量地址注册到 weak 表中。</li>
<li>objc_destroyWeak(&amp;obj1)：释放一个 __weak 变量。具体通过执行 <code>objc_storeWeak(&amp;obj1, 0)</code>，把变量的地址从 weak 表中删除。</li>
</ul>
<p>简单来说，就是 <code>objc_storeWeak(&amp;obj1, obj)</code> 通过第二个参数决定是注册变量地址到 weak 表，还是删除地址。</p>
<p>通常面试到 weak 问题时，都会问下 weak 的实现原理，主要问的就是对 weak 表的理解。</p>
<blockquote>
<p>weak 表与引用计数表相同，作为散列表被实现。如果使用 weak 表，将废弃对象的地址作为键值进行检索，就能高速获取对应的附有 <strong>weak 修饰符的变量的地址。另外，由于一个对象可以赋值给多个附有 </strong>weak 修饰符的变量，所以一个键值，可注册多个变量的地址。</p>
</blockquote>
<p>当对象被释放时，执行流程是：</p>
<ul>
<li>objc_release</li>
<li>因为引用计数为 0，所以执行 dealloc</li>
<li>_objc_rootDealloc</li>
<li>object_dispose</li>
<li>objct_destructInstance</li>
<li>objc_clear_deallocating<ul>
<li>从 weak 表中获取废弃对象的地址为键值的记录。</li>
<li>将包含在记录中所有附有 __weak 修饰符变量的地址，赋值为 nil。</li>
<li>从 weak 表中删除该记录。</li>
<li>从引用计数表众删除被废弃对象的地址为键值的记录。</li>
</ul>
</li>
</ul>
<p>由上可知，大量使用 <strong>weak 修饰符的变量，会消耗相应的 CPU 资源。我们只在需要避免循环吗引用时使用 </strong>weak 修饰符即可。</p>
<p>另外，<strong>实际上存在着不支持 __weak 修饰符的类</strong>。但是这种类极为罕见。如 <code>NSMachPort</code>、<code>allowsWeakReference</code>、<code>retainWeakReference</code> 等。知道就好了。</p>
<h4 id="unsafe-unretained-修饰符"><a href="#unsafe-unretained-修饰符" class="headerlink" title="_unsafe_unretained 修饰符"></a>_unsafe_unretained 修饰符</h4><p>在 iOS4 以及 OSX Snow Leopard 的应用程序中代替 __weak 修饰符的。附有 _unsafe_unretained 修饰符的变量不属于编译器的内存管理对象。</p>
<p>这里有了解即可，不在细说。</p>
<h4 id="autoreleasing-修饰符"><a href="#autoreleasing-修饰符" class="headerlink" title="__autoreleasing 修饰符"></a>__autoreleasing 修饰符</h4><h5 id="autoreleasing-使用方法"><a href="#autoreleasing-使用方法" class="headerlink" title="__autoreleasing 使用方法"></a>__autoreleasing 使用方法</h5><p>在 ARC 有效时，用 @autoreleasepool 块代替 MRC 下 NSAutoreleasePool 类，用附有 __autoreleasing 修饰符的变量代替 MRC 下 autorelease 方法，即对象被注册到 autoreleasepool。</p>
<p><img src="https://i.loli.net/2019/11/29/rVEcXHaZIORCQJj.png" alt="arc-autorelease.png"></p>
<p>通常，我们并不需要显式的调用 __autoreleasing 修饰符。</p>
<p>在访问附有 __autoreleasing 修饰符的变量时，实际上必定要访问注册到 autoreleasepool 的对象。<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="keyword">id</span> obj0 = [[<span class="built_in">NSObject</span> alloc] init];</span><br><span class="line"><span class="keyword">id</span> __<span class="keyword">weak</span> obj1 = obj0;</span><br><span class="line"><span class="built_in">NSLog</span>(<span class="string">@"class = %@"</span>, [obj1 <span class="keyword">class</span>]);     <span class="comment">// NSObject</span></span><br></pre></td></tr></table></figure></p>
<p>以下源代码与其相同：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="keyword">id</span> obj0 = [[<span class="built_in">NSObject</span> alloc] init];</span><br><span class="line"><span class="keyword">id</span> __<span class="keyword">weak</span> obj1 = obj0;</span><br><span class="line"><span class="keyword">id</span> __autoreleasing tmp = obj1;</span><br><span class="line"><span class="built_in">NSLog</span>(<span class="string">@"class = %@"</span>, [tmp <span class="keyword">class</span>]);     <span class="comment">// NSObject</span></span><br></pre></td></tr></table></figure></p>
<p>为什么在访问附有 <strong>autoreleasing 修饰符的变量时，必须要访问注册到 autoreleasepool 的对象呢？这是因为 </strong>weak 修饰符只持有对象的弱引用，而在访问引用对象的过程中，该对象有可能被废弃，如果把要访问的对象注册到 autoreleasepool 中，那么在 @autoreleasepool 块结束之前都能确保该对象存在。因此，使用 __weak 修饰符的变量就不定要使用注册到 autoreleasepool 中的对象。</p>
<blockquote>
<p>上面这段话是书中原话，但是结合上文 <strong>weak 的实现原理，感觉这么描述不是很对。应该说，附有 </strong>weak 修饰符的变量持有的对象，如果该对象不在 autoreleasepool 中，则编译器会将该对象注册到 autoreleasepool 中，并提供给该变量使用。这个只是个人理解，作为参考。</p>
</blockquote>
<h5 id="autoreleasing-内部实现"><a href="#autoreleasing-内部实现" class="headerlink" title="__autoreleasing 内部实现"></a>__autoreleasing 内部实现</h5><p>将对象赋值给附有 __autoreleasing 修饰符的变量，等同于 ARC 无效时调用对象的 autorelease 方法。<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="keyword">@autoreleasepool</span> &#123;</span><br><span class="line">    <span class="keyword">id</span> __autoreleasing obj = [[<span class="built_in">NSObject</span> alloc] init];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>该源码主要将 NSObject 类对象注册到 autoreleasepool 中，可作如下转换：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="comment">/// 编译器的模拟代码</span></span><br><span class="line"><span class="keyword">id</span> pool = objc_autoreleasePoolPush();</span><br><span class="line"><span class="keyword">id</span> obj = objc_msgSend(<span class="built_in">NSObject</span>, <span class="keyword">@selector</span>(alloc));</span><br><span class="line">objc_msgSend(obj, <span class="keyword">@selector</span>(init));</span><br><span class="line">objc_autorelease(obj);</span><br><span class="line">objc_autoreleasePoolPop(pool);</span><br></pre></td></tr></table></figure></p>
<p>这里能够看到 pool 入栈、执行autorelease、出栈 三个方法。在 MRC 下有过详细说明。</p>
<p>在 alloc/new/copy/mutableCopy 方法群之外的方法中使用注册到 autoreleasepool 中的对象，会有一些区别：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="keyword">@autoreleasepool</span> &#123;</span><br><span class="line">    <span class="keyword">id</span> __autoreleasing obj = [<span class="built_in">NSMutableArray</span> array];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>编译器模拟代码如下：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="keyword">id</span> pool = objc_autoreleasePoolPush();</span><br><span class="line"><span class="keyword">id</span> obj = objc_msgSend(<span class="built_in">NSMutableArray</span>, <span class="keyword">@selector</span>(array));</span><br><span class="line">objc_retainAutoreleaseReturnValue(obj);</span><br><span class="line">objc_autorelease(obj);</span><br><span class="line">objc_autoreleasePoolPop(pool);</span><br></pre></td></tr></table></figure></p>
<p>注册到 autoreleasepool 的方法 objc_autorelease 并没有变。</p>
<h4 id="引用计数"><a href="#引用计数" class="headerlink" title="引用计数"></a>引用计数</h4><p>参考：<a href="https://blog.csdn.net/iuyo89007/article/details/51761720?utm_source=blogxgwz7" target="_blank" rel="noopener">iOS ARC下获取引用计数(retain count)</a></p>
<p>在 ARC 有效时，是无法正常查看一个类当前的引用计数的。不过，可以通过下面三个方法来获取到：</p>
<ol>
<li>使用 KVC</li>
<li>使用私有 API</li>
<li>使用CFGetRetainCount</li>
</ol>
<p>实践如下：</p>
<figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="keyword">id</span> obj = [[<span class="built_in">NSObject</span> alloc] init];</span><br><span class="line"><span class="keyword">id</span> __<span class="keyword">weak</span> obj1 = obj;</span><br><span class="line"><span class="comment">// KVC</span></span><br><span class="line"><span class="built_in">NSLog</span>(<span class="string">@"count10 = %@"</span>,[obj valueForKey:<span class="string">@"retainCount"</span>]);      <span class="comment">// 1</span></span><br><span class="line"><span class="built_in">NSLog</span>(<span class="string">@"count11 = %@"</span>,[obj1 valueForKey:<span class="string">@"retainCount"</span>]);     <span class="comment">// 2</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">NSLog</span>(<span class="string">@"address0 = %p"</span>, obj);           <span class="comment">// 0x600001757be0</span></span><br><span class="line"><span class="built_in">NSLog</span>(<span class="string">@"address1 = %p"</span>, obj1);          <span class="comment">// 0x600001757be0</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 私有API</span></span><br><span class="line">OBJC_EXTERN <span class="keyword">int</span> _objc_rootRetainCount(<span class="keyword">id</span>);</span><br><span class="line"><span class="built_in">NSLog</span>(<span class="string">@"count20 = %d"</span>,_objc_rootRetainCount(obj));             <span class="comment">// 1</span></span><br><span class="line"><span class="built_in">NSLog</span>(<span class="string">@"count21 = %d"</span>,_objc_rootRetainCount(obj1));            <span class="comment">// 2</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 使用CFGetRetainCount</span></span><br><span class="line"><span class="built_in">NSLog</span>(<span class="string">@"count30 = %zd"</span>, <span class="built_in">CFGetRetainCount</span>((__bridge <span class="built_in">CFTypeRef</span>)(obj)));       <span class="comment">// 1</span></span><br><span class="line"><span class="built_in">NSLog</span>(<span class="string">@"count30 = %zd"</span>, <span class="built_in">CFGetRetainCount</span>((__bridge <span class="built_in">CFTypeRef</span>)(obj1)));      <span class="comment">// 2</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">@autoreleasepool</span> &#123;</span><br><span class="line">    <span class="keyword">id</span> anObj = [[<span class="built_in">NSObject</span> alloc] init];</span><br><span class="line">    <span class="keyword">id</span> __autoreleasing o = anObj;</span><br><span class="line">    <span class="built_in">NSLog</span>(<span class="string">@"count40 = %@"</span>,[anObj valueForKey:<span class="string">@"retainCount"</span>]);      <span class="comment">// 2</span></span><br><span class="line">    <span class="built_in">NSLog</span>(<span class="string">@"count40 = %@"</span>,[o valueForKey:<span class="string">@"retainCount"</span>]);          <span class="comment">// 2</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ul>
<li>由于弱引用不持有对象，附有 __weak 修饰符的变量不会对原对象的引用计数产生影响。</li>
<li>__autoreleasing 持有对象。对引用计数产生影响。</li>
</ul>
<blockquote>
<p>为什么 weak 变量指向对象的引用计数改变了，其实我不是很确定，虽然对象地址一样，但是可能是 weak 表导致的。这里不是特别清楚。</p>
</blockquote>
<h3 id="ARC-规则"><a href="#ARC-规则" class="headerlink" title="ARC 规则"></a>ARC 规则</h3><p>在 ARC 有效的情况下编译源代码，必须遵守一定的规则：</p>
<ul>
<li>不能使用 retain/release/retainCount/autorelease</li>
<li>不能使用 NSAllocateObject/NSDeallocateObjct</li>
<li>必须遵循内存管理的方法命名规则</li>
<li>不要显式调用 dealloc</li>
<li>使用 @autoreleasepool 代替 NSAutoreleasePool</li>
<li>不能使用区域 NSZone</li>
<li>对象型变量不能作为 C 语言结构体（struct/union）的成员</li>
<li>显式转换 id 和 void*</li>
</ul>
<h4 id="1-不能使用-retain-release-retainCount-autorelease"><a href="#1-不能使用-retain-release-retainCount-autorelease" class="headerlink" title="1. 不能使用 retain/release/retainCount/autorelease"></a>1. 不能使用 retain/release/retainCount/autorelease</h4><p>ARC 有效时，禁止使用 retain/release/retainCount/autorelease。否则会编译报错。</p>
<h4 id="2-不能使用-NSAllocateObject-NSDeallocateObjct"><a href="#2-不能使用-NSAllocateObject-NSDeallocateObjct" class="headerlink" title="2. 不能使用 NSAllocateObject/NSDeallocateObjct"></a>2. 不能使用 NSAllocateObject/NSDeallocateObjct</h4><p>ARC 有效时，使用 NSAllocateObject/NSDeallocateObjct 会编译报错。</p>
<h4 id="3-必须遵循内存管理的方法命名规则"><a href="#3-必须遵循内存管理的方法命名规则" class="headerlink" title="3. 必须遵循内存管理的方法命名规则"></a>3. 必须遵循内存管理的方法命名规则</h4><p>对象的生成／持有的方法必须遵循以下命名规则：</p>
<ul>
<li>alloc</li>
<li>new</li>
<li>copy</li>
<li>mutableCopy</li>
<li>init</li>
</ul>
<p>前四种方法和 MRC 下一样。而关于init方法的要求则更为严格：</p>
<ul>
<li>必须是实例方法</li>
<li>必须返回对象</li>
<li>返回对象的类型必须是id类型或方法声明类的对象类型</li>
</ul>
<h4 id="4-不要显式调用-dealloc"><a href="#4-不要显式调用-dealloc" class="headerlink" title="4. 不要显式调用 dealloc"></a>4. 不要显式调用 dealloc</h4><p>对象被废弃时，不论 ARC 是否有效，都会调用对象的 dealloc 方法。</p>
<p>ARC 无效时：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">-(<span class="keyword">void</span>)dealloc &#123;</span><br><span class="line">    [<span class="keyword">super</span> dealloc];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>ARC 有效时，dealloc 无法显式调用，否则编译报错。ARC 会自动处理这个方法，因此也不比书写 <code>[super dealloc]</code>。dealloc 中只需书写废弃对象时所要做的操作即可：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">-(<span class="keyword">void</span>) &#123;</span><br><span class="line">    <span class="comment">// 处理</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<h4 id="5-使用-autoreleasepool-代替-NSAutoreleasePool"><a href="#5-使用-autoreleasepool-代替-NSAutoreleasePool" class="headerlink" title="5. 使用 @autoreleasepool 代替 NSAutoreleasePool"></a>5. 使用 @autoreleasepool 代替 NSAutoreleasePool</h4><p>ARC 有效时，使用 NSAutoreleasePool 会编译报错。</p>
<h4 id="6-不能使用区域-NSZone"><a href="#6-不能使用区域-NSZone" class="headerlink" title="6. 不能使用区域 NSZone"></a>6. 不能使用区域 NSZone</h4><p>不管 ARC 是否有效，区域 NSZone 在现在的运行时系统（编译器宏 <strong>OBJC2</strong> 被设定的环境）中已单纯地被忽略。并且 ARC 有效时，使用 NSZone 会编译报错。</p>
<h4 id="7-对象型变量不能作为-C-语言结构体（struct-union）的成员"><a href="#7-对象型变量不能作为-C-语言结构体（struct-union）的成员" class="headerlink" title="7. 对象型变量不能作为 C 语言结构体（struct/union）的成员"></a>7. 对象型变量不能作为 C 语言结构体（struct/union）的成员</h4><p>C语言的结构体如果存在Objective-C对象型变量，便会引起错误，因为C语言在规约上没有方法来管理结构体成员的生存周期。</p>
<h4 id="8-显式转换-id-和-void"><a href="#8-显式转换-id-和-void" class="headerlink" title="8. 显式转换 id 和 void*"></a>8. 显式转换 id 和 void*</h4><p>非ARC下，这两个类型是可以直接赋值的：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="keyword">id</span> obj = [<span class="built_in">NSObject</span> alloc] init];</span><br><span class="line"><span class="keyword">void</span> *p = obj;</span><br><span class="line"><span class="keyword">id</span> o = p;</span><br></pre></td></tr></table></figure></p>
<p>但是在ARC下就会引起编译错误。为了避免错误，我们需要通过__bridege来转换。<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="keyword">id</span> obj = [[<span class="built_in">NSObject</span> alloc] init];</span><br><span class="line"><span class="keyword">void</span> *p = (__bridge <span class="keyword">void</span>*)obj;<span class="comment">//显式转换</span></span><br><span class="line"><span class="keyword">id</span> o = (__bridge <span class="keyword">id</span>)p;<span class="comment">//显式转换</span></span><br></pre></td></tr></table></figure></p>
<blockquote>
<p>书中用了大量的篇幅介绍桥接，这里暂时不做扩展了。</p>
</blockquote>
<h3 id="属性"><a href="#属性" class="headerlink" title="属性"></a>属性</h3><p>属性的声明和所有权修饰符的对应关系：</p>
<table>
<thead>
<tr>
<th style="text-align:left">属性关键字</th>
<th>所有权修饰符</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left">assign</td>
<td>__unsafe_unretained 修饰符</td>
</tr>
<tr>
<td style="text-align:left">copy</td>
<td>__strong 修饰符（但是赋值的是被复制的对象）</td>
</tr>
<tr>
<td style="text-align:left">retain</td>
<td>__strong 修饰符</td>
</tr>
<tr>
<td style="text-align:left">strong</td>
<td>__strong 修饰符</td>
</tr>
<tr>
<td style="text-align:left">unsafe_unretained</td>
<td>__unsafe_unretained 修饰符</td>
</tr>
<tr>
<td style="text-align:left">weak</td>
<td>__weak 修饰符</td>
</tr>
</tbody>
</table>
<h3 id="数组"><a href="#数组" class="headerlink" title="数组"></a>数组</h3><p><strong>unsafe_unretained 修饰符以外的 </strong>strong/<strong>weak/</strong>autoreleasing 修饰符保证其指定的变量初始化为 nil。</p>
<blockquote>
<p>书中说了一些 C 语言相关的数组处理，不在细说。</p>
</blockquote>
<h2 id="后记"><a href="#后记" class="headerlink" title="后记"></a>后记</h2><p>不论是作为面试知识，还是对 Objective-C 有更深入的了解，引用计数都值得我们深入学习下。下一篇写 Block ，希望一周时间能补出来。</p>
<p><a href="">《Objective-C高级编程》三篇总结之二：Block 篇</a></p>
]]></content>
  </entry>
  <entry>
    <title>Swift进阶之八：泛型</title>
    <url>/2019/11/23/Swift%E8%BF%9B%E9%98%B6%E4%B9%8B%E5%85%AB%EF%BC%9A%E6%B3%9B%E5%9E%8B/</url>
    <content><![CDATA[<h2 id="泛型"><a href="#泛型" class="headerlink" title="泛型"></a>泛型</h2><p>泛型编程的目的是表达算法或者数据结构所要求的 <strong>核心接口</strong>。</p>
<a id="more"></a>
<h3 id="重载"><a href="#重载" class="headerlink" title="重载"></a>重载</h3><p>拥有同样的名字，但是参数和返回类型不同的多个方法互相被称为重载方法。</p>
<h4 id="自由函数的重载"><a href="#自由函数的重载" class="headerlink" title="自由函数的重载"></a>自由函数的重载</h4><ol>
<li><code>Swift</code> 有一系列复杂的规则来确定到底使用了那个重载函数，这套规则是基于函数是否有泛型，以及传入的参数是怎么样的类型来确定使用优先级。总结为一句话：<strong>选择最具体的一个</strong>。也就是说， 非通用函数会优先于通用函数被使用。如下：<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">raise</span><span class="params">(<span class="number">_</span> base: Double, to exponent: Double)</span></span> -&gt; <span class="type">Double</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> pow(base, exponent)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">raise</span><span class="params">(<span class="number">_</span> base: Float, to exponent: Float)</span></span> -&gt; <span class="type">Float</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> powf(base, exponent)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> double = raise(<span class="number">2.0</span>, to: <span class="number">3</span>)              <span class="comment">// 8</span></span><br><span class="line">type(of: double)                            <span class="comment">// Double.Type</span></span><br><span class="line"><span class="keyword">let</span> float: <span class="type">Float</span> = raise(<span class="number">2.0</span>, to: <span class="number">3.0</span>)      <span class="comment">// 8</span></span><br><span class="line">type(of: float)                             <span class="comment">// Float.Type</span></span><br></pre></td></tr></table></figure>
</li>
</ol>
<p>例如，系统的 <code>CGRect</code> 就是一个很好的重载的例子：<br><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">init</span>(x: <span class="type">CGFloat</span>, y: <span class="type">CGFloat</span>, width: <span class="type">CGFloat</span>, height: <span class="type">CGFloat</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">init</span>(x: <span class="type">Double</span>, y: <span class="type">Double</span>, width: <span class="type">Double</span>, height: <span class="type">Double</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">init</span>(x: <span class="type">Int</span>, y: <span class="type">Int</span>, width: <span class="type">Int</span>, height: <span class="type">Int</span>)</span><br></pre></td></tr></table></figure></p>
<ol start="2">
<li>重载的使用是在编译期间静态决定的，也就是说，编译器会依赖变量的静态类型来决定要调用哪一个重载，而不是运行时根据值的动态类型来决定。如下：<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">log</span>&lt;V: UIView&gt;<span class="params">(<span class="number">_</span> view: V)</span></span> &#123;</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">"It's a <span class="subst">\(type(of: view)</span>), frame:<span class="subst">\(view.frame)</span>"</span>)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">log</span><span class="params">(<span class="number">_</span> view: UILabel)</span></span> &#123;</span><br><span class="line">    <span class="keyword">let</span> text = view.text ?? <span class="string">"Empty"</span></span><br><span class="line">    <span class="built_in">print</span>(<span class="string">"It's a label, text = <span class="subst">\(text)</span>"</span>)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> label = <span class="type">UILabel</span>(frame: <span class="type">CGRect</span>(x: <span class="number">10</span>, y: <span class="number">10</span>, width: <span class="number">100</span>, height: <span class="number">40</span>))</span><br><span class="line">label.text = <span class="string">"Name"</span></span><br><span class="line">log(label)</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> button = <span class="type">UIButton</span>(frame: <span class="type">CGRect</span>(x: <span class="number">100</span>, y: <span class="number">100</span>, width: <span class="number">100</span>, height: <span class="number">100</span>))</span><br><span class="line">log(button)</span><br><span class="line"><span class="comment">// 输出：</span></span><br><span class="line"><span class="type">It's</span> a label, text = <span class="type">Name</span></span><br><span class="line"><span class="type">It's</span> a <span class="type">UIButton</span>, frame:(<span class="number">100.0</span>, <span class="number">100.0</span>, <span class="number">100.0</span>, <span class="number">100.0</span>)</span><br></pre></td></tr></table></figure>
</li>
</ol>
<p>对上面代码修改：<br><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">log</span>&lt;V: UIView&gt;<span class="params">(<span class="number">_</span> view: V)</span></span> &#123;</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">"It's a <span class="subst">\(type(of: view)</span>), frame:<span class="subst">\(view.frame)</span>"</span>)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">log</span><span class="params">(<span class="number">_</span> view: UILabel)</span></span> &#123;</span><br><span class="line">    <span class="keyword">let</span> text = view.text ?? <span class="string">"Empty"</span></span><br><span class="line">    <span class="built_in">print</span>(<span class="string">"It's a label, text = <span class="subst">\(text)</span>"</span>)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> label = <span class="type">UILabel</span>(frame: <span class="type">CGRect</span>(x: <span class="number">10</span>, y: <span class="number">10</span>, width: <span class="number">100</span>, height: <span class="number">40</span>))</span><br><span class="line">label.text = <span class="string">"Name"</span></span><br><span class="line">log(label)</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> button = <span class="type">UIButton</span>(frame: <span class="type">CGRect</span>(x: <span class="number">100</span>, y: <span class="number">100</span>, width: <span class="number">100</span>, height: <span class="number">100</span>))</span><br><span class="line">log(button)</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> views = [label, button]</span><br><span class="line">views.forEach &#123; (v) <span class="keyword">in</span></span><br><span class="line">    log(v)</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 输出：</span></span><br><span class="line"><span class="type">It's</span> a label, text = <span class="type">Name</span></span><br><span class="line"><span class="type">It's</span> a <span class="type">UIButton</span>, frame:(<span class="number">100.0</span>, <span class="number">100.0</span>, <span class="number">100.0</span>, <span class="number">100.0</span>)</span><br><span class="line"><span class="type">It's</span> a <span class="type">UILabel</span>, frame:(<span class="number">10.0</span>, <span class="number">10.0</span>, <span class="number">100.0</span>, <span class="number">40.0</span>)</span><br><span class="line"><span class="type">It's</span> a <span class="type">UIButton</span>, frame:(<span class="number">100.0</span>, <span class="number">100.0</span>, <span class="number">100.0</span>, <span class="number">100.0</span>)</span><br></pre></td></tr></table></figure></p>
<p>证明一点：<strong>重载的使用是在编译期间静态决定</strong>。</p>
<h4 id="运算符重载"><a href="#运算符重载" class="headerlink" title="运算符重载"></a>运算符重载</h4><p>运算符重载与自由函数不同：对于重载的运算符，类型检查器会使用非泛型版本，而不考虑泛型版本。</p>
<p>下面，我们对前面的幂运算，我们先重新定一个运算符，然后重载：</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="comment">/// 幂运算比乘法运算优先级更高</span></span><br><span class="line">precedencegroup <span class="type">ExponentiationPrecedence</span> &#123;</span><br><span class="line">    <span class="keyword">associativity</span>: <span class="keyword">left</span></span><br><span class="line">    higherThan: <span class="type">MultiplicationPrecedence</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">infix</span> <span class="keyword">operator</span> **: <span class="type">ExponentiationPrecedence</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> **<span class="params">(lhs: Double, rhs: Double)</span></span> -&gt; <span class="type">Double</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> pow(lhs, rhs)</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 重载</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> **&lt;I: SignedInteger&gt;<span class="params">(lhs: I, rhs: I)</span></span> -&gt; <span class="type">I</span> &#123;</span><br><span class="line">    <span class="keyword">let</span> result = <span class="type">Double</span>(lhs) ** <span class="type">Double</span>(rhs)</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">numericCast</span>(<span class="type">Int</span>(result))</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>如果写 <code>2 ** 3</code> 会报错，需要写 <code>2.3 ** 3.0</code> 方可。</p>
<h4 id="使用泛型约束进行重载"><a href="#使用泛型约束进行重载" class="headerlink" title="使用泛型约束进行重载"></a>使用泛型约束进行重载</h4><p>即不具体到具体类型处理问题。传入的变量符合规范几个，如下：<br><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">isSubset</span>&lt;S: Sequence&gt;<span class="params">(of other: S)</span></span> -&gt; <span class="type">Bool</span></span><br></pre></td></tr></table></figure></p>
<h4 id="使用闭包对行为进行参数化"><a href="#使用闭包对行为进行参数化" class="headerlink" title="使用闭包对行为进行参数化"></a>使用闭包对行为进行参数化</h4><p>简单来说，<code>map</code>、<code>reduce</code> 等高阶函数即是如此。</p>
<h3 id="对集合采用泛型操作"><a href="#对集合采用泛型操作" class="headerlink" title="对集合采用泛型操作"></a>对集合采用泛型操作</h3><blockquote>
<p>先 pass，之后对算法有单独整理</p>
</blockquote>
<h2 id="简单总结"><a href="#简单总结" class="headerlink" title="简单总结"></a>简单总结</h2><p>这本书中介绍了泛型一些偏门，或者说更加深层次的运用，暂时搁置吧。希望后期来补。</p>
]]></content>
  </entry>
  <entry>
    <title>Swift进阶之七：错误处理</title>
    <url>/2019/11/23/Swift%E8%BF%9B%E9%98%B6%E4%B9%8B%E4%B8%83%EF%BC%9A%E9%94%99%E8%AF%AF%E5%A4%84%E7%90%86/</url>
    <content><![CDATA[<h2 id="错误处理"><a href="#错误处理" class="headerlink" title="错误处理"></a>错误处理</h2><h3 id="Result-类型"><a href="#Result-类型" class="headerlink" title="Result 类型"></a><code>Result</code> 类型</h3><p>先看下定义：</p>
<a id="more"></a>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">enum</span> <span class="title">Result</span>&lt;<span class="title">Success</span>, <span class="title">Failure</span>&gt; <span class="title">where</span> <span class="title">Failure</span> : <span class="title">Error</span> </span>&#123;</span><br><span class="line">    <span class="comment">/// A success, storing a `Success` value.</span></span><br><span class="line">    <span class="keyword">case</span> success(<span class="type">Success</span>)</span><br><span class="line"></span><br><span class="line">    <span class="comment">/// A failure, storing a `Failure` value.</span></span><br><span class="line">    <span class="keyword">case</span> failure(<span class="type">Failure</span>)</span><br><span class="line">    <span class="comment">/// 下面还有很多方法定义</span></span><br><span class="line">&#125;</span><br><span class="line">``` </span><br><span class="line">用来判断成功或者失败情况。如 `<span class="type">Alamofire</span>` 网络返回用的就是这个。</span><br><span class="line"></span><br><span class="line">### 抛出和捕获</span><br><span class="line">定义抛出错误类型，如：</span><br><span class="line"></span><br><span class="line">```swift</span><br><span class="line"><span class="class"><span class="keyword">enum</span> <span class="title">ParseError</span>: <span class="title">Error</span> </span>&#123;</span><br><span class="line">    <span class="keyword">case</span> wrongEncoding</span><br><span class="line">    <span class="keyword">case</span> warning(line: <span class="type">Int</span>, message: <span class="type">String</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="带有类型的错误"><a href="#带有类型的错误" class="headerlink" title="带有类型的错误"></a>带有类型的错误</h3><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">enum</span> <span class="title">Result</span>&lt;<span class="title">A</span>, <span class="title">ErrorType</span>&gt; </span>&#123;</span><br><span class="line">    <span class="keyword">case</span> failure(<span class="type">ErrorType</span>)</span><br><span class="line">    <span class="keyword">case</span> success(<span class="type">A</span>)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">parse</span><span class="params">(text: String)</span></span> -&gt; <span class="type">Result</span>&lt;[<span class="type">String</span>], <span class="type">ParseError</span>&gt;</span><br></pre></td></tr></table></figure>
<p>这种写法有时候会忽略返回值，如 <code>_ = parse(&quot;hello&quot;)</code> 。。所以，如果要处理错误，还是用 <code>throws</code> 函数吧。</p>
<h3 id="将错误桥接到-Objective-C"><a href="#将错误桥接到-Objective-C" class="headerlink" title="将错误桥接到 Objective-C"></a>将错误桥接到 <code>Objective-C</code></h3><p>让自定义的错误遵循 <code>CustomNSError</code> 协议即可：<br><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">extension</span> <span class="title">ParseError</span>: <span class="title">CustomNSError</span> </span>&#123;</span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">let</span> errorDomain = <span class="string">"io.objc.parserError"</span></span><br><span class="line">    <span class="keyword">var</span> errorCode: <span class="type">Int</span> &#123;</span><br><span class="line">        <span class="keyword">switch</span> <span class="keyword">self</span> &#123;</span><br><span class="line">        <span class="keyword">case</span> .wrongEncoding:</span><br><span class="line">            <span class="keyword">return</span> <span class="number">100</span></span><br><span class="line">        <span class="keyword">case</span> .warning:</span><br><span class="line">            <span class="keyword">return</span> <span class="number">200</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">var</span> errorUserInfo: [<span class="type">String</span>: <span class="type">Any</span>] &#123;</span><br><span class="line">        <span class="keyword">return</span> [:]</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<h3 id="错误与函数参数"><a href="#错误与函数参数" class="headerlink" title="错误与函数参数"></a>错误与函数参数</h3><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">extension</span> <span class="title">Sequence</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">func</span> <span class="title">all</span><span class="params">(condition: <span class="params">(Iterator.Element)</span></span></span> <span class="keyword">throws</span> -&gt; <span class="type">Bool</span>) <span class="keyword">rethrows</span> -&gt; <span class="type">Bool</span> &#123;</span><br><span class="line">        <span class="keyword">for</span> element <span class="keyword">in</span> <span class="keyword">self</span> &#123;</span><br><span class="line">            <span class="keyword">guard</span> <span class="keyword">try</span> condition(element) <span class="keyword">else</span> &#123; <span class="keyword">return</span> <span class="literal">false</span> &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">true</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ul>
<li><code>rethrows</code> : <code>rethrows</code> 告诉编译器，这个函数只会在它的参数函数抛出错误的时候抛出错误。</li>
</ul>
<h3 id="使用-defer-进行清理"><a href="#使用-defer-进行清理" class="headerlink" title="使用 defer 进行清理"></a>使用 <code>defer</code> 进行清理</h3><ul>
<li>相同作用域的多个 <code>defer</code> ，按照逆序执行。</li>
</ul>
<h3 id="错误和可选值"><a href="#错误和可选值" class="headerlink" title="错误和可选值"></a>错误和可选值</h3><ul>
<li><code>try?</code>：错误原因是可选值。</li>
</ul>
<h3 id="错误链"><a href="#错误链" class="headerlink" title="错误链"></a>错误链</h3><ul>
<li>通过链式语法抛出错误</li>
</ul>
<h3 id="高阶函数和错误"><a href="#高阶函数和错误" class="headerlink" title="高阶函数和错误"></a>高阶函数和错误</h3><ul>
<li>异步 <code>API</code> 错误可以用 <code>Result</code> 或者闭包，<code>throws</code> 不太适合处理异步情况。</li>
</ul>
]]></content>
  </entry>
  <entry>
    <title>Swift进阶之六：字符串</title>
    <url>/2019/11/23/Swift%E8%BF%9B%E9%98%B6%E4%B9%8B%E5%85%AD%EF%BC%9A%E5%AD%97%E7%AC%A6%E4%B8%B2/</url>
    <content><![CDATA[<p>Swift进阶之六：字符串</p>
<h2 id="字符串"><a href="#字符串" class="headerlink" title="字符串"></a>字符串</h2><h3 id="字位簇（cu）和标准等价"><a href="#字位簇（cu）和标准等价" class="headerlink" title="字位簇（cu）和标准等价"></a>字位簇（cu）和标准等价</h3><p>针对不同的特殊符号，所占的字节数是有区别的，这里看个例子即可，具体内容在 <code>Swift 语言指导</code> 字符串章节也有介绍。</p>
<a id="more"></a>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> singel = <span class="string">"Pok\u&#123;00E9&#125;mon"</span>               <span class="comment">// Pokémon</span></span><br><span class="line"><span class="keyword">let</span> double = <span class="string">"Pok\u&#123;0065&#125;\u&#123;0301&#125;mon"</span>       <span class="comment">// Pokémon</span></span><br><span class="line">singel.<span class="built_in">count</span>            <span class="comment">// 7</span></span><br><span class="line">double.<span class="built_in">count</span>            <span class="comment">// 7</span></span><br><span class="line">singel == double        <span class="comment">// true</span></span><br><span class="line">singel.utf16.<span class="built_in">count</span>      <span class="comment">// 7</span></span><br><span class="line">double.utf16.<span class="built_in">count</span>      <span class="comment">// 8</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> emoji = <span class="string">"🥰😍😗😛"</span></span><br><span class="line"><span class="keyword">let</span> flags = <span class="string">"🏳️‍🌈🇦🇿"</span></span><br><span class="line">emoji.<span class="built_in">count</span>             <span class="comment">// 4</span></span><br><span class="line">flags.<span class="built_in">count</span>             <span class="comment">// 2</span></span><br><span class="line">emoji.utf16.<span class="built_in">count</span>       <span class="comment">// 8</span></span><br><span class="line">flags.utf16.<span class="built_in">count</span>       <span class="comment">// 10</span></span><br></pre></td></tr></table></figure>
<h3 id="字符串和集合"><a href="#字符串和集合" class="headerlink" title="字符串和集合"></a>字符串和集合</h3><p><code>String</code> 并不是一个集合，没有继承 <code>Collection</code> 和 <code>Sequence</code> 的协议扩展。<code>String</code> 是一个结构体。</p>
<h4 id="字符串与切片"><a href="#字符串与切片" class="headerlink" title="字符串与切片"></a>字符串与切片</h4><p>字符串切片数据类型为 <code>Substring</code>，一个结构体，可以直接转化为 <code>Stirng</code> :<br><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> world = <span class="string">"Hello World"</span>.suffix(<span class="number">6</span>)     <span class="comment">// World</span></span><br><span class="line"><span class="type">String</span>(world)                           <span class="comment">// World</span></span><br></pre></td></tr></table></figure></p>
<h3 id="简单的正则表达式匹配器"><a href="#简单的正则表达式匹配器" class="headerlink" title="简单的正则表达式匹配器"></a>简单的正则表达式匹配器</h3><p>这里通过切片及可选值实现了一个正则表达式及匹配器。暂时不做扩展。</p>
<h3 id="ExpressibleByStringLiteral"><a href="#ExpressibleByStringLiteral" class="headerlink" title="ExpressibleByStringLiteral"></a><code>ExpressibleByStringLiteral</code></h3><p>实现 <code>String</code> 的字面量协议，其实通过 <code>ExpressibleBy**</code> 几乎可以实现所有类型的字面量。</p>
<h3 id="String-内部结构"><a href="#String-内部结构" class="headerlink" title="String 内部结构"></a><code>String</code> 内部结构</h3><h4 id="Character-的内部组织结构"><a href="#Character-的内部组织结构" class="headerlink" title="Character 的内部组织结构"></a><code>Character</code> 的内部组织结构</h4><h3 id="编码单元标识方式"><a href="#编码单元标识方式" class="headerlink" title="编码单元标识方式"></a>编码单元标识方式</h3><h3 id="CustomStringConvertible-和-CustomDebugStringConvertible"><a href="#CustomStringConvertible-和-CustomDebugStringConvertible" class="headerlink" title="CustomStringConvertible 和 CustomDebugStringConvertible"></a><code>CustomStringConvertible</code> 和 <code>CustomDebugStringConvertible</code></h3><p>打印信息。</p>
<h3 id="文本输出流"><a href="#文本输出流" class="headerlink" title="文本输出流"></a>文本输出流</h3><ul>
<li><code>print</code> </li>
<li><code>dump</code></li>
</ul>
<h3 id="字符串性能"><a href="#字符串性能" class="headerlink" title="字符串性能"></a>字符串性能</h3><p>通过修改编码方式，来优化性能。</p>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p><code>String</code> 是 <code>Swift</code> 最重要的基础类型之一。我们每天都在使用。这一章主要介绍了我们不会经常用到的点。目前还是筹备面试吧，这里不做深究。</p>
]]></content>
  </entry>
  <entry>
    <title>Swift进阶之五：函数</title>
    <url>/2019/11/23/Swift%E8%BF%9B%E9%98%B6%E4%B9%8B%E4%BA%94%EF%BC%9A%E5%87%BD%E6%95%B0/</url>
    <content><![CDATA[<h2 id="函数"><a href="#函数" class="headerlink" title="函数"></a>函数</h2><p><strong>函数可以被赋值给变量，也能够作为函数的输入和输出</strong>。 </p>
<a id="more"></a>
<h3 id="函数的灵活性"><a href="#函数的灵活性" class="headerlink" title="函数的灵活性"></a>函数的灵活性</h3><ul>
<li>函数作为参数</li>
<li>函数作为数据</li>
</ul>
<h3 id="局部函数和变量捕获"><a href="#局部函数和变量捕获" class="headerlink" title="局部函数和变量捕获"></a>局部函数和变量捕获</h3><ul>
<li>方法内部定义方法，即方法嵌套</li>
</ul>
<h3 id="函数作为代理"><a href="#函数作为代理" class="headerlink" title="函数作为代理"></a>函数作为代理</h3><ul>
<li>在代理和协议的模式中，并不适合使用结构体</li>
<li>使用函数，而非代理，即针对结构体，可以使用一些 <strong>闭包</strong> 来代替协议执行一些回调。</li>
</ul>
<h3 id="inout-参数和可变方法"><a href="#inout-参数和可变方法" class="headerlink" title="inout 参数和可变方法"></a><code>inout</code> 参数和可变方法</h3><ul>
<li><code>inout</code> 做的事并不是传递引用，而是通过值传递，然后复制回来。官方文档描述为： <strong><code>inout</code> 参数将一个值传递给函数，函数可以改变这个值，然后将原来的值替换，并从函数中传出</strong>。</li>
</ul>
<h3 id="计算属性和下标"><a href="#计算属性和下标" class="headerlink" title="计算属性和下标"></a>计算属性和下标</h3><ul>
<li>延迟属性 <code>lazy</code> 会被自动声明为 <code>mutating</code> ，因此这个属性也必须声明为 <code>var</code> 。</li>
<li>使用不同的参数重载下标。</li>
</ul>
<h3 id="自动闭包"><a href="#自动闭包" class="headerlink" title="自动闭包"></a>自动闭包</h3><ul>
<li>可逃逸闭包使用 <code>@escaping</code> 修饰</li>
</ul>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>函数是我们开发中接触最多的对象了。这章节大多是实际操作。只要 <code>inout</code> 开发中较少遇到。但是，个人观点，这种非直观性的修改值，还是应该少用。毕竟，后期维护的时候，可能自己都忘了在哪修改的了。</p>
]]></content>
  </entry>
  <entry>
    <title>Swift进阶之四：结构体和类</title>
    <url>/2019/11/23/Swift%E8%BF%9B%E9%98%B6%E4%B9%8B%E5%9B%9B%EF%BC%9A%E7%BB%93%E6%9E%84%E4%BD%93%E5%92%8C%E7%B1%BB/</url>
    <content><![CDATA[<h2 id="结构体和类"><a href="#结构体和类" class="headerlink" title="结构体和类"></a>结构体和类</h2><p>下面是结构体和类的主要区别：</p>
<a id="more"></a>
<ul>
<li>结构体（和枚举）是<strong>值类型</strong>，类是<strong>引用类型</strong>。再设计结构体时，我们可以要求编译器保证不可变性。对类来说，我们要自己确保这件事情。</li>
<li>内存管理方式有所不同。结构体可以直接别持有和访问，但是类的实例只能通过引用间接的访问。结构体不会被引用，但是会被复制。也就是说，结构体的持有者是唯一的，而类却可以有多个持有者。</li>
<li>使用类可以通过继承来共享代码。而结构体是不能被继承的，想要在不同的结构体之间共享代码，就需要使用不同的组合，如组合、泛型以及协议扩展等。</li>
</ul>
<h3 id="值类型"><a href="#值类型" class="headerlink" title="值类型"></a>值类型</h3><ol>
<li>结构体只有一个持有者。比如，当我们将结构体变量传递给一个函数时，函数将接收到结构体的复制，它也只能改变自己的这份复制，这叫做 <strong>值语义（<code>value semantics</code>）</strong>，有时也叫作复制语义。而对对象来说，它们是通过传递引用来工作的，因此类对象会拥有很多持有者，这叫做 <strong>引用语义（<code>reference semantics</code>）</strong>。</li>
<li>因为结构体只有一个持有者，所以它不会造成循环引用。而类和函数都属于引用类型，要避免造成循环引用的问题。</li>
</ol>
<h3 id="可变性"><a href="#可变性" class="headerlink" title="可变性"></a>可变性</h3><p>可变性会带来一些隐藏的问题，因为，总是无法保证一个对象修改后，所以相关的引用也需要这次改动。</p>
<h3 id="结构体"><a href="#结构体" class="headerlink" title="结构体"></a>结构体</h3><ol>
<li>对结构体进行改变，从语义上来说，与重新为它赋值是一样的。当我们改变结构体中某个属性时，意味着改变了结构体，会触发该结构体的属性观察器。如下：<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ViewController</span>: <span class="title">UIViewController</span> </span>&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">Rectangle</span> </span>&#123;</span><br><span class="line">        <span class="keyword">var</span> width: <span class="type">Int</span></span><br><span class="line">        <span class="keyword">var</span> height: <span class="type">Int</span></span><br><span class="line">        <span class="keyword">var</span> someOne: <span class="type">Person</span></span><br><span class="line">        <span class="keyword">var</span> room: <span class="type">Room</span></span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">Room</span> </span>&#123;</span><br><span class="line">        <span class="keyword">var</span> number: <span class="type">Int</span></span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="class"><span class="keyword">class</span> <span class="title">Person</span> </span>&#123;</span><br><span class="line">        <span class="keyword">var</span> name: <span class="type">String</span></span><br><span class="line">        </span><br><span class="line">        <span class="keyword">init</span>(n: <span class="type">String</span>) &#123;</span><br><span class="line">            <span class="keyword">self</span>.name = n</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">var</span> screen = <span class="type">Rectangle</span>(width: <span class="number">0</span>, height: <span class="number">0</span>, someOne: <span class="type">Person</span>(n: <span class="string">"Tom"</span>), room: <span class="type">Room</span>(number: <span class="number">100</span>)) &#123;</span><br><span class="line">        <span class="keyword">didSet</span> &#123;</span><br><span class="line">            <span class="built_in">print</span>(<span class="string">"Screen changed: <span class="subst">\(screen)</span>"</span>)</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">func</span> <span class="title">viewDidLoad</span><span class="params">()</span></span> &#123;</span><br><span class="line">        <span class="keyword">super</span>.viewDidLoad()</span><br><span class="line">        screen.width = <span class="number">10</span></span><br><span class="line">        <span class="built_in">print</span>(<span class="string">"----------分割线----------"</span>)</span><br><span class="line">        screen.someOne = <span class="type">Person</span>(n: <span class="string">"Lilei"</span>)</span><br><span class="line">        <span class="built_in">print</span>(<span class="string">"----------分割线----------"</span>)</span><br><span class="line">        screen.someOne.name = <span class="string">"Jack"</span></span><br><span class="line">        <span class="built_in">print</span>(<span class="string">"----------分割线----------"</span>)</span><br><span class="line">        screen.room.number = <span class="number">24</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line">打印结果：</span><br><span class="line"><span class="type">Screen</span> changed: <span class="type">Rectangle</span>(width: <span class="number">10</span>, height: <span class="number">0</span>, someOne: <span class="type">SwiftDeep</span>.<span class="type">ViewController</span>.<span class="type">Person</span>, room: <span class="type">SwiftDeep</span>.<span class="type">ViewController</span>.<span class="type">Room</span>(number: <span class="number">100</span>))</span><br><span class="line">----------分割线----------</span><br><span class="line"><span class="type">Screen</span> changed: <span class="type">Rectangle</span>(width: <span class="number">10</span>, height: <span class="number">0</span>, someOne: <span class="type">SwiftDeep</span>.<span class="type">ViewController</span>.<span class="type">Person</span>, room: <span class="type">SwiftDeep</span>.<span class="type">ViewController</span>.<span class="type">Room</span>(number: <span class="number">100</span>))</span><br><span class="line">----------分割线----------</span><br><span class="line">----------分割线----------</span><br><span class="line"><span class="type">Screen</span> changed: <span class="type">Rectangle</span>(width: <span class="number">10</span>, height: <span class="number">0</span>, someOne: <span class="type">SwiftDeep</span>.<span class="type">ViewController</span>.<span class="type">Person</span>, room: <span class="type">SwiftDeep</span>.<span class="type">ViewController</span>.<span class="type">Room</span>(number: <span class="number">24</span>))</span><br></pre></td></tr></table></figure>
</li>
</ol>
<ul>
<li>结构体内部属性，或者内部结构体即结构体属性发生变化时，最外层的结构体也会被改变</li>
<li>结构体内部对象改变时，因为引用地址改变，外层结构体改变</li>
<li>结构体内部对象的属性改变时，因为对象引用地址未变，所以外层结构体不会重新赋值</li>
</ul>
<h3 id="写时复制"><a href="#写时复制" class="headerlink" title="写时复制"></a>写时复制</h3><p>对于 <code>Swift</code> 来说，字典是直接存在堆上的，简单看个例子：<br><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = [<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>]</span><br><span class="line"><span class="keyword">let</span> b = [<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>]</span><br><span class="line"><span class="keyword">let</span> <span class="built_in">c</span> = a</span><br><span class="line"><span class="built_in">print</span>(<span class="string">"a address: "</span>, <span class="type">Unmanaged</span>.passRetained(a <span class="keyword">as</span> <span class="type">AnyObject</span>).toOpaque())</span><br><span class="line"><span class="built_in">print</span>(<span class="string">"b address: "</span>, <span class="type">Unmanaged</span>.passRetained(b <span class="keyword">as</span> <span class="type">AnyObject</span>).toOpaque())</span><br><span class="line"><span class="built_in">print</span>(<span class="string">"c address: "</span>, <span class="type">Unmanaged</span>.passRetained(<span class="built_in">c</span> <span class="keyword">as</span> <span class="type">AnyObject</span>).toOpaque())</span><br><span class="line">a.append(<span class="number">10</span>)</span><br><span class="line"><span class="built_in">print</span>(<span class="string">"a new address: "</span>, <span class="type">Unmanaged</span>.passRetained(a <span class="keyword">as</span> <span class="type">AnyObject</span>).toOpaque())</span><br><span class="line">输出：</span><br><span class="line">a address:  <span class="number">0x00006000009e6ee0</span></span><br><span class="line">b address:  <span class="number">0x00006000009d5680</span></span><br><span class="line"><span class="built_in">c</span> address:  <span class="number">0x00006000009b1620</span></span><br><span class="line">a new address:  <span class="number">0x00006000009d8b00</span></span><br></pre></td></tr></table></figure></p>
<p>备注：书中说，<strong>如果 <code>c = a</code>，在 <code>c</code> 不改变时，这两个数组指向内存中同一个位置，这两个数组共享了它们的存储部分</strong>。但是我打印数组的指针地址和书中描述的是不一致的。不知道是否理解有误。</p>
<h4 id="写时复制（昂贵方式）"><a href="#写时复制（昂贵方式）" class="headerlink" title="写时复制（昂贵方式）"></a>写时复制（昂贵方式）</h4><p>即每次都创建一个私有备份，重新赋值</p>
<h4 id="写时复制（高效方式）"><a href="#写时复制（高效方式）" class="headerlink" title="写时复制（高效方式）"></a>写时复制（高效方式）</h4><p>主要通过系统 <code>API</code> <code>isKnownUniquelyReferenced(&lt;#T##object: &amp;T##T#&gt;)</code> 判断对象是否唯一引用的，是的话就原地修改。</p>
<h4 id="写时复制的陷阱"><a href="#写时复制的陷阱" class="headerlink" title="写时复制的陷阱"></a>写时复制的陷阱</h4><p>简单来说，除非必须，还是少用为好。理解原理即可。</p>
<h3 id="闭包和可变性"><a href="#闭包和可变性" class="headerlink" title="闭包和可变性"></a>闭包和可变性</h3><ol>
<li>函数（包括闭包）是引用类型。</li>
<li><code>Swift</code> 的结构体一般被储存在栈上，而非堆上。不过这其实是一种优化：默认情况下，结构体时存储在堆上的，但是绝大多数时候，这个优化会生效，并将结构体存储到栈上。当结构体被一个函数闭合时，优化将不再生效，此时这个结构体将存储在堆上。与此类似，如果结构体太大，它也会被存储在堆上。</li>
</ol>
<h3 id="内存"><a href="#内存" class="headerlink" title="内存"></a>内存</h3><p>值类型不用大新循环引用的问题，如下：<br><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Person</span> </span>&#123;</span><br><span class="line">    <span class="keyword">var</span> name: <span class="type">String</span></span><br><span class="line">    <span class="keyword">var</span> parents: [<span class="type">Person</span>]</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> john = <span class="type">Person</span>(name: <span class="string">"John"</span>, parents: [])</span><br><span class="line">john.parents = [john]</span><br><span class="line">john <span class="comment">// Person(name: "John", parents: [Person(name: "John", parents: [])])</span></span><br></pre></td></tr></table></figure></p>
<p>因为值类型始终是复制，持有至只要一个，所以不会循环引用。</p>
<h4 id="weak-引用-和-unowned-引用"><a href="#weak-引用-和-unowned-引用" class="headerlink" title="weak 引用 和 unowned 引用"></a><code>weak</code> 引用 和 <code>unowned</code> 引用</h4><ol>
<li><code>weak</code> 表示不增加引用计数，当引用对象被释放时，则<code>weak</code> 对象置为 <code>nil</code>,所以被 <code>weak</code> 修饰的对象都是可选值类型。<code>unowned</code> 需要确保被引用对象始终有效，本身不会主动置为 <code>nil</code>。</li>
<li><code>unowned</code> 容易造成僵尸对象，好处则是可以用 <code>let</code> 修饰。其实建议更多的使用<code>weak</code> ，强制我们自己去处理 <code>nil</code> 情况。</li>
</ol>
<h3 id="闭包和内存"><a href="#闭包和内存" class="headerlink" title="闭包和内存"></a>闭包和内存</h3><p>通过使用 <strong>捕获列表</strong> 将捕获变量标记为 <code>weak</code> 或者 <code>unowned</code> 来解决循环引用。</p>
]]></content>
  </entry>
  <entry>
    <title>Swift进阶之三：可选值</title>
    <url>/2019/11/23/Swift%E8%BF%9B%E9%98%B6%E4%B9%8B%E4%B8%89%EF%BC%9A%E5%8F%AF%E9%80%89%E5%80%BC/</url>
    <content><![CDATA[<h2 id="可选值"><a href="#可选值" class="headerlink" title="可选值"></a>可选值</h2><h3 id="哨岗值"><a href="#哨岗值" class="headerlink" title="哨岗值"></a>哨岗值</h3><ol>
<li>函数返回了一个 <code>魔法</code> 数值来表示函数并没有返回正确的值。这样的值被称为 <strong>哨岗值</strong>。</li>
</ol>
<a id="more"></a>
<ol start="2">
<li><p>下面举例，说明哨岗值存在的问题：</p>
 <figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="built_in">NSString</span> *someStr;</span><br><span class="line"><span class="keyword">if</span> ([someStr rangeOfString:<span class="string">@"swift"</span>].location != <span class="built_in">NSNotFound</span>) &#123;</span><br><span class="line">    <span class="built_in">NSLog</span>(<span class="string">@"mention swift"</span>);</span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="built_in">NSLog</span>(<span class="string">@"Not"</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p> 输出结果是 <code>mention swift</code>，因为 <code>NSNotFound</code> 定义为 <code>static const NSInteger NSNotFound = NSIntegerMax;</code>，而 <code>[someStr rangeOfString:@&quot;swift&quot;].location = 0</code>。</p>
 <!-- more -->
</li>
</ol>
<h3 id="通过枚举解决魔法数问题"><a href="#通过枚举解决魔法数问题" class="headerlink" title="通过枚举解决魔法数问题"></a>通过枚举解决魔法数问题</h3><p>简单来说，就是通过 <code>关联值</code> 解决放回错误值的情况。 <code>Swift</code> 中有更间断清晰的做法，即 <code>可选值</code>。</p>
<h3 id="可选值概览"><a href="#可选值概览" class="headerlink" title="可选值概览"></a>可选值概览</h3><h4 id="if-let"><a href="#if-let" class="headerlink" title="if let"></a><code>if let</code></h4><p>同一个 <code>if</code> 语句中可以绑定多个值。并且，后面绑定的值可以基于前面成功解包的值进行操作。如下：<br><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> urlStr = <span class="string">"http://www.objc.io/logo.png"</span></span><br><span class="line"><span class="keyword">if</span> <span class="keyword">let</span> url = <span class="type">URL</span>(string: urlStr),</span><br><span class="line">    <span class="keyword">let</span> data = <span class="keyword">try</span>? <span class="type">Data</span>(contentsOf: url),</span><br><span class="line">    <span class="keyword">let</span> image = <span class="type">UIImage</span>(data: data) &#123;</span><br><span class="line">l   et imageV = <span class="type">UIImageView</span>(image: image)</span><br><span class="line">    <span class="keyword">self</span>.view.addSubview(imageV)</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="built_in">print</span>(<span class="string">"图片获取失败"</span>)</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure></p>
<h4 id="while-let"><a href="#while-let" class="headerlink" title="while let"></a><code>while let</code></h4><p><code>while let</code> 与 <code>if let</code> 非常相似，它代表当遇到 <code>nil</code> 时终止的一个循环。</p>
<h4 id="双重可选值"><a href="#双重可选值" class="headerlink" title="双重可选值"></a>双重可选值</h4><p>即可选值可以嵌套在另一个可选值中。如下：<br><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> numStrs = [<span class="string">"1"</span>, <span class="string">"2"</span>, <span class="string">"three"</span>]</span><br><span class="line"><span class="keyword">let</span> nums = numStrs.<span class="built_in">map</span> &#123; <span class="type">Int</span>($<span class="number">0</span>) &#125;</span><br><span class="line">nums.forEach &#123; (n) <span class="keyword">in</span></span><br><span class="line">    <span class="built_in">print</span>(n)</span><br><span class="line">&#125;</span><br><span class="line">        </span><br><span class="line"><span class="comment">// 使用迭代器</span></span><br><span class="line"><span class="keyword">var</span> iterator = nums.makeIterator()</span><br><span class="line"><span class="keyword">while</span> <span class="keyword">let</span> num = iterator.next() &#123;</span><br><span class="line">    <span class="built_in">print</span>(num)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>其中， <code>next()</code> 方法即是双重可选值的体现。</p>
<h4 id="if-var-and-while-var"><a href="#if-var-and-while-var" class="headerlink" title="if var and while var"></a><code>if var and while var</code></h4><p>获取一个原始值的副本，但是新值的修改，对原始值没有影响。如下：<br><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> name: <span class="type">String</span>?</span><br><span class="line"><span class="keyword">if</span> <span class="keyword">var</span> newName = name &#123;</span><br><span class="line">  newName = <span class="string">"jack"</span></span><br><span class="line">  <span class="built_in">print</span>(name, newName)</span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">  <span class="built_in">print</span>(<span class="string">"name is nil"</span>)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> addr: <span class="type">String</span>? = <span class="string">"china"</span></span><br><span class="line"><span class="keyword">if</span> <span class="keyword">var</span> newAdd = addr &#123;</span><br><span class="line">  newAdd = <span class="string">"china beijing"</span></span><br><span class="line">  <span class="built_in">print</span>(addr, newAdd)</span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">  <span class="built_in">print</span>(<span class="string">"addr is nil"</span>)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">输出：</span><br><span class="line">name <span class="keyword">is</span> <span class="literal">nil</span></span><br><span class="line"><span class="type">Optional</span>(<span class="string">"china"</span>) china beijing</span><br></pre></td></tr></table></figure></p>
<h4 id="解包后可选值的作用域"><a href="#解包后可选值的作用域" class="headerlink" title="解包后可选值的作用域"></a>解包后可选值的作用域</h4><p>简单来说，<code>if</code> 定义的临时值，只用在 <code>if</code> 涵盖的大括号内。适当的使用 <code>guard</code> ，可以使代码更加精简易读。</p>
<h4 id="可选链"><a href="#可选链" class="headerlink" title="可选链"></a>可选链</h4><p>即 <code>chapters?.preChapter?.priceTip?.price</code> 这样的链式可选，其中任何一个为空，则返回 <code>nil</code>。</p>
<h4 id="nil-合并运算符"><a href="#nil-合并运算符" class="headerlink" title="nil 合并运算符"></a><code>nil</code> 合并运算符</h4><p>如果值为空时，用一个默认值来替代它：<br><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> price =  chapters?.preChapter?.priceTip?.price ?? <span class="number">0</span></span><br></pre></td></tr></table></figure></p>
<h4 id="可选值-map"><a href="#可选值-map" class="headerlink" title="可选值 map"></a>可选值 <code>map</code></h4><p><code>map</code>：接受一个闭包，如果可选值有内容，则调用这个闭包对其进行转换。如下：</p>
 <figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> numStrs = [<span class="string">"1"</span>, <span class="string">"2"</span>, <span class="string">"three"</span>]</span><br><span class="line"><span class="keyword">let</span> nums1 = numStrs.<span class="built_in">map</span> &#123; <span class="type">Int</span>($<span class="number">0</span>) &#125;</span><br><span class="line"></span><br><span class="line">nums1.forEach &#123; (n) <span class="keyword">in</span></span><br><span class="line">    <span class="built_in">print</span>(n)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">输出：</span><br><span class="line"><span class="type">Optional</span>(<span class="number">1</span>)</span><br><span class="line"><span class="type">Optional</span>(<span class="number">2</span>)</span><br><span class="line"><span class="literal">nil</span></span><br></pre></td></tr></table></figure>
<h4 id="可选值-compactMap"><a href="#可选值-compactMap" class="headerlink" title="可选值 compactMap"></a>可选值 <code>compactMap</code></h4><p><code>compactMap</code> 即 <code>flatMap</code>，已在最新版 <code>swift</code> 中更改命名。</p>
<p><code>compactMap</code> 和 <code>if let</code> 相似，都是对一个可选值进行展开，如下：<br><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> urlStr = <span class="string">"http://www.objc.io/logo.png"</span></span><br><span class="line"><span class="keyword">let</span> imageV = <span class="type">URL</span>(string: urlStr)</span><br><span class="line">    .flatMap &#123; <span class="keyword">try</span>? <span class="type">Data</span>(contentsOf: $<span class="number">0</span>) &#125;</span><br><span class="line">    .flatMap &#123; <span class="type">UIImage</span>(data: $<span class="number">0</span>) &#125;</span><br><span class="line">    .<span class="built_in">map</span> &#123; <span class="type">UIImageView</span>(image: $<span class="number">0</span>) &#125;</span><br><span class="line"><span class="keyword">if</span> <span class="keyword">let</span> imageView = imageV &#123;</span><br><span class="line">    <span class="keyword">self</span>.view.addSubview(imageView)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<h5 id="使用-compactMap-过滤-nil"><a href="#使用-compactMap-过滤-nil" class="headerlink" title="使用 compactMap 过滤 nil"></a>使用 <code>compactMap</code> 过滤 <code>nil</code></h5><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> numStrs = [<span class="string">"1"</span>, <span class="string">"2"</span>, <span class="string">"three"</span>]</span><br><span class="line"><span class="keyword">let</span> nums2 = numStrs.compactMap &#123; <span class="type">Int</span>($<span class="number">0</span>) &#125;</span><br><span class="line">nums2.forEach &#123; (n) <span class="keyword">in</span></span><br><span class="line">    <span class="built_in">print</span>(n)</span><br><span class="line">&#125;</span><br><span class="line">输出：</span><br><span class="line"><span class="number">1</span></span><br><span class="line"><span class="number">2</span></span><br></pre></td></tr></table></figure>
<h4 id="可选值判等"><a href="#可选值判等" class="headerlink" title="可选值判等"></a>可选值判等</h4><ol>
<li>当我们使用一个非可选值的时候，如果需要匹配可选值内容，则 <code>Swift</code> 总会将它 <strong>升级</strong> 为一个可选值然后使用。这个隐式转换对于写出清晰紧凑的代码非常有用。</li>
<li><p>使用下标为一个字典赋值 <code>nil</code>，实际上是将这个键从字典中移除。如下：</p>
 <figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> dictWithNils: [<span class="type">String</span>: <span class="type">Int</span>?] = [</span><br><span class="line"><span class="string">"one"</span>: <span class="number">1</span>,</span><br><span class="line"><span class="string">"two"</span>: <span class="number">2</span>,</span><br><span class="line"><span class="string">"three"</span>: <span class="literal">nil</span>]</span><br><span class="line">dictWithNils[<span class="string">"two"</span>] = <span class="literal">nil</span></span><br><span class="line"><span class="built_in">print</span>(dictWithNils)</span><br><span class="line">输出：</span><br><span class="line">[<span class="string">"three"</span>: <span class="literal">nil</span>, <span class="string">"one"</span>: <span class="type">Optional</span>(<span class="number">1</span>)]</span><br></pre></td></tr></table></figure>
<p> 可使用下面仁义方式：</p>
 <figure class="highlight swift"><table><tr><td class="code"><pre><span class="line">dictWithNils[<span class="string">"two"</span>]? = <span class="literal">nil</span></span><br><span class="line"><span class="comment">// 或者 </span></span><br><span class="line">dictWithNils[<span class="string">"two"</span>] = .some(<span class="literal">nil</span>)</span><br></pre></td></tr></table></figure>
</li>
</ol>
<pre><code>另外，书中 `dictWithNils[&quot;two&quot;] = Optional(nil)` ，经测试，依然会移除这个键。
</code></pre><h4 id="switch-case-匹配可选值"><a href="#switch-case-匹配可选值" class="headerlink" title="switch-case 匹配可选值"></a><code>switch-case</code> 匹配可选值</h4><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> [<span class="string">"2"</span>, <span class="string">"foo"</span>, <span class="string">"42"</span>, <span class="string">"100"</span>] &#123;</span><br><span class="line">    <span class="keyword">if</span> <span class="keyword">let</span> num = <span class="type">Int</span>(i) &#123;</span><br><span class="line">        <span class="keyword">switch</span> num &#123;</span><br><span class="line">        <span class="keyword">case</span> <span class="number">42</span>:</span><br><span class="line">            <span class="built_in">print</span>(<span class="string">"The meaning of life"</span>)</span><br><span class="line">        <span class="keyword">case</span> <span class="number">0</span> ..&lt; <span class="number">10</span>:</span><br><span class="line">            <span class="built_in">print</span>(<span class="string">"A single digit"</span>)</span><br><span class="line">        <span class="keyword">case</span> <span class="literal">nil</span>:</span><br><span class="line">            <span class="built_in">print</span>(<span class="string">"Not a number"</span>)</span><br><span class="line">        <span class="keyword">default</span>:</span><br><span class="line">            <span class="built_in">print</span>(<span class="string">"A mystery number"</span>)</span><br><span class="line">        &#125;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="built_in">print</span>(<span class="string">"Not a number"</span>)</span><br><span class="line">    &#125;            </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="强制解包的时机"><a href="#强制解包的时机" class="headerlink" title="强制解包的时机"></a>强制解包的时机</h3><blockquote>
<p>当确定某个值不可能是 <code>nil</code> 时，可以使用叹号。我们应当希望，如果它不巧意外的是 <code>nil</code>，那么程序会直接的挂掉</p>
</blockquote>
<h4 id="改进强制解包的错误信息"><a href="#改进强制解包的错误信息" class="headerlink" title="改进强制解包的错误信息"></a>改进强制解包的错误信息</h4><p>定义新的运算符。如下：</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">infix</span> <span class="keyword">operator</span> !!</span><br><span class="line"><span class="function"><span class="keyword">func</span> !!&lt;T&gt;<span class="params">(wrapped: T?, failureText: @autoclosure <span class="params">()</span></span></span> -&gt; <span class="type">String</span>) -&gt; <span class="type">T</span> &#123;</span><br><span class="line">     <span class="keyword">if</span> <span class="keyword">let</span> x = wrapped &#123; <span class="keyword">return</span> x &#125;</span><br><span class="line">     <span class="built_in">fatalError</span>(failureText())</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>使用：<br><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> s = <span class="string">"foo"</span></span><br><span class="line"><span class="keyword">let</span> i = <span class="type">Int</span>(s) !! <span class="string">"Expecting integer, got <span class="subst">\(s)</span>"</span></span><br></pre></td></tr></table></figure></p>
<p>运行，崩溃，并打印信息：<br><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="type">Fatal</span> error: <span class="type">Expecting</span> integer, got foo</span><br></pre></td></tr></table></figure></p>
<h4 id="在调试版本中进行断言"><a href="#在调试版本中进行断言" class="headerlink" title="在调试版本中进行断言"></a>在调试版本中进行断言</h4><p>定义新的运算符。如下：<br><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">infix</span> <span class="keyword">operator</span> !?</span><br><span class="line"><span class="function"><span class="keyword">func</span> !?&lt;T: ExpressibleByIntegerLiteral&gt; <span class="params">(wrapped: T?, failureText: @autoclosure <span class="params">()</span></span></span> -&gt; <span class="type">String</span>) -&gt; <span class="type">T</span> &#123;</span><br><span class="line">    <span class="built_in">assert</span>(wrapped != <span class="literal">nil</span>, failureText())</span><br><span class="line">    <span class="keyword">return</span> wrapped ?? <span class="number">0</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<h3 id="多灾多难的隐式可选值"><a href="#多灾多难的隐式可选值" class="headerlink" title="多灾多难的隐式可选值"></a>多灾多难的隐式可选值</h3><p>目前使用隐式解包的原因有两个：</p>
<ol>
<li>暂时来说，可能还需要到 <code>Objective-C</code> 里调用哪些没有检查返回是否安全的代码。</li>
<li>因为一个值只有很短暂的为 <code>nil</code> ，在一段时间后，他就会不再是 <code>nil</code>。如 <code>Xcode</code> 中 <code>Interface Builder</code> 的使用方式。</li>
</ol>
]]></content>
  </entry>
  <entry>
    <title>Swift进阶之二：集合类型协议</title>
    <url>/2019/11/23/Swift%E8%BF%9B%E9%98%B6%E4%B9%8B%E4%BA%8C%EF%BC%9A%E9%9B%86%E5%90%88%E7%B1%BB%E5%9E%8B%E5%8D%8F%E8%AE%AE/</url>
    <content><![CDATA[<h2 id="集合类型协议"><a href="#集合类型协议" class="headerlink" title="集合类型协议"></a>集合类型协议</h2><p>讨论 <code>Sequence</code> 和 <code>Collection</code> 协议，它们构成了几何模型的基石。</p>
<a id="more"></a>
<h3 id="序列-Sequence"><a href="#序列-Sequence" class="headerlink" title="序列 Sequence"></a>序列 <code>Sequence</code></h3><p>满足 <code>Sequence</code> 协议的要求十分简单，只需提供一个返回迭代器 <code>Iterator</code> 的 <code>makeIterator()</code> 方法：</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">protocol</span> <span class="title">Sequence</span> </span>&#123;</span><br><span class="line">    <span class="comment">/// A type that provides the sequence's iteration interface and</span></span><br><span class="line">    <span class="comment">/// encapsulates its iteration state.</span></span><br><span class="line">    associatedtype <span class="type">Iterator</span> : <span class="type">IteratorProtocol</span></span><br><span class="line">    <span class="comment">/// Returns an iterator over the elements of this sequence.</span></span><br><span class="line">    <span class="function"><span class="keyword">func</span> <span class="title">makeIterator</span><span class="params">()</span></span> -&gt; <span class="type">Self</span>.<span class="type">Iterator</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="迭代器"><a href="#迭代器" class="headerlink" title="迭代器"></a>迭代器</h4><p>序列通过创建一个迭代器来对元素访问。迭代器每次产生一个序列的值，并且在遍历序列时对遍历状态进行管理。通常，除非我们需要实现自己自定义的序列类型，否则我们基本直接操作迭代器。迭代器遵循的协议 <code>IteratorProtocol</code>，只有一个 <code>next()</code> 方法，我们的 <code>for</code> 循环，其实就是基于迭代器实现的。</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">protocol</span> <span class="title">IteratorProtocol</span> </span>&#123;</span><br><span class="line">    associatedtype <span class="type">Element</span></span><br><span class="line">    <span class="keyword">mutating</span> <span class="function"><span class="keyword">func</span> <span class="title">next</span><span class="params">()</span></span> -&gt; <span class="type">Self</span>.<span class="type">Element</span>?</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>例如：<br><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> animals = [<span class="string">"Antelope"</span>, <span class="string">"Butterfly"</span>, <span class="string">"Camel"</span>, <span class="string">"Dolphin"</span>]</span><br><span class="line">   <span class="keyword">for</span> animal <span class="keyword">in</span> animals &#123;</span><br><span class="line">       <span class="built_in">print</span>(animal)</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="comment">// Prints "Antelope"</span></span><br><span class="line">   <span class="comment">// Prints "Butterfly"</span></span><br><span class="line">   <span class="comment">// Prints "Camel"</span></span><br><span class="line">   <span class="comment">// Prints "Dolphin"</span></span><br><span class="line">   <span class="keyword">var</span> animalIterator = animals.makeIterator()</span><br><span class="line">   <span class="keyword">while</span> <span class="keyword">let</span> animal = animalIterator.next() &#123;</span><br><span class="line">       <span class="built_in">print</span>(animal)</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="comment">// Prints "Antelope"</span></span><br><span class="line">   <span class="comment">// Prints "Butterfly"</span></span><br><span class="line">   <span class="comment">// Prints "Camel"</span></span><br><span class="line">   <span class="comment">// Prints "Dolphin"</span></span><br></pre></td></tr></table></figure></p>
<ul>
<li>遵守序列协议</li>
<li>迭代器和值语义</li>
<li>基于函数的迭代器和序列</li>
</ul>
<p>除了迭代器，还有 <code>无线序列</code>、<code>不稳定序列</code>、<code>序列和迭代器之间的关系</code>、<code>子序列</code> 等相关细节内容。这里暂时不做展开，一方面开发中极少用到，需要时在查阅即可。这里仅做只是扩展。</p>
<h3 id="集合类型"><a href="#集合类型" class="headerlink" title="集合类型"></a>集合类型</h3><p>集合类型 <code>Collection</code> 是指稳定的序列，能够遍历多次而保持一致。集合类型不能是无限的。<strong>序列和集合一个重要区别，就是序列可以是无限的， 而集合则不行</strong>。<code>Collection</code> 协议是建立在 <code>Sequence</code> 协议上的。</p>
<h2 id="简单总结"><a href="#简单总结" class="headerlink" title="简单总结"></a>简单总结</h2><p>这一章主要是基于最基本的两个协议，定义自己的了类型，其实开发中用到的并不多。目前还是以准备面试、扩展知识面为主，这章暂时搁置吧。。。</p>
]]></content>
  </entry>
  <entry>
    <title>Swift进阶之一：内建集合类型</title>
    <url>/2019/11/23/Swift%E8%BF%9B%E9%98%B6%E4%B9%8B%E4%B8%80%EF%BC%9A%E5%86%85%E5%BB%BA%E9%9B%86%E5%90%88%E7%B1%BB%E5%9E%8B/</url>
    <content><![CDATA[<h2 id="数组"><a href="#数组" class="headerlink" title="数组"></a>数组</h2><h3 id="数组和可变性"><a href="#数组和可变性" class="headerlink" title="数组和可变性"></a>数组和可变性</h3><ol>
<li>数组 <code>Array</code> 是值类型。 <code>Foundation</code> 下的 <code>NSArray</code> 是引用类型。如：</li>
</ol>
<a id="more"></a>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> a = <span class="type">NSMutableArray</span>(array: [<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>])</span><br><span class="line"><span class="comment">// 尽管我们不想让 b 改变</span></span><br><span class="line"><span class="keyword">let</span> b = a</span><br><span class="line">a.insert(<span class="number">5</span>, at: <span class="number">0</span>)</span><br><span class="line"><span class="comment">// 可是因为 NSArray 的引用特性，b 依然变了</span></span><br><span class="line"><span class="built_in">print</span>(b)    <span class="comment">// 5 1 2 3</span></span><br><span class="line"><span class="comment">// 如果真的就是用 NSArray，还不想让改变</span></span><br><span class="line"><span class="keyword">let</span> <span class="built_in">c</span> = a.copy() <span class="keyword">as</span>! <span class="type">NSArray</span></span><br><span class="line">a.insert(<span class="number">10</span>, at: <span class="number">0</span>)</span><br><span class="line"><span class="built_in">print</span>(<span class="built_in">c</span>)    <span class="comment">// 5 1 2 3</span></span><br></pre></td></tr></table></figure>
<ol start="2">
<li><code>Array</code> 是值复制，但是用了 <code>写时复制</code> 的技术，在被拷贝的数据修改前，他们共享内存。之后会有 <code>写时拷贝</code> 的更多说明。</li>
</ol>
<!-- more -->
<h3 id="数组和可选值"><a href="#数组和可选值" class="headerlink" title="数组和可选值"></a>数组和可选值</h3><ul>
<li>想要寻找一个指定元素的位置？ <code>if let index = array.index { someMatchingLogic($0) }</code></li>
<li>想要筛选出某个符合标准的元素集合？ <code>array.filter { soneCCriteria($0) }</code></li>
</ul>
<h3 id="数组变形"><a href="#数组变形" class="headerlink" title="数组变形"></a>数组变形</h3><h4 id="Map"><a href="#Map" class="headerlink" title="Map"></a><code>Map</code></h4><ol>
<li><p><code>Map</code> 来自函数式编程的世界，使用方法如下：</p>
 <figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> fibs = [<span class="number">0</span>, <span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">5</span>]</span><br><span class="line"><span class="keyword">let</span> squares = fibs.<span class="built_in">map</span> &#123; $<span class="number">0</span> * $<span class="number">0</span> &#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p><code>Map</code> 的函数签名： <code>@inlinable public func map&lt;T&gt;(_ transform: (Element) throws -&gt; T) rethrows -&gt; [T]</code></p>
</li>
</ol>
<h4 id="使用函数将行为参数化"><a href="#使用函数将行为参数化" class="headerlink" title="使用函数将行为参数化"></a>使用函数将行为参数化</h4><p>简单来说，就是接收一个函数，作为参数来处理具体事务，简化对外逻辑。例如：</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">extension</span> <span class="title">Sequence</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">func</span> <span class="title">last</span><span class="params">(<span class="keyword">where</span> predicate: <span class="params">(Iterator.Element)</span></span></span> -&gt; <span class="type">Bool</span>) -&gt; <span class="type">Iterator</span>.<span class="type">Element</span>? &#123;</span><br><span class="line">        <span class="keyword">for</span> element <span class="keyword">in</span> reversed() <span class="keyword">where</span> predicate(element) &#123;</span><br><span class="line">            <span class="keyword">return</span> element</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">nil</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="Filter"><a href="#Filter" class="headerlink" title="Filter"></a><code>Filter</code></h4><ol>
<li><code>filter</code> 应该只用于，需要知道数组所有结果的时候，因为 <code>filter</code> 回创建一个全新的数组。</li>
<li>如果仅仅是查询是否包含，用 <code>contains</code> 效率高得多。</li>
</ol>
<h4 id="Reduce"><a href="#Reduce" class="headerlink" title="Reduce"></a><code>Reduce</code></h4><p><code>reduce</code> 用于对数组中所有元素做相加操作，可以使字符、数字、集合等，功能很强大， 这里只举个简单的例子吧：<br><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> fibs = [<span class="number">0</span>, <span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">5</span>]</span><br><span class="line"><span class="keyword">let</span> new = fibs.<span class="built_in">reduce</span>(<span class="number">2</span>, +)</span><br><span class="line"><span class="comment">// new = 13</span></span><br></pre></td></tr></table></figure></p>
<h4 id="flatMap"><a href="#flatMap" class="headerlink" title="flatMap"></a><code>flatMap</code></h4><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="meta">@available</span>(swift, deprecated: <span class="number">4.1</span>, renamed: <span class="string">"compactMap(_:)"</span>, message: <span class="string">"Please use compactMap(_:) for the case where closure returns an optional value"</span>)</span><br><span class="line">   <span class="keyword">public</span> <span class="function"><span class="keyword">func</span> <span class="title">flatMap</span>&lt;ElementOfResult&gt;<span class="params">(<span class="number">_</span> transform: <span class="params">(Element)</span></span></span> <span class="keyword">throws</span> -&gt; <span class="type">ElementOfResult</span>?) <span class="keyword">rethrows</span> -&gt; [<span class="type">ElementOfResult</span>]</span><br></pre></td></tr></table></figure>
<p><code>flatMap</code> 在 <code>Swift4.1</code> 时被取消了。替代方法，官方也举了个简单的例子：</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> possibleNumbers = [<span class="string">"1"</span>, <span class="string">"2"</span>, <span class="string">"three"</span>, <span class="string">"///4///"</span>, <span class="string">"5"</span>]</span><br><span class="line"><span class="keyword">let</span> mapped: [<span class="type">Int</span>?] = possibleNumbers.<span class="built_in">map</span> &#123; str <span class="keyword">in</span> <span class="type">Int</span>(str) &#125;</span><br><span class="line"><span class="comment">// [1, 2, nil, nil, 5]</span></span><br><span class="line"><span class="keyword">let</span> compactMapped: [<span class="type">Int</span>] = possibleNumbers.compactMap &#123; str <span class="keyword">in</span> <span class="type">Int</span>(str) &#125;</span><br><span class="line"><span class="comment">// [1, 2, 5]</span></span><br></pre></td></tr></table></figure>
<h4 id="forEach"><a href="#forEach" class="headerlink" title="forEach"></a><code>forEach</code></h4><p>使用 <code>forEach</code> 进行迭代要特别注意一点，就是使用 <code>return</code> 语句仅仅是从闭包返回，不会终止循环，譬如：<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">(1 ..&lt; 10).forEach &#123; (num) in</span><br><span class="line">    print(num)</span><br><span class="line">    if num &gt; 2 &#123; return &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>会打印出所有数字，关于 <code>forEach</code>，官方方法定义中有如下描述</p>
<blockquote>
<ol>
<li>You cannot use a <code>break</code> or <code>continue</code> statement to exit the current call of the <code>body</code> closure or skip subsequent calls.</li>
<li>Using the <code>return</code> statement in the <code>body</code> closure will exit only from the current call to <code>body</code>, not from any outer scope, and won’t skip subsequent calls.</li>
</ol>
</blockquote>
<h3 id="数组类型"><a href="#数组类型" class="headerlink" title="数组类型"></a>数组类型</h3><h5 id="切片"><a href="#切片" class="headerlink" title="切片"></a>切片</h5><p>切片类型只是数组的一中表示形式，它背后的数据类型仍然是原来的数组。对于庞大的数组，切片可以理解为对数组的片段预览，并不是重新生成一份内存。具体可看对 <code>ArraySlice</code> 的官方注释：</p>
<blockquote>
<p>The <code>ArraySlice</code> type makes it fast and efficient for you to perform operations on sections of a larger array. Instead of copying over the elements of a slice to new storage, an <code>ArraySlice</code> instance presents a view onto the storage of a larger array. And because <code>ArraySlice</code>  presents the same interface as <code>Array</code>, you can generally perform the</p>
</blockquote>
<h2 id="字典"><a href="#字典" class="headerlink" title="字典"></a>字典</h2><h3 id="可变性"><a href="#可变性" class="headerlink" title="可变性"></a>可变性</h3><p>记着一点即可，字典是值类型。</p>
<h3 id="有用的字典扩展"><a href="#有用的字典扩展" class="headerlink" title="有用的字典扩展"></a>有用的字典扩展</h3><ol>
<li><p>合并两个字典，相同 <code>key</code> 则更新 <code>value</code>:</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">extension</span> <span class="title">Dictionary</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 合并数组</span></span><br><span class="line">    <span class="keyword">mutating</span> <span class="function"><span class="keyword">func</span> <span class="title">merge</span>&lt;S&gt;<span class="params">(<span class="number">_</span> other: S)</span></span> <span class="keyword">where</span> <span class="type">S</span>: <span class="type">Sequence</span>, <span class="type">S</span>.<span class="type">Iterator</span>.<span class="type">Element</span> == (key: <span class="type">Key</span>, value: <span class="type">Value</span>) &#123;</span><br><span class="line">        <span class="keyword">for</span> (k, v) <span class="keyword">in</span> other &#123;</span><br><span class="line">            <span class="keyword">self</span>[k] = v;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>从一个 <code>(key, value)</code> 键值对序列来创建字典：</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">extension</span> <span class="title">Dictionary</span> </span>&#123;</span><br><span class="line">    <span class="comment">/// 合并数组</span></span><br><span class="line">    <span class="keyword">mutating</span> <span class="function"><span class="keyword">func</span> <span class="title">merge</span>&lt;S&gt;<span class="params">(<span class="number">_</span> other: S)</span></span> <span class="keyword">where</span> <span class="type">S</span>: <span class="type">Sequence</span>, <span class="type">S</span>.<span class="type">Iterator</span>.<span class="type">Element</span> == (key: <span class="type">Key</span>, value: <span class="type">Value</span>) &#123;</span><br><span class="line">        <span class="keyword">for</span> (k, v) <span class="keyword">in</span> other &#123;</span><br><span class="line">            <span class="keyword">self</span>[k] = v;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">/// 从一个 `(key, value)` 键值对序列来创建字典</span></span><br><span class="line">    <span class="keyword">init</span>&lt;<span class="type">S</span>: <span class="type">Sequence</span>&gt;(<span class="number">_</span> sequence: <span class="type">S</span>) <span class="keyword">where</span> <span class="type">S</span>.<span class="type">Iterator</span>.<span class="type">Element</span> == (key: <span class="type">Key</span>, value: <span class="type">Value</span>) &#123;</span><br><span class="line">        <span class="keyword">self</span> = [:]</span><br><span class="line">        <span class="keyword">self</span>.merge(sequence)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>添加 <code>map</code> 转换</p>
</li>
</ol>
<p><strong>重点</strong>：</p>
<p>以上方法， 现在均已加入 <code>Dictionary</code> 标准库。。。并且还更新了很多快捷的系统方法。</p>
<h2 id="Set"><a href="#Set" class="headerlink" title="Set"></a><code>Set</code></h2><p>集合 <code>Set</code> 是一组无序的元素集合。每个元素仅出现一次。和 <code>Dictionary</code> 一样，<code>Set</code> 也是通过哈希表实现的。</p>
<h3 id="集合代数"><a href="#集合代数" class="headerlink" title="集合代数"></a>集合代数</h3><p>集合 <code>Set</code> 和数学上的集合有着密切的关系，可以求两个集合的交集、并集等。</p>
<h3 id="在闭包中使用集合"><a href="#在闭包中使用集合" class="headerlink" title="在闭包中使用集合"></a>在闭包中使用集合</h3><p>获取一个序列中的所有唯一元素：</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">extension</span> <span class="title">Sequence</span> <span class="title">where</span> <span class="title">Iterator</span>.<span class="title">Element</span>: <span class="title">Hashable</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">func</span> <span class="title">unique</span><span class="params">()</span></span> -&gt; [<span class="type">Iterator</span>.<span class="type">Element</span>] &#123;</span><br><span class="line">        <span class="keyword">var</span> seen: <span class="type">Set</span>&lt;<span class="type">Iterator</span>.<span class="type">Element</span>&gt; = []</span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">filter</span> &#123;</span><br><span class="line">            <span class="keyword">if</span> seen.<span class="built_in">contains</span>($<span class="number">0</span>) &#123;</span><br><span class="line">                <span class="keyword">return</span> <span class="literal">false</span></span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                seen.insert($<span class="number">0</span>)</span><br><span class="line">                <span class="keyword">return</span> <span class="literal">true</span></span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">print</span>([<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">5</span>,<span class="number">21</span>,<span class="number">12</span>,<span class="number">5</span>,<span class="number">3</span>,<span class="number">2</span>].unique())  <span class="comment">// [1, 2, 3, 5, 21, 12]</span></span><br></pre></td></tr></table></figure>
<h2 id="Range"><a href="#Range" class="headerlink" title="Range"></a><code>Range</code></h2><p>在标准库中，目前有四种范围类型：</p>
<table>
<thead>
<tr>
<th style="text-align:left">描述</th>
<th>半开范围</th>
<th>闭合范围</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left">元素满足 <code>Comparable</code></td>
<td><code>Range</code></td>
<td><code>CloseRange</code></td>
</tr>
<tr>
<td style="text-align:left">元素满足 <code>Strideable</code> (以整数为步长)</td>
<td><code>CountableRange</code></td>
<td><code>CountableCloseRange</code></td>
</tr>
</tbody>
</table>
<ul>
<li>只有 <strong>半开范围</strong> 能表达 <strong>空区间</strong> 的概念。（当范围的上下边界相等时，比如 <code>5 ..&lt; 5</code>）。</li>
<li>只有 <strong>闭合范围</strong> 能够包含它的元素类型所能表达的最大值。半开范围总是至少会有一个值比范围所表达的值更大。</li>
</ul>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>希望这几天能坚持把这本书分章梳理完，更多的扩展 <code>Swift</code> 知识面，加深印象，对于特别细的知识点，这里不做展开。</p>
]]></content>
  </entry>
  <entry>
    <title>Runloop分析</title>
    <url>/2019/11/23/Runloop%E5%88%86%E6%9E%90/</url>
    <content><![CDATA[<h2 id="RunLoop-参考：深入理解RunLoop"><a href="#RunLoop-参考：深入理解RunLoop" class="headerlink" title="RunLoop 参考：深入理解RunLoop"></a>RunLoop 参考：深入理解RunLoop</h2><p><a href="https://blog.ibireme.com/2015/05/18/runloop/" target="_blank" rel="noopener">ibireme：《深入理解RunLoop》</a></p>
<a id="more"></a>
<h3 id="Runloop-的概念"><a href="#Runloop-的概念" class="headerlink" title="Runloop 的概念"></a>Runloop 的概念</h3><p>首先，让一个线程随时能处理事件，但是并不退出，这样额模型通常称作 <code>Event Loop</code>，如下：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">funcation loop() &#123;</span><br><span class="line">    initialize();</span><br><span class="line">    <span class="keyword">do</span> &#123;</span><br><span class="line">        var message = get_next_message();</span><br><span class="line">        process_message(message);</span><br><span class="line">    &#125; <span class="keyword">while</span> (message != quit);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>实现这种模型的关键点在于：如何管理事件/消息，如何让线程在没有消息处理时休眠以避免资源浪费，在消息到来时立刻被唤醒。</p>
<p><code>Runloop</code> 实际上就是一个对象，这个对象管理了其所需要处理的事件和消息，并提供一个入口函数来执行上面的 <code>Event Loop</code> 逻辑。线程执行了这个函数后，就会一直处于喊个函数内部 “接收消息-&gt;等待-&gt;处理”的循环中，直到这个循环结束（如返回 <code>quit</code> 消息），函数返回。</p>
<p><code>iOS/MacOS</code> 提供了两个这样的对象：<code>NSRunLoop</code> 和 <code>CFRunLoopRef</code>。</p>
<ul>
<li><code>CFRunLoopRef</code> 是在 <code>CoreFoundation</code> 框架内的，它提供了纯 <code>C</code> 的 <code>API</code>，所有这些 <code>API</code> 都是线程安全的。</li>
<li><code>NSRunLoop</code> 是基于 <code>CFRunLoopRef</code> 的封装，提供了面向对象的 <code>API</code>，但这些 <code>API</code> 不是线程安全的。</li>
</ul>
<h3 id="RunLoop-与线程的关系"><a href="#RunLoop-与线程的关系" class="headerlink" title="RunLoop 与线程的关系"></a>RunLoop 与线程的关系</h3><ol>
<li><code>RunLoop</code> 是通过 <code>p_thread</code> 管理的。苹果不允许直接创建 <code>RunLoop</code>，它提供了两个自动获取的方法：<code>CFRunLoopGetCurrent()</code> 和 <code>CFRunLoopGetMain()</code>。</li>
<li>线程和 <code>RunLoop</code> 是一一对应的，其关系保存在一个全局的 <code>Dictionary</code> 里。线程刚创建时是没有 <code>RunLoop</code> 的，如果不主动获取，那它就会一直没有。<code>RunLoop</code> 的创建是在第一次获取时，销毁发生在线程结束时。</li>
</ol>
<h3 id="RunLoop-对外的接口"><a href="#RunLoop-对外的接口" class="headerlink" title="RunLoop 对外的接口"></a>RunLoop 对外的接口</h3><p>在 <code>CoreFoundation</code> 里面关于 <code>RunLoop</code> 有五个类：</p>
<ul>
<li>CFRunLoopRef</li>
<li>CFRunLoopSourceRef</li>
<li>CFRunLoopObserverRef</li>
<li>CFRunLoopTimerRef</li>
<li>CFRunLoopModeRef</li>
</ul>
<p>其中， <code>CFRunLoopModeRef</code> 类没有对外暴露，只是通过 <code>CFRunLoopRef</code> 的接口进行了封装，他们关系如下：</p>
<p><img src="https://i.loli.net/2019/11/21/mIRNLsuBScf4O2J.png" alt="RunLoop_0.png"></p>
<p>一个 <code>RunLoop</code> 包含若干 <code>Mode</code>，每个 <code>Mode</code> 又包含若干 <code>Source/Timer/Observer</code>。每次调用 <code>RunLoop</code> 的主函数时，只能指定其中一个 <code>Mode</code>，这个 <code>Mode</code> 被称为 <code>CurrentMode</code>。如果需要切换 <code>Mode</code>，只能退出 <code>Loop</code>，再重新指定一个 Mode 进入。这样做主要是为了分割不同组的 <code>Source/Timer/Observer</code>，让其互不影响。这也是为啥 <code>ScrollView</code> 滑动时，默认 Mode 下计时器停止的原因。</p>
<p><code>Source/Timer/Observer</code> 被统称为 <code>mode item</code>，一个 <code>item</code> 内可以同时加入多个 <code>mode</code>。但一个 <code>item</code> 被重复加入同一个 <code>mode</code> 是不会有效果的。如果一个 <code>mode</code> 中一个 <code>item</code> 都没有，则 <code>RunLoop</code> 会直接退出，不进入循环。</p>
<h4 id="CFRunLoopSourceRef"><a href="#CFRunLoopSourceRef" class="headerlink" title="CFRunLoopSourceRef"></a>CFRunLoopSourceRef</h4><p>是事件产生的地方。 <code>Source</code> 有两个版本： <code>Source0</code> 和 <code>Source1</code>。</p>
<ul>
<li><code>Source0</code> 只包含一个回调（指针），它并不能主动触发事件。使用时需要先调用 <code>CFRunLoopSourceSignal(source)</code>，将这个 <code>source</code> 标记为待处理， 然后手动调用 <code>CFRunLoopWakeUp(runloop)</code> 来唤醒 <code>RunLoop</code> ，让其处理事件。</li>
<li><code>Source1</code> 包含了一个 <code>mach_port</code> 和一个回调（指针），被用于通过内核和其他线程相互发送消息。这种 <code>Source</code> 能主动唤醒 <code>RunLoop</code> 的线程。</li>
</ul>
<h4 id="CFRunLoopTimerRef"><a href="#CFRunLoopTimerRef" class="headerlink" title="CFRunLoopTimerRef"></a>CFRunLoopTimerRef</h4><p><code>CFRunLoopTimerRef</code> 是基于时间的触发器，它和 <code>NSTimer</code> 是 <code>toll-free bridged</code> 的，可以混用。其包含一个时间长度和回调（指针）。当其加入到 <code>RunLoop</code> 时，<code>RunLoop</code> 会注册对应的时间点，当时间点到时，<code>RunLoop</code> 被唤醒执行那个回调。</p>
<h4 id="CFRunLoopObserverRef"><a href="#CFRunLoopObserverRef" class="headerlink" title="CFRunLoopObserverRef"></a>CFRunLoopObserverRef</h4><p><code>CFRunLoopObserverRef</code> 是观察者。 每个 <code>Observer</code> 都包含了一个回调（指针），当 <code>RunLoop</code> 状态发生变化时，观察者就能通过回调接收到这个变化。可以观测的时间点有以下几个：</p>
<figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="built_in">CF_OPTIONS</span>(<span class="built_in">CFOptionFlags</span>, <span class="built_in">CFRunLoopActivity</span>) &#123;</span><br><span class="line">    kCFRunLoopEntry         = (<span class="number">1</span>UL &lt;&lt; <span class="number">0</span>), <span class="comment">// 即将进入Loop</span></span><br><span class="line">    kCFRunLoopBeforeTimers  = (<span class="number">1</span>UL &lt;&lt; <span class="number">1</span>), <span class="comment">// 即将处理 Timer</span></span><br><span class="line">    kCFRunLoopBeforeSources = (<span class="number">1</span>UL &lt;&lt; <span class="number">2</span>), <span class="comment">// 即将处理 Source</span></span><br><span class="line">    kCFRunLoopBeforeWaiting = (<span class="number">1</span>UL &lt;&lt; <span class="number">5</span>), <span class="comment">// 即将进入休眠</span></span><br><span class="line">    kCFRunLoopAfterWaiting  = (<span class="number">1</span>UL &lt;&lt; <span class="number">6</span>), <span class="comment">// 刚从休眠中唤醒</span></span><br><span class="line">    kCFRunLoopExit          = (<span class="number">1</span>UL &lt;&lt; <span class="number">7</span>), <span class="comment">// 即将退出Loop</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<h3 id="RunLoop-的-Mode"><a href="#RunLoop-的-Mode" class="headerlink" title="RunLoop 的 Mode"></a>RunLoop 的 Mode</h3><p><code>CFRunLoopMode</code> 和 CFRunLoop` 的结构如下：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> __CFRunLoopMode &#123;</span><br><span class="line">    <span class="built_in">CFRuntimeBase</span> _base;</span><br><span class="line">    pthread_mutex_t _lock;	<span class="comment">/* must have the run loop locked before locking this */</span></span><br><span class="line">    <span class="built_in">CFStringRef</span> _name;</span><br><span class="line">    Boolean _stopped;</span><br><span class="line">    <span class="keyword">char</span> _padding[<span class="number">3</span>];</span><br><span class="line">    <span class="built_in">CFMutableSetRef</span> _sources0;</span><br><span class="line">    <span class="built_in">CFMutableSetRef</span> _sources1;</span><br><span class="line">    <span class="built_in">CFMutableArrayRef</span> _observers;</span><br><span class="line">    <span class="built_in">CFMutableArrayRef</span> _timers;</span><br><span class="line">    <span class="built_in">CFMutableDictionaryRef</span> _portToV1SourceMap;</span><br><span class="line">    __CFPortSet _portSet;</span><br><span class="line">    <span class="built_in">CFIndex</span> _observerMask;</span><br><span class="line"><span class="meta">#if USE_DISPATCH_SOURCE_FOR_TIMERS</span></span><br><span class="line">    dispatch_source_t _timerSource;</span><br><span class="line">    <span class="built_in">dispatch_queue_t</span> _queue;</span><br><span class="line">    Boolean _timerFired; <span class="comment">// set to true by the source when a timer has fired</span></span><br><span class="line">    Boolean _dispatchTimerArmed;</span><br><span class="line"><span class="meta">#endif</span></span><br><span class="line"><span class="meta">#if USE_MK_TIMER_TOO</span></span><br><span class="line">    mach_port_t _timerPort;</span><br><span class="line">    Boolean _mkTimerArmed;</span><br><span class="line"><span class="meta">#endif</span></span><br><span class="line"><span class="meta">#if DEPLOYMENT_TARGET_WINDOWS</span></span><br><span class="line">    DWORD _msgQMask;</span><br><span class="line">    <span class="keyword">void</span> (*_msgPump)(<span class="keyword">void</span>);</span><br><span class="line"><span class="meta">#endif</span></span><br><span class="line">    uint64_t _timerSoftDeadline; <span class="comment">/* TSR */</span></span><br><span class="line">    uint64_t _timerHardDeadline; <span class="comment">/* TSR */</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">struct</span> __CFRunLoop &#123;</span><br><span class="line">    <span class="built_in">CFRuntimeBase</span> _base;</span><br><span class="line">    pthread_mutex_t _lock;			<span class="comment">/* locked for accessing mode list */</span></span><br><span class="line">    __CFPort _wakeUpPort;			<span class="comment">// used for CFRunLoopWakeUp </span></span><br><span class="line">    Boolean _unused;</span><br><span class="line">    <span class="keyword">volatile</span> _per_run_data *_perRunData;              <span class="comment">// reset for runs of the run loop</span></span><br><span class="line">    pthread_t _pthread;</span><br><span class="line">    uint32_t _winthread;</span><br><span class="line">    <span class="built_in">CFMutableSetRef</span> _commonModes;</span><br><span class="line">    <span class="built_in">CFMutableSetRef</span> _commonModeItems;</span><br><span class="line">    <span class="built_in">CFRunLoopModeRef</span> _currentMode;</span><br><span class="line">    <span class="built_in">CFMutableSetRef</span> _modes;</span><br><span class="line">    <span class="keyword">struct</span> _block_item *_blocks_head;</span><br><span class="line">    <span class="keyword">struct</span> _block_item *_blocks_tail;</span><br><span class="line">    <span class="built_in">CFTypeRef</span> _counterpart;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></p>
<p>筛选出比较关键的信息，大致如下：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> __CFRunLoopMode &#123;</span><br><span class="line">    <span class="built_in">CFStringRef</span> _name;            <span class="comment">// Mode Name, 例如 @"kCFRunLoopDefaultMode"</span></span><br><span class="line">    <span class="built_in">CFMutableSetRef</span> _sources0;    <span class="comment">// Set</span></span><br><span class="line">    <span class="built_in">CFMutableSetRef</span> _sources1;    <span class="comment">// Set</span></span><br><span class="line">    <span class="built_in">CFMutableArrayRef</span> _observers; <span class="comment">// Array</span></span><br><span class="line">    <span class="built_in">CFMutableArrayRef</span> _timers;    <span class="comment">// Array</span></span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">struct</span> __CFRunLoop &#123;</span><br><span class="line">    <span class="built_in">CFMutableSetRef</span> _commonModes;     <span class="comment">// Set</span></span><br><span class="line">    <span class="built_in">CFMutableSetRef</span> _commonModeItems; <span class="comment">// Set&lt;Source/Observer/Timer&gt;</span></span><br><span class="line">    <span class="built_in">CFRunLoopModeRef</span> _currentMode;    <span class="comment">// Current Runloop Mode</span></span><br><span class="line">    <span class="built_in">CFMutableSetRef</span> _modes;           <span class="comment">// Set</span></span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></p>
<ul>
<li>苹果公开提供的 Mode 有两个：kCFRunLoopDefaultMode (NSDefaultRunLoopMode) 和 UITrackingRunLoopMode，可以用这两个 Mode Name 来操作其对应的 Mode。</li>
<li>可以自定义 Mode。RunLoop 内部 Mode 只能增加，不能减少。</li>
</ul>
<h3 id="RunLoop-内部逻辑"><a href="#RunLoop-内部逻辑" class="headerlink" title="RunLoop 内部逻辑"></a>RunLoop 内部逻辑</h3><p>RunLoop 内部的逻辑大致如下：</p>
<p><img src="https://i.loli.net/2019/11/21/sKQS28HIniOJDpd.png" alt="RunLoop_1.png"></p>
<h3 id="苹果用-RunLoop-实现的功能"><a href="#苹果用-RunLoop-实现的功能" class="headerlink" title="苹果用 RunLoop 实现的功能"></a>苹果用 RunLoop 实现的功能</h3><p>APP启动时，系统默认注册了5个Mode:</p>
<ol>
<li>kCFRunLoopDefaultMode: App的默认 Mode，通常主线程是在这个 Mode 下运行的。</li>
<li>UITrackingRunLoopMode: 界面跟踪 Mode，用于 ScrollView 追踪触摸滑动，保证界面滑动时不受其他 Mode 影响。</li>
<li>UIInitializationRunLoopMode: 在刚启动 App 时第进入的第一个 Mode，启动完成后就不再使用。</li>
<li>GSEventReceiveRunLoopMode: 接受系统事件的内部 Mode，通常用不到。</li>
<li>kCFRunLoopCommonModes: 这是一个占位的 Mode，没有实际作用。</li>
</ol>
<p>可以在 <a href="http://iphonedevwiki.net/index.php/CFRunLoop" target="_blank" rel="noopener">这里</a> 看到更多的苹果内部的 Mode，但那些 Mode 在开发中就很难遇到了。</p>
<h2 id="RunLoop-参考：iOS线下分享《RunLoop》by-孙源-sunnyxx"><a href="#RunLoop-参考：iOS线下分享《RunLoop》by-孙源-sunnyxx" class="headerlink" title="RunLoop 参考：iOS线下分享《RunLoop》by 孙源@sunnyxx"></a>RunLoop 参考：iOS线下分享《RunLoop》by 孙源@sunnyxx</h2><p><a href="https://v.youku.com/v_show/id_XODgxODkzODI0.html" target="_blank" rel="noopener">iOS线下分享《RunLoop》by 孙源@sunnyxx</a></p>
<h3 id="RunLoop-机制"><a href="#RunLoop-机制" class="headerlink" title="RunLoop 机制"></a><code>RunLoop</code> 机制</h3><p>为什么要有 <code>RunLoop</code>:</p>
<ul>
<li>使程序一直运行并接受用户输入</li>
<li>决定程序在何时应该处理哪些 <code>Event</code></li>
<li>调用解耦（<code>Message Quene</code>）</li>
<li>节省 <code>CPU</code> 时间</li>
</ul>
<p><img src="https://i.loli.net/2019/11/20/KfWci8M9mUjIJEo.png" alt="RunLoop机制"></p>
<ul>
<li><code>RunLoop</code> 与线程（<code>Thread</code>）一一绑定并非说是一个 <code>Thread</code> 只能对应一个 <code>RunLoop</code>, 而是对应一个在外层的 <code>RunLoop</code>,<code>RunLoop</code> 可以嵌套使用。</li>
<li><code>1</code> 对 <code>n</code> 是通过数组结构实现的。</li>
</ul>
<h3 id="CFRunLoopTimer"><a href="#CFRunLoopTimer" class="headerlink" title="CFRunLoopTimer"></a><code>CFRunLoopTimer</code></h3><p>我们常用的 <code>Timer</code> 相关的，都是基于 <code>CFRunLoopTimer</code> 的封装，如：</p>
<figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">+ (<span class="built_in">NSTimer</span> *)timerWithTimeInterval:(<span class="built_in">NSTimeInterval</span>)ti </span><br><span class="line">invocation:(<span class="built_in">NSInvocation</span> *)invocation repeats:(<span class="built_in">BOOL</span>)yesOrNo;</span><br><span class="line">+ (<span class="built_in">NSTimer</span> *)scheduledTimerWithTimeInterval:(<span class="built_in">NSTimeInterval</span>)ti </span><br><span class="line">invocation:(<span class="built_in">NSInvocation</span> *)invocation repeats:(<span class="built_in">BOOL</span>)yesOrNo;</span><br></pre></td></tr></table></figure>
<p>或者延迟执行：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">- (<span class="keyword">void</span>)performSelector:(SEL)aSelector withObject:(<span class="keyword">nullable</span> <span class="keyword">id</span>)anArgument</span><br><span class="line">afterDelay:(<span class="built_in">NSTimeInterval</span>)delay inModes:(<span class="built_in">NSArray</span>&lt;<span class="built_in">NSRunLoopMode</span>&gt; *)modes;</span><br></pre></td></tr></table></figure></p>
<p>或者屏幕刷新频率 <code>CADisplayLink</code>：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">+ (<span class="built_in">CADisplayLink</span> *)displayLinkWithTarget:(<span class="keyword">id</span>)target selector:(SEL)sel;</span><br><span class="line">- (<span class="keyword">void</span>)addToRunLoop:(<span class="built_in">NSRunLoop</span> *)runloop forMode:(<span class="built_in">NSRunLoopMode</span>)mode;</span><br></pre></td></tr></table></figure></p>
<h3 id="CFRunLoopSource"><a href="#CFRunLoopSource" class="headerlink" title="CFRunLoopSource"></a><code>CFRunLoopSource</code></h3><ul>
<li><code>Source</code> 是 <code>RunLoop</code> 的数据抽象类（Protocol）</li>
<li><code>RunLoop</code> 定义了两个 <code>Version</code> 的 <code>Source</code>，可以从堆栈信息中查看:<ul>
<li><code>Source0</code>: 处理 <code>App</code> 内部事件、<code>App</code> 自己负责管理（触发），如 <code>UIEvent</code>，<code>CFSocket</code>等。</li>
<li><code>Source1</code>: 由 <code>RunLoop</code> 内核管理，<code>Mach Port</code> 驱动，如<code>CFMachPort</code>、<code>CFMessagePort</code> （都是官方文档说的。。。）</li>
<li>如果需要，可以选择一种来实现自己的 <code>Source</code>。（这事儿知道就行了）</li>
</ul>
</li>
</ul>
<h3 id="CFRunLoopObserver"><a href="#CFRunLoopObserver" class="headerlink" title="CFRunLoopObserver"></a><code>CFRunLoopObserver</code></h3><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* Run Loop Observer Activities */</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="built_in">CF_OPTIONS</span>(<span class="built_in">CFOptionFlags</span>, <span class="built_in">CFRunLoopActivity</span>) &#123;</span><br><span class="line">    kCFRunLoopEntry = (<span class="number">1</span>UL &lt;&lt; <span class="number">0</span>),</span><br><span class="line">    kCFRunLoopBeforeTimers = (<span class="number">1</span>UL &lt;&lt; <span class="number">1</span>),</span><br><span class="line">    kCFRunLoopBeforeSources = (<span class="number">1</span>UL &lt;&lt; <span class="number">2</span>),</span><br><span class="line">    kCFRunLoopBeforeWaiting = (<span class="number">1</span>UL &lt;&lt; <span class="number">5</span>),</span><br><span class="line">    kCFRunLoopAfterWaiting = (<span class="number">1</span>UL &lt;&lt; <span class="number">6</span>),</span><br><span class="line">    kCFRunLoopExit = (<span class="number">1</span>UL &lt;&lt; <span class="number">7</span>),</span><br><span class="line">    kCFRunLoopAllActivities = <span class="number">0x0FFFFFFF</span>U</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<ul>
<li>向外部报告当前 <code>RunLoop</code> 状态的更改。</li>
<li>框架中很多机制都由 <code>CFRunLoopObserver</code> 触发，如 <code>CAAnimation</code>。（其实这个也是猜测，并没有实质文档说明）</li>
</ul>
<h4 id="Topic-CFRunLoopObserver-与-Autorelease-Pool-关系"><a href="#Topic-CFRunLoopObserver-与-Autorelease-Pool-关系" class="headerlink" title="Topic: CFRunLoopObserver 与 Autorelease Pool 关系"></a><code>Topic</code>: <code>CFRunLoopObserver</code> 与 <code>Autorelease Pool</code> 关系</h4><blockquote>
<p><code>UIKit</code> 通过 <code>RunLoopObserver</code> 在 <code>RunLoop</code> 循环过程中，对 <code>Autorelease Pool</code> 进行 <code>Pop</code> 和 <code>Push</code> 操作，将这次 <code>Loop</code> 中产生的 <code>Autorelease</code> 对象释放。视频中作者测试，是在两次 <code>Sleep</code> 之间。</p>
</blockquote>
<h3 id="CFRunLoopMode"><a href="#CFRunLoopMode" class="headerlink" title="CFRunLoopMode"></a><code>CFRunLoopMode</code></h3><ul>
<li><code>RunLoop</code> 在同一段时间，只能，并且必须在一种特定的 <code>Mode</code> 下 <code>Run</code>。</li>
<li>更换 <code>Mode</code> 时，当前 <code>Loop</code> 会停止，然后重新启动 <code>Loop</code>。</li>
<li><code>Mode</code> 是 <code>iOS App</code> 滑动顺畅的关键。</li>
<li>可以定制自己的 <code>Mode</code>。</li>
</ul>
<ol>
<li><code>NSDefaultRunLoopMode</code>: 默认状态，越是空闲时的状态。</li>
<li><code>UITrackingRunLoopMode</code>: 滑动时的状态 <code>ScrollView</code>。</li>
<li><code>UIInitializationRunLoopMode</code>: 私有（不可见，堆栈追踪能看到，其他均为猜测）</li>
<li><code>NSRunLoopCommonModes</code>: 包含<code>NSDefaultRunLoopMode</code> 和 <code>UITrackingRunLoopMode</code> 两种状态。:</li>
</ol>
<h4 id="Topic-UITrackingRunLoopMode-与-Timer"><a href="#Topic-UITrackingRunLoopMode-与-Timer" class="headerlink" title="Topic: UITrackingRunLoopMode 与 Timer"></a><code>Topic</code>: <code>UITrackingRunLoopMode</code> 与 <code>Timer</code></h4><ol>
<li><p>这个方法，是将 <code>Timer</code> 加到默认的 <code>NSDefaultRunLoopMode</code> 模式下。</p>
<figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">[<span class="built_in">NSTimer</span> scheduledTimerWithTimeInterval:<span class="number">1</span></span><br><span class="line">                                 target:<span class="keyword">self</span></span><br><span class="line">                               selector:<span class="keyword">@selector</span>(timeCount)</span><br><span class="line">                               userInfo:<span class="literal">nil</span></span><br><span class="line">                                repeats:<span class="literal">YES</span>];</span><br></pre></td></tr></table></figure>
</li>
<li><p>如果存在 <code>ScrollView</code>，滑动时，<code>[NSRunLoop currentRunLoop].currentMode</code> 从 <code>NSDefaultRunLoopMode</code> 改变为 <code>UITrackingRunLoopMode</code>，此时，<code>Timer</code> 暂停，结束滑动后， <code>Timer</code> 继续。如果想要滑动时 <code>Timer</code> 正常运行，则将 <code>Timer</code> 添加到 <code>NSRunLoopCommonModes</code> 模式下即可，即：</p>
<figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">[[<span class="built_in">NSRunLoop</span> currentRunLoop] addTimer:<span class="keyword">self</span>.timer forMode:<span class="built_in">NSRunLoopCommonModes</span>];</span><br></pre></td></tr></table></figure>
</li>
</ol>
<h4 id="Topic-RunLoopMode-的切换"><a href="#Topic-RunLoopMode-的切换" class="headerlink" title="Topic: RunLoopMode 的切换"></a><code>Topic</code>: <code>RunLoopMode</code> 的切换</h4><ul>
<li>滑动前：<code>NSDefaultRunLoopMode</code></li>
<li>滑动中：<code>UITrackingRunLoopMode</code></li>
<li>滑动结束后：<code>NSDefaultRunLoopMode</code></li>
</ul>
<h4 id="RunLoop-与-GCD"><a href="#RunLoop-与-GCD" class="headerlink" title="RunLoop 与 GCD"></a><code>RunLoop</code> 与 <code>GCD</code></h4><blockquote>
<p>视频中这一块也是在讨论，并没有定论</p>
</blockquote>
<ol>
<li><code>GCD</code> 本身与 <code>RunLoop</code> 没有关系。</li>
<li><code>GCD</code> 中 <code>dispatch</code> 到 <code>main queue</code> 的 <code>block</code> 被分发到 <code>main runloop</code> 中执行，<code>dispatch_after</code> 同理。</li>
</ol>
<h3 id="Runloop-的等待与唤醒"><a href="#Runloop-的等待与唤醒" class="headerlink" title="Runloop 的等待与唤醒"></a>Runloop 的等待与唤醒</h3><ul>
<li>指定用户唤醒的 <code>mach_port</code> 端口。</li>
<li>调用 <code>mach_msg</code> 监听唤醒端口，被唤醒前，系统内核将这个线程挂起，停留在 <code>mach_msg_trap</code> 状态。</li>
<li>由另一个线程（或另一个进程中的某个线程）向内核发送这个端口的 <code>msg</code> 后，<code>trap</code> 状态被唤醒， <code>Runloop</code> 继续开始干活。</li>
</ul>
]]></content>
  </entry>
  <entry>
    <title>runtime：打印类属性、方法信息</title>
    <url>/2019/11/11/runtime%EF%BC%9A%E6%89%93%E5%8D%B0%E7%B1%BB%E5%B1%9E%E6%80%A7%E3%80%81%E6%96%B9%E6%B3%95%E4%BF%A1%E6%81%AF/</url>
    <content><![CDATA[<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>这篇文章专注打印一个类的方法，属性等信息。以及实践过程中遇到的问题的思考和探索。</p>
<a id="more"></a>
<h2 id="正文"><a href="#正文" class="headerlink" title="正文"></a>正文</h2><p>分条列举说明。</p>
<h3 id="方法、属性打印"><a href="#方法、属性打印" class="headerlink" title="方法、属性打印"></a>方法、属性打印</h3><p>测试类 <code>Person</code> 头文件定义：</p>
<figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="meta">#import <span class="meta-string">&lt;Foundation/Foundation.h&gt;</span></span></span><br><span class="line"><span class="meta">#include <span class="meta-string">&lt;objc/message.h&gt;</span></span></span><br><span class="line"><span class="built_in">NS_ASSUME_NONNULL_BEGIN</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">@protocol</span> <span class="title">HumanInfo</span> &lt;<span class="title">NSObject</span>&gt;</span></span><br><span class="line">-(<span class="keyword">void</span>)setSomeOneCountry;</span><br><span class="line"><span class="keyword">@end</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">@interface</span> <span class="title">Person</span> : <span class="title">NSObject</span>&lt;<span class="title">HumanInfo</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">/// 类属性</span></span><br><span class="line"><span class="keyword">@property</span>(<span class="keyword">nonatomic</span>, <span class="keyword">strong</span>, <span class="keyword">class</span>) <span class="built_in">NSString</span> *address;</span><br><span class="line"><span class="comment">/// 实例属性</span></span><br><span class="line"><span class="keyword">@property</span>(<span class="keyword">nonatomic</span>, <span class="keyword">strong</span>) <span class="built_in">NSString</span> *phone;</span><br><span class="line"></span><br><span class="line"><span class="comment">/// 类方法</span></span><br><span class="line">+(<span class="built_in">NSInteger</span>)howOldAreYou;</span><br><span class="line"><span class="comment">/// 实例方法</span></span><br><span class="line">-(<span class="keyword">void</span>)whatName:(<span class="built_in">NSString</span> * _Nullable)name;</span><br><span class="line">-(<span class="built_in">NSString</span> *_Nullable)fullName;</span><br><span class="line"></span><br><span class="line"><span class="comment">// MARK:- runtime</span></span><br><span class="line"><span class="comment">/// 获取协议方法</span></span><br><span class="line">-(<span class="keyword">void</span>)getProtocolMethods;</span><br><span class="line"><span class="comment">/// 属性的获取。并且动态修改属性信息</span></span><br><span class="line">-(<span class="keyword">void</span>)getIvarAndChange;</span><br><span class="line"><span class="comment">/// 获取实例方法</span></span><br><span class="line">-(<span class="keyword">void</span>)getInstanceMethod;</span><br><span class="line"><span class="comment">/// 获取类方法</span></span><br><span class="line">-(<span class="keyword">void</span>)getClassMethod;</span><br><span class="line"></span><br><span class="line"><span class="keyword">@end</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">NS_ASSUME_NONNULL_END</span></span><br></pre></td></tr></table></figure>
<h4 id="获取协议方法"><a href="#获取协议方法" class="headerlink" title="获取协议方法"></a>获取协议方法</h4><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="comment">/// 获取协议及协议方法</span></span><br><span class="line">- (<span class="keyword">void</span>)getProtocolMethods &#123;</span><br><span class="line">    <span class="keyword">unsigned</span> <span class="keyword">int</span> count;</span><br><span class="line">    <span class="comment">// 获取协议列表</span></span><br><span class="line">    __<span class="keyword">unsafe_unretained</span> Protocol **protocolList = class_copyProtocolList([<span class="keyword">self</span> <span class="keyword">class</span>], &amp;count);</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">unsigned</span> <span class="keyword">int</span> i = <span class="number">0</span>; i &lt; count; i++) &#123;</span><br><span class="line">        Protocol *pro = protocolList[i];</span><br><span class="line">        <span class="keyword">const</span> <span class="keyword">char</span> *protoclName = protocol_getName(pro);</span><br><span class="line">        <span class="built_in">NSLog</span>(<span class="string">@"协议名称：%@"</span>, [<span class="built_in">NSString</span> stringWithUTF8String:protoclName]);</span><br><span class="line">        <span class="comment">// 获取协议方法名</span></span><br><span class="line">        <span class="built_in">NSArray</span> *names = [<span class="keyword">self</span> methodListWithProtocol:pro];</span><br><span class="line">        <span class="keyword">for</span> (<span class="built_in">NSString</span> *name <span class="keyword">in</span> names) &#123;</span><br><span class="line">            <span class="built_in">NSLog</span>(<span class="string">@"%@ 协议方法： %@"</span>, [<span class="built_in">NSString</span> stringWithUTF8String:protoclName], name);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/// 获取协议方法</span></span><br><span class="line">-(<span class="built_in">NSArray</span>&lt;<span class="built_in">NSString</span> *&gt;*)methodListWithProtocol: (Protocol *)protocol &#123;</span><br><span class="line">    <span class="keyword">unsigned</span> <span class="keyword">int</span> count = <span class="number">0</span>;</span><br><span class="line">    <span class="built_in">NSMutableArray</span>&lt;<span class="built_in">NSString</span> *&gt; *methodList = @[].mutableCopy;</span><br><span class="line">    <span class="keyword">struct</span> objc_method_description *methods = protocol_copyMethodDescriptionList(protocol, <span class="literal">YES</span>, <span class="literal">YES</span>, &amp;count);</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">unsigned</span> <span class="keyword">int</span> i = <span class="number">0</span>; i &lt; count; i++) &#123;</span><br><span class="line">        <span class="keyword">struct</span> objc_method_description method = methods[i];</span><br><span class="line">        <span class="built_in">NSString</span> *name = <span class="built_in">NSStringFromSelector</span>(method.name);</span><br><span class="line">        [methodList addObject:name];</span><br><span class="line">    &#125;</span><br><span class="line">    free(methods);</span><br><span class="line">    <span class="keyword">return</span> methodList;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/// 协议方法</span></span><br><span class="line">- (<span class="keyword">void</span>)setSomeOneCountry &#123;</span><br><span class="line">    <span class="built_in">NSLog</span>(<span class="string">@"设置所属国家"</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>测试：</p>
<figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">Person *p = [[Person alloc] init];</span><br><span class="line">[p getProtocolMethods];</span><br></pre></td></tr></table></figure>
<p>结果：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">协议名称：HumanInfo</span><br><span class="line">HumanInfo 协议方法： setSomeOneCountry</span><br></pre></td></tr></table></figure></p>
<ul>
<li>输入 <code>protocol_</code> ，根据快捷提示，可以看到很多为协议专门准备的方法，包括添加属性、获取属性列表、方法列表等。</li>
<li><code>__unsafe_unretained Protocol **protocolList</code> 为什么要这么定义，通过查看 <code>class_copyProtocolList</code> 方法即可： <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">OBJC_EXPORT Protocol * __unsafe_unretained _Nonnull * _Nullable </span><br><span class="line">class_copyProtocolList(Class _Nullable cls, unsigned int * _Nullable outCount)</span><br><span class="line">OBJC_AVAILABLE(10.5, 2.0, 9.0, 1.0, 2.0);</span><br></pre></td></tr></table></figure>
</li>
</ul>
<h4 id="获取属性列表并动态更改属性值"><a href="#获取属性列表并动态更改属性值" class="headerlink" title="获取属性列表并动态更改属性值"></a>获取属性列表并动态更改属性值</h4><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="comment">/// 属性的获取。并且动态修改属性信息</span></span><br><span class="line">-(<span class="keyword">void</span>)getIvarAndChange &#123;</span><br><span class="line">    <span class="built_in">NSLog</span>(<span class="string">@"修改前： %@"</span>, <span class="keyword">self</span>.phone);</span><br><span class="line">    <span class="keyword">unsigned</span> <span class="keyword">int</span> count;</span><br><span class="line">    Ivar *vars = class_copyIvarList([<span class="keyword">self</span> <span class="keyword">class</span>], &amp;count);</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">unsigned</span> <span class="keyword">int</span> i = <span class="number">0</span>; i &lt; count; i++) &#123;</span><br><span class="line">        Ivar oneVar = vars[i];</span><br><span class="line">        <span class="comment">// 获取名字和类型</span></span><br><span class="line">        <span class="keyword">const</span> <span class="keyword">char</span> *memberName = ivar_getName(oneVar);</span><br><span class="line">        <span class="keyword">const</span> <span class="keyword">char</span> *memberType = ivar_getTypeEncoding(oneVar);</span><br><span class="line">        <span class="comment">// 一次打印属性名称和属性类型</span></span><br><span class="line">        <span class="built_in">NSLog</span>(<span class="string">@"属性信息  %s: %s"</span>, memberName, memberType);</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 修改属性</span></span><br><span class="line">        <span class="keyword">if</span> (strcmp(memberName, <span class="string">"phone"</span>) == <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="comment">// 修改前</span></span><br><span class="line">            <span class="built_in">NSString</span> *name = (<span class="built_in">NSString</span> *)object_getIvar(<span class="keyword">self</span>, oneVar);</span><br><span class="line">            <span class="built_in">NSLog</span>(<span class="string">@"开始修改手机属性  phone: %@"</span>, name);</span><br><span class="line">            <span class="comment">// 修改后</span></span><br><span class="line">            object_setIvar(<span class="keyword">self</span>, oneVar, <span class="string">@"telphoneNumber"</span>);</span><br><span class="line">            <span class="comment">// 修改后获取</span></span><br><span class="line">            <span class="built_in">NSString</span> *newName = (<span class="built_in">NSString</span> *)object_getIvar(<span class="keyword">self</span>, oneVar);</span><br><span class="line">            <span class="built_in">NSLog</span>(<span class="string">@"修改后的手机属性  %@"</span>, newName);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    free(vars);</span><br><span class="line">    <span class="built_in">NSLog</span>(<span class="string">@"方法列表获取结束，修改后属性信息 phone： %@"</span>, <span class="keyword">self</span>.phone);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>测试：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">[p getIvarAndChange];</span><br></pre></td></tr></table></figure></p>
<p>结果：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="number">2019</span><span class="number">-11</span><span class="number">-05</span> <span class="number">17</span>:<span class="number">34</span>:<span class="number">13.838345</span>+<span class="number">0800</span> CateAndExt[<span class="number">51413</span>:<span class="number">735475</span>] 修改前： (null)</span><br><span class="line"><span class="number">2019</span><span class="number">-11</span><span class="number">-05</span> <span class="number">17</span>:<span class="number">34</span>:<span class="number">13.838493</span>+<span class="number">0800</span> CateAndExt[<span class="number">51413</span>:<span class="number">735475</span>] 属性信息  phone: <span class="string">@"NSString"</span></span><br><span class="line"><span class="number">2019</span><span class="number">-11</span><span class="number">-05</span> <span class="number">17</span>:<span class="number">34</span>:<span class="number">13.838639</span>+<span class="number">0800</span> CateAndExt[<span class="number">51413</span>:<span class="number">735475</span>] 开始修改手机属性  phone: (null)</span><br><span class="line"><span class="number">2019</span><span class="number">-11</span><span class="number">-05</span> <span class="number">17</span>:<span class="number">34</span>:<span class="number">13.838763</span>+<span class="number">0800</span> CateAndExt[<span class="number">51413</span>:<span class="number">735475</span>] 修改后的手机属性  telphoneNumber</span><br><span class="line"><span class="number">2019</span><span class="number">-11</span><span class="number">-05</span> <span class="number">17</span>:<span class="number">34</span>:<span class="number">13.838888</span>+<span class="number">0800</span> CateAndExt[<span class="number">51413</span>:<span class="number">735475</span>] 属性信息  _delegate: <span class="string">@"&lt;HumanInfo&gt;"</span></span><br><span class="line"><span class="number">2019</span><span class="number">-11</span><span class="number">-05</span> <span class="number">17</span>:<span class="number">34</span>:<span class="number">13.838994</span>+<span class="number">0800</span> CateAndExt[<span class="number">51413</span>:<span class="number">735475</span>] 方法列表获取结束，修改后属性信息 phone： telphoneNumber</span><br></pre></td></tr></table></figure></p>
<ul>
<li>不能获取到类的类属性。</li>
<li><p>测试时建了 <code>Person</code> 的分类并添加属性，不能获取到分类中的属性信息。</p>
  <figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="meta">#import <span class="meta-string">"Person.h"</span></span></span><br><span class="line"></span><br><span class="line"><span class="built_in">NS_ASSUME_NONNULL_BEGIN</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">@interface</span> <span class="title">Person</span> (<span class="title">Man</span>)</span></span><br><span class="line"><span class="keyword">@property</span>(<span class="keyword">nonatomic</span>, <span class="keyword">strong</span>) <span class="built_in">NSString</span> *nickName;</span><br><span class="line"></span><br><span class="line"><span class="keyword">@end</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">NS_ASSUME_NONNULL_END</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>其他暂时没问题。</p>
</li>
</ul>
<h4 id="获取实例方法列表"><a href="#获取实例方法列表" class="headerlink" title="获取实例方法列表"></a>获取实例方法列表</h4><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="comment">/// 获取实例方法</span></span><br><span class="line"><span class="comment">/// 获取实例方法</span></span><br><span class="line">-(<span class="keyword">void</span>)getInstanceMethod &#123;</span><br><span class="line">    <span class="keyword">unsigned</span> <span class="keyword">int</span> count;</span><br><span class="line">    Method *methodList = class_copyMethodList([<span class="keyword">self</span> <span class="keyword">class</span>], &amp;count);</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">unsigned</span> <span class="keyword">int</span> i = <span class="number">0</span>; i &lt; count; i++) &#123;</span><br><span class="line">        Method oneMethod = methodList[i];</span><br><span class="line">        <span class="comment">// 获取方法名</span></span><br><span class="line">        <span class="built_in">NSString</span> *methodName = <span class="built_in">NSStringFromSelector</span>(method_getName(oneMethod));</span><br><span class="line">        <span class="comment">// 获取返回类型</span></span><br><span class="line">        <span class="keyword">char</span> retName[<span class="number">512</span>] = &#123;&#125;;</span><br><span class="line">        method_getReturnType(oneMethod, retName, <span class="number">512</span>);</span><br><span class="line">        <span class="comment">// 获取参数个数</span></span><br><span class="line">        <span class="keyword">unsigned</span> <span class="keyword">int</span> argCount = method_getNumberOfArguments(oneMethod);</span><br><span class="line">        <span class="comment">// 方法指针地址</span></span><br><span class="line">        IMP methodImp = method_getImplementation(oneMethod);</span><br><span class="line">        <span class="built_in">NSLog</span>(<span class="string">@"MethodName: %@ 😎 retName: %s 😎 argCount: %u 😎 methodImp: %p"</span>,methodName, retName, argCount, methodImp);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>测试：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">[p getInstanceMethod];</span><br></pre></td></tr></table></figure></p>
<p>结果:<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="number">2019</span><span class="number">-11</span><span class="number">-05</span> <span class="number">18</span>:<span class="number">04</span>:<span class="number">33.361437</span>+<span class="number">0800</span> CateAndExt[<span class="number">52474</span>:<span class="number">754299</span>] MethodName: getInstanceMethod 😎 retName: v 😎 argCount: <span class="number">2</span> 😎 methodImp: <span class="number">0x10ec3c100</span></span><br><span class="line"><span class="number">2019</span><span class="number">-11</span><span class="number">-05</span> <span class="number">18</span>:<span class="number">04</span>:<span class="number">33.361690</span>+<span class="number">0800</span> CateAndExt[<span class="number">52474</span>:<span class="number">754299</span>] MethodName: methodListWithProtocol: 😎 retName: @ 😎 argCount: <span class="number">3</span> 😎 methodImp: <span class="number">0x10ec3bd90</span></span><br><span class="line"><span class="number">2019</span><span class="number">-11</span><span class="number">-05</span> <span class="number">18</span>:<span class="number">04</span>:<span class="number">33.361835</span>+<span class="number">0800</span> CateAndExt[<span class="number">52474</span>:<span class="number">754299</span>] MethodName: setSomeOneCountry 😎 retName: v 😎 argCount: <span class="number">2</span> 😎 methodImp: <span class="number">0x10ec3bed0</span></span><br><span class="line"><span class="number">2019</span><span class="number">-11</span><span class="number">-05</span> <span class="number">18</span>:<span class="number">04</span>:<span class="number">33.361971</span>+<span class="number">0800</span> CateAndExt[<span class="number">52474</span>:<span class="number">754299</span>] MethodName: whatName: 😎 retName: v 😎 argCount: <span class="number">3</span> 😎 methodImp: <span class="number">0x10ec3b900</span></span><br><span class="line"><span class="number">2019</span><span class="number">-11</span><span class="number">-05</span> <span class="number">18</span>:<span class="number">04</span>:<span class="number">33.362098</span>+<span class="number">0800</span> CateAndExt[<span class="number">52474</span>:<span class="number">754299</span>] MethodName: getProtocolMethods 😎 retName: v 😎 argCount: <span class="number">2</span> 😎 methodImp: <span class="number">0x10ec3ba10</span></span><br><span class="line"><span class="number">2019</span><span class="number">-11</span><span class="number">-05</span> <span class="number">18</span>:<span class="number">04</span>:<span class="number">33.362222</span>+<span class="number">0800</span> CateAndExt[<span class="number">52474</span>:<span class="number">754299</span>] MethodName: getIvarAndChange 😎 retName: v 😎 argCount: <span class="number">2</span> 😎 methodImp: <span class="number">0x10ec3bf00</span></span><br><span class="line"><span class="number">2019</span><span class="number">-11</span><span class="number">-05</span> <span class="number">18</span>:<span class="number">04</span>:<span class="number">33.362369</span>+<span class="number">0800</span> CateAndExt[<span class="number">52474</span>:<span class="number">754299</span>] MethodName: getClassMethod 😎 retName: v 😎 argCount: <span class="number">2</span> 😎 methodImp: <span class="number">0x10ec3c2a0</span></span><br><span class="line"><span class="number">2019</span><span class="number">-11</span><span class="number">-05</span> <span class="number">18</span>:<span class="number">04</span>:<span class="number">33.362543</span>+<span class="number">0800</span> CateAndExt[<span class="number">52474</span>:<span class="number">754299</span>] MethodName: .cxx_destruct 😎 retName: v 😎 argCount: <span class="number">2</span> 😎 methodImp: <span class="number">0x10ec3c350</span></span><br><span class="line"><span class="number">2019</span><span class="number">-11</span><span class="number">-05</span> <span class="number">18</span>:<span class="number">04</span>:<span class="number">33.364629</span>+<span class="number">0800</span> CateAndExt[<span class="number">52474</span>:<span class="number">754299</span>] MethodName: delegate 😎 retName: @ 😎 argCount: <span class="number">2</span> 😎 methodImp: <span class="number">0x10ec3c310</span></span><br><span class="line"><span class="number">2019</span><span class="number">-11</span><span class="number">-05</span> <span class="number">18</span>:<span class="number">04</span>:<span class="number">33.364777</span>+<span class="number">0800</span> CateAndExt[<span class="number">52474</span>:<span class="number">754299</span>] MethodName: setDelegate: 😎 retName: v 😎 argCount: <span class="number">3</span> 😎 methodImp: <span class="number">0x10ec3c330</span></span><br><span class="line"><span class="number">2019</span><span class="number">-11</span><span class="number">-05</span> <span class="number">18</span>:<span class="number">04</span>:<span class="number">33.364899</span>+<span class="number">0800</span> CateAndExt[<span class="number">52474</span>:<span class="number">754299</span>] MethodName: phone 😎 retName: @ 😎 argCount: <span class="number">2</span> 😎 methodImp: <span class="number">0x10ec3c2b0</span></span><br><span class="line"><span class="number">2019</span><span class="number">-11</span><span class="number">-05</span> <span class="number">18</span>:<span class="number">04</span>:<span class="number">33.365038</span>+<span class="number">0800</span> CateAndExt[<span class="number">52474</span>:<span class="number">754299</span>] MethodName: fullName 😎 retName: @ 😎 argCount: <span class="number">2</span> 😎 methodImp: <span class="number">0x10ec3b960</span></span><br><span class="line"><span class="number">2019</span><span class="number">-11</span><span class="number">-05</span> <span class="number">18</span>:<span class="number">04</span>:<span class="number">33.365172</span>+<span class="number">0800</span> CateAndExt[<span class="number">52474</span>:<span class="number">754299</span>] MethodName: nickName 😎 retName: @ 😎 argCount: <span class="number">2</span> 😎 methodImp: <span class="number">0x10ec3b6a0</span></span><br><span class="line"><span class="number">2019</span><span class="number">-11</span><span class="number">-05</span> <span class="number">18</span>:<span class="number">04</span>:<span class="number">33.365297</span>+<span class="number">0800</span> CateAndExt[<span class="number">52474</span>:<span class="number">754299</span>] MethodName: setNickName: 😎 retName: v 😎 argCount: <span class="number">3</span> 😎 methodImp: <span class="number">0x10ec3b6d0</span></span><br><span class="line"><span class="number">2019</span><span class="number">-11</span><span class="number">-05</span> <span class="number">18</span>:<span class="number">04</span>:<span class="number">33.365419</span>+<span class="number">0800</span> CateAndExt[<span class="number">52474</span>:<span class="number">754299</span>] MethodName: setPhone: 😎 retName: v 😎 argCount: <span class="number">3</span> 😎 methodImp: <span class="number">0x10ec3c2d0</span></span><br></pre></td></tr></table></figure></p>
<ul>
<li>该方法不能获取到类方法。</li>
<li>方法列表包括属性的 <code>setter</code> 和 <code>getter</code> 方法。</li>
<li>尽管该方法写在 <code>Person</code> 中，但是获取到了分类中的方法列表。</li>
<li>方法返回类型 <code>@</code> 、 <code>v</code> 分表代表什么意思，为什么参数个数会比看到的多了两个，参考文章 <a href="https://www.jianshu.com/p/9d744a6e011f" target="_blank" rel="noopener">runtime Method精讲</a>。</li>
</ul>
<h4 id="获取类方法"><a href="#获取类方法" class="headerlink" title="获取类方法"></a>获取类方法</h4><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="comment">/// 获取类方法</span></span><br><span class="line">-(<span class="keyword">void</span>)getClassMethod &#123;</span><br><span class="line">    <span class="keyword">unsigned</span> <span class="keyword">int</span> count;</span><br><span class="line">    Class metaclass = object_getClass([<span class="keyword">self</span> <span class="keyword">class</span>]);</span><br><span class="line">    Method *methodList = class_copyMethodList(metaclass, &amp;count);</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">unsigned</span> <span class="keyword">int</span> i = <span class="number">0</span>; i &lt; count; i++) &#123;</span><br><span class="line">        Method oneMethod = methodList[i];</span><br><span class="line">        <span class="comment">// 获取方法名</span></span><br><span class="line">        <span class="built_in">NSString</span> *methodName = <span class="built_in">NSStringFromSelector</span>(method_getName(oneMethod));</span><br><span class="line">        <span class="comment">// 获取返回类型</span></span><br><span class="line">        <span class="keyword">char</span> retName[<span class="number">512</span>] = &#123;&#125;;</span><br><span class="line">        method_getReturnType(oneMethod, retName, <span class="number">512</span>);</span><br><span class="line">        <span class="comment">// 获取参数个数</span></span><br><span class="line">        <span class="keyword">unsigned</span> <span class="keyword">int</span> argCount = method_getNumberOfArguments(oneMethod);</span><br><span class="line">        <span class="comment">// 方法指针地址</span></span><br><span class="line">        IMP methodImp = method_getImplementation(oneMethod);</span><br><span class="line">        <span class="built_in">NSLog</span>(<span class="string">@"类方法MethodName: %@ 🧐 retName: %s 🧐 argCount: %u 🧐 methodImp: %p"</span>,methodName, retName, argCount, methodImp);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>修改分类：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="meta">#import <span class="meta-string">"Person.h"</span></span></span><br><span class="line"></span><br><span class="line"><span class="built_in">NS_ASSUME_NONNULL_BEGIN</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">@interface</span> <span class="title">Person</span> (<span class="title">Man</span>)</span></span><br><span class="line"><span class="keyword">@property</span>(<span class="keyword">nonatomic</span>, <span class="keyword">strong</span>) <span class="built_in">NSString</span> *nickName;</span><br><span class="line"></span><br><span class="line">+(<span class="built_in">NSInteger</span>)howOldAreYou;</span><br><span class="line">+(<span class="built_in">NSInteger</span>)howOldAreYouoneMore;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">@end</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">NS_ASSUME_NONNULL_END</span></span><br></pre></td></tr></table></figure></p>
<p>测试：</p>
<figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">[p getClassMethod];</span><br></pre></td></tr></table></figure>
<p> 结果：<br> <figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="number">2019</span><span class="number">-11</span><span class="number">-05</span> <span class="number">18</span>:<span class="number">34</span>:<span class="number">19.045761</span>+<span class="number">0800</span> CateAndExt[<span class="number">53499</span>:<span class="number">774413</span>] 类方法MethodName: howOldAreYou 🧐 retName: q 🧐 argCount: <span class="number">2</span> 🧐 methodImp: <span class="number">0x10204e3b0</span></span><br><span class="line"><span class="number">2019</span><span class="number">-11</span><span class="number">-05</span> <span class="number">18</span>:<span class="number">34</span>:<span class="number">19.045947</span>+<span class="number">0800</span> CateAndExt[<span class="number">53499</span>:<span class="number">774413</span>] 类方法MethodName: howOldAreYou 🧐 retName: q 🧐 argCount: <span class="number">2</span> 🧐 methodImp: <span class="number">0x10204e5c0</span></span><br><span class="line"><span class="number">2019</span><span class="number">-11</span><span class="number">-05</span> <span class="number">18</span>:<span class="number">34</span>:<span class="number">19.047406</span>+<span class="number">0800</span> CateAndExt[<span class="number">53499</span>:<span class="number">774413</span>] 类方法MethodName: howOldAreYouoneMore 🧐 retName: q 🧐 argCount: <span class="number">2</span> 🧐 methodImp: <span class="number">0x10204e3d0</span></span><br><span class="line"><span class="number">2019</span><span class="number">-11</span><span class="number">-05</span> <span class="number">18</span>:<span class="number">34</span>:<span class="number">19.047614</span>+<span class="number">0800</span> CateAndExt[<span class="number">53499</span>:<span class="number">774413</span>] 类方法MethodName: address 🧐 retName: @ 🧐 argCount: <span class="number">2</span> 🧐 methodImp: <span class="number">0x10204e660</span></span><br><span class="line"><span class="number">2019</span><span class="number">-11</span><span class="number">-05</span> <span class="number">18</span>:<span class="number">34</span>:<span class="number">19.048768</span>+<span class="number">0800</span> CateAndExt[<span class="number">53499</span>:<span class="number">774413</span>] 类方法MethodName: setAddress: 🧐 retName: v 🧐 argCount: <span class="number">3</span> 🧐 methodImp: <span class="number">0x10204e680</span></span><br></pre></td></tr></table></figure></p>
<ul>
<li>仅仅能获取类方法，获取不到实例方法。</li>
<li>类方法、以及类属性，都能获取到。</li>
<li>分类中的类方法也能获取到。分类写了和主类完全一致的方法时，两个方法同时存在。调用重复的方法时，优先调用子类方法。当然，可以通过runtime强制调用主类方法，这里不在讨论。</li>
<li>通过方法指针可确定，分配和主类重复的方法时实质上是两个不同的方法。</li>
</ul>
<p>由此联想，是否可以通过 <code>Class metaclass = object_getClass([self class]);</code> 获取类属性。</p>
<figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="comment">/// 获取类属性</span></span><br><span class="line">-(<span class="keyword">void</span>)getClassIvar &#123;</span><br><span class="line">    <span class="keyword">unsigned</span> <span class="keyword">int</span> count;</span><br><span class="line">    Class metaclass = object_getClass([<span class="keyword">self</span> <span class="keyword">class</span>]);</span><br><span class="line">    objc_property_t *properties = class_copyPropertyList(metaclass, &amp;count);</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">unsigned</span> <span class="keyword">int</span> i = <span class="number">0</span>; i &lt; count; i++) &#123;</span><br><span class="line">        objc_property_t oneP = properties[i];</span><br><span class="line">        <span class="comment">// 获取名字和类型</span></span><br><span class="line">        <span class="keyword">const</span> <span class="keyword">char</span> *memberName = property_getName(oneP);</span><br><span class="line">        <span class="comment">// 一次打印属性名称和属性类型</span></span><br><span class="line">        <span class="built_in">NSLog</span>(<span class="string">@"类属性信息  %s"</span>, memberName);</span><br><span class="line">    &#125;</span><br><span class="line">    free(properties);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>打印结果：</p>
<figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="number">2019</span><span class="number">-11</span><span class="number">-05</span> <span class="number">18</span>:<span class="number">34</span>:<span class="number">19.050084</span>+<span class="number">0800</span> CateAndExt[<span class="number">53499</span>:<span class="number">774413</span>] 类属性信息  address</span><br></pre></td></tr></table></figure>
<ul>
<li>使用 <code>class_copyIvarList</code> 不能获取到类方法，但是使用 <code>class_copyPropertyList</code> 则可以。引发另一个问题：<code>class_copyIvarList</code>  和  <code>class_copyPropertyList</code> 有什么区别。</li>
</ul>
<p>以下引用自 <a href="https://blog.csdn.net/east5683/article/details/45875713" target="_blank" rel="noopener">class_copyPropertyList与class_copyIvarList区别</a>:</p>
<blockquote>
<p>class_copyPropertyList返回的仅仅是对象类的属性(@property申明的属性)，而class_copyIvarList返回类的所有属性和变量(包括在@interface大括号中声明的变量)。</p>
</blockquote>
<p>原作者是写的测试函数，我在项目中直接测试，和文中结果不太一样。</p>
<p>为 <code>Person</code> 添加属性<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">@interface</span> <span class="title">Person</span> : <span class="title">NSObject</span>&lt;<span class="title">HumanInfo</span>&gt;</span></span><br><span class="line"></span><br><span class="line">&#123;</span><br><span class="line">    <span class="built_in">NSInteger</span> height;</span><br><span class="line">    <span class="built_in">NSString</span> *workName;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/// 类属性</span></span><br><span class="line"><span class="keyword">@property</span>(<span class="keyword">nonatomic</span>, <span class="keyword">strong</span>, <span class="keyword">class</span>) <span class="built_in">NSString</span> *address;</span><br><span class="line"><span class="comment">/// 实例属性</span></span><br><span class="line"><span class="keyword">@property</span>(<span class="keyword">nonatomic</span>, <span class="keyword">strong</span>) <span class="built_in">NSString</span> *phone;</span><br><span class="line"><span class="keyword">@property</span>(<span class="keyword">nonatomic</span>, <span class="keyword">assign</span>) <span class="keyword">id</span> &lt;HumanInfo&gt; delegate;</span><br></pre></td></tr></table></figure></p>
<p>添加一个新的属性打印方法：</p>
<figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="comment">/// 另一个获取属性的方法</span></span><br><span class="line">-(<span class="keyword">void</span>)getProperties &#123;</span><br><span class="line">    <span class="keyword">unsigned</span> <span class="keyword">int</span> count;</span><br><span class="line">    <span class="keyword">unsigned</span> <span class="keyword">int</span> pCount;</span><br><span class="line">    Ivar *vars = class_copyIvarList([<span class="keyword">self</span> <span class="keyword">class</span>], &amp;count);</span><br><span class="line">    objc_property_t *properties = class_copyPropertyList([<span class="keyword">self</span> <span class="keyword">class</span>], &amp;pCount);</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">unsigned</span> <span class="keyword">int</span> i = <span class="number">0</span>; i &lt; count; i++) &#123;</span><br><span class="line">        Ivar oneVar = vars[i];</span><br><span class="line">        <span class="comment">// 获取名字和类型</span></span><br><span class="line">        <span class="keyword">const</span> <span class="keyword">char</span> *memberName = ivar_getName(oneVar);</span><br><span class="line">        <span class="comment">// 一次打印属性名称和属性类型</span></span><br><span class="line">        <span class="built_in">NSLog</span>(<span class="string">@"class_copyIvarList 属性  %s"</span>, memberName);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">unsigned</span> <span class="keyword">int</span> i = <span class="number">0</span>; i &lt; pCount; i++) &#123;</span><br><span class="line">        objc_property_t oneP = properties[i];</span><br><span class="line">        <span class="comment">// 获取名字和类型</span></span><br><span class="line">        <span class="keyword">const</span> <span class="keyword">char</span> *memberName = property_getName(oneP);</span><br><span class="line">        <span class="comment">// 一次打印属性名称和属性类型</span></span><br><span class="line">        <span class="built_in">NSLog</span>(<span class="string">@"class_copyPropertyList 属性  %s"</span>, memberName);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    free(vars);</span><br><span class="line">    free(properties);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>打印结果：<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="number">2019</span><span class="number">-11</span><span class="number">-05</span> <span class="number">18</span>:<span class="number">51</span>:<span class="number">52.544776</span>+<span class="number">0800</span> CateAndExt[<span class="number">54040</span>:<span class="number">784325</span>] class_copyIvarList 属性  height</span><br><span class="line"><span class="number">2019</span><span class="number">-11</span><span class="number">-05</span> <span class="number">18</span>:<span class="number">51</span>:<span class="number">52.545474</span>+<span class="number">0800</span> CateAndExt[<span class="number">54040</span>:<span class="number">784325</span>] class_copyIvarList 属性  workName</span><br><span class="line"><span class="number">2019</span><span class="number">-11</span><span class="number">-05</span> <span class="number">18</span>:<span class="number">51</span>:<span class="number">52.546048</span>+<span class="number">0800</span> CateAndExt[<span class="number">54040</span>:<span class="number">784325</span>] class_copyIvarList 属性  phone</span><br><span class="line"><span class="number">2019</span><span class="number">-11</span><span class="number">-05</span> <span class="number">18</span>:<span class="number">51</span>:<span class="number">52.546394</span>+<span class="number">0800</span> CateAndExt[<span class="number">54040</span>:<span class="number">784325</span>] class_copyIvarList 属性  _delegate</span><br><span class="line"><span class="number">2019</span><span class="number">-11</span><span class="number">-05</span> <span class="number">18</span>:<span class="number">51</span>:<span class="number">52.546694</span>+<span class="number">0800</span> CateAndExt[<span class="number">54040</span>:<span class="number">784325</span>] class_copyPropertyList 属性  nickName</span><br><span class="line"><span class="number">2019</span><span class="number">-11</span><span class="number">-05</span> <span class="number">18</span>:<span class="number">51</span>:<span class="number">52.547136</span>+<span class="number">0800</span> CateAndExt[<span class="number">54040</span>:<span class="number">784325</span>] class_copyPropertyList 属性  phone</span><br><span class="line"><span class="number">2019</span><span class="number">-11</span><span class="number">-05</span> <span class="number">18</span>:<span class="number">51</span>:<span class="number">52.547538</span>+<span class="number">0800</span> CateAndExt[<span class="number">54040</span>:<span class="number">784325</span>] class_copyPropertyList 属性  delegate</span><br><span class="line"><span class="number">2019</span><span class="number">-11</span><span class="number">-05</span> <span class="number">18</span>:<span class="number">51</span>:<span class="number">52.547878</span>+<span class="number">0800</span> CateAndExt[<span class="number">54040</span>:<span class="number">784325</span>] class_copyPropertyList 属性  hash</span><br><span class="line"><span class="number">2019</span><span class="number">-11</span><span class="number">-05</span> <span class="number">18</span>:<span class="number">51</span>:<span class="number">52.548443</span>+<span class="number">0800</span> CateAndExt[<span class="number">54040</span>:<span class="number">784325</span>] class_copyPropertyList 属性  superclass</span><br><span class="line"><span class="number">2019</span><span class="number">-11</span><span class="number">-05</span> <span class="number">18</span>:<span class="number">51</span>:<span class="number">52.549367</span>+<span class="number">0800</span> CateAndExt[<span class="number">54040</span>:<span class="number">784325</span>] class_copyPropertyList 属性  description</span><br><span class="line"><span class="number">2019</span><span class="number">-11</span><span class="number">-05</span> <span class="number">18</span>:<span class="number">51</span>:<span class="number">52.549822</span>+<span class="number">0800</span> CateAndExt[<span class="number">54040</span>:<span class="number">784325</span>] class_copyPropertyList 属性  debugDescription</span><br></pre></td></tr></table></figure></p>
<ul>
<li><code>class_copyIvarList</code> 只能找到当前类的所有属性，包括 <code>@property</code> 修饰的以及大括号修饰的变量。</li>
<li><code>class_copyPropertyList</code> 对于当前类只能打印 <code>@property</code> 修饰的属性，但是，还能找到父类、分类中的属性。</li>
<li>饰的以及大括号修饰的变量。即当前类所有的属性。</li>
<li><code>class_copyPropertyList</code>  能找到类属性。 </li>
</ul>
<h3 id="Method-分析"><a href="#Method-分析" class="headerlink" title="Method 分析"></a><code>Method</code> 分析</h3><p>由于内容较多且独立，所以拆分出一篇单独的文章分享。</p>
<p><a href="">runtime <code>Method</code>、<code>Ivar</code>、<code>Category</code>、<code>objc_property_t</code> 分析</a></p>
<h2 id="后记"><a href="#后记" class="headerlink" title="后记"></a>后记</h2><p>虽然这些仅仅是 <code>runtime</code> 的冰山一角，但是关于方法、属性的查询、打印应该做了比较详细的分析，后续关于此类的问题，也会在这里补全。</p>
<p>参考文章：</p>
<p><a href="https://blog.csdn.net/east5683/article/details/45875713" target="_blank" rel="noopener">class_copyPropertyList与class_copyIvarList区别</a></p>
<p><a href="https://www.jianshu.com/p/1d9c47b28f77" target="_blank" rel="noopener">iOS 获取类的属性,实例方法,类方法等</a></p>
]]></content>
      <tags>
        <tag>runtime</tag>
      </tags>
  </entry>
  <entry>
    <title>源码阅读CTMediator</title>
    <url>/2019/11/11/%E6%BA%90%E7%A0%81%E9%98%85%E8%AF%BBCTMediator/</url>
    <content><![CDATA[<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>今年项目进行组件化时做了一些技术调研，关于路由最终决定在 <code>Target-Action</code> 和 <code>协议代理</code> 两个方案中选择。因为项目采用 <code>Swift</code> 语言开发，并且 <code>Swift4.0</code> 之后支持了协议扩展，所以选用了后者。具体参考的是 <a href="https://github.com/Vegetarians/Lotusoot.git" target="_blank" rel="noopener">灵活的 Swift 组件解耦和通信工具Lotusoot</a>。之后也会对这个的源码在细读一遍。</p>
<a id="more"></a>
<p>因为近期回归 <code>Objective-C</code>，所以这里再阅读一次 <a href="https://github.com/casatwy/CTMediator" target="_blank" rel="noopener">CTMediator</a> 的源码实现。</p>
<p>参考文章：</p>
<p><a href="https://casatwy.com/iOS-Modulization.html" target="_blank" rel="noopener">iOS应用架构谈 组件化方案</a></p>
<p>这里只是提供了组件化思路文章的入口，作者 <code>casatwy</code> 这系列的几篇文章，每篇都值得细读多遍。</p>
<h2 id="源码分析"><a href="#源码分析" class="headerlink" title="源码分析"></a>源码分析</h2><p>因为 <code>CTMediator</code> 非常的精简，更主要的是代表了一种思路，很多非常细节的点或者业务相关紧密的，都没在源码中处理，但是大都提出了一些建议入口。所以这里会对所有代码进行展示分析。</p>
<h3 id="对外公开方法"><a href="#对外公开方法" class="headerlink" title="对外公开方法"></a>对外公开方法</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">#import &lt;UIKit/UIKit.h&gt;</span><br><span class="line"></span><br><span class="line">extern NSString * const kCTMediatorParamsKeySwiftTargetModuleName;</span><br><span class="line"></span><br><span class="line">@interface CTMediator : NSObject</span><br><span class="line"></span><br><span class="line">+ (instancetype)sharedInstance;</span><br><span class="line"></span><br><span class="line">// 远程App调用入口</span><br><span class="line">- (id)performActionWithUrl:(NSURL *)url completion:(void(^)(NSDictionary *info))completion;</span><br><span class="line">// 本地组件调用入口</span><br><span class="line">- (id)performTarget:(NSString *)targetName action:(NSString *)actionName params:(NSDictionary *)params shouldCacheTarget:(BOOL)shouldCacheTarget;</span><br><span class="line">- (void)releaseCachedTargetWithTargetName:(NSString *)targetName;</span><br><span class="line"></span><br><span class="line">@end</span><br></pre></td></tr></table></figure>
<h3 id="获取单例对象"><a href="#获取单例对象" class="headerlink" title="获取单例对象"></a>获取单例对象</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">#pragma mark - public methods</span><br><span class="line">+ (instancetype)sharedInstance</span><br><span class="line">&#123;</span><br><span class="line">    static CTMediator *mediator;</span><br><span class="line">    static dispatch_once_t onceToken;</span><br><span class="line">    dispatch_once(&amp;onceToken, ^&#123;</span><br><span class="line">        mediator = [[CTMediator alloc] init];</span><br><span class="line">    &#125;);</span><br><span class="line">    return mediator;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这里并没有对 <code>alloc</code> 、<code>allocWithZone</code> 等方法补充，只是作为规定，使用 <code>CTMediator</code> 必须使用该单例。关于单例的实现，<code>Swift</code> 语言真的是精简安全太多:<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">class CTMediator &#123;</span><br><span class="line">    // 单例实现</span><br><span class="line">    static let shared = CTMediator()</span><br><span class="line">    private init() &#123;&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<h3 id="远程调用入口"><a href="#远程调用入口" class="headerlink" title="远程调用入口"></a>远程调用入口</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">// 远程App调用入口</span><br><span class="line">- (id)performActionWithUrl:(NSURL *)url completion:(void(^)(NSDictionary *info))completion;</span><br></pre></td></tr></table></figure>
<p>远程调用时，是对约定格式的 <code>URL</code> 进行解析，解析完成后，依然执行的是本地组件的调用入口：<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">/*</span><br><span class="line"> scheme://[target]/[action]?[params]</span><br><span class="line"> </span><br><span class="line"> url sample:</span><br><span class="line"> aaa://targetA/actionB?id=1234</span><br><span class="line"> */</span><br><span class="line"></span><br><span class="line">- (id)performActionWithUrl:(NSURL *)url completion:(void (^)(NSDictionary *))completion</span><br><span class="line">&#123;</span><br><span class="line">    NSMutableDictionary *params = [[NSMutableDictionary alloc] init];</span><br><span class="line">    NSString *urlString = [url query];</span><br><span class="line">    for (NSString *param in [urlString componentsSeparatedByString:@&quot;&amp;&quot;]) &#123;</span><br><span class="line">        NSArray *elts = [param componentsSeparatedByString:@&quot;=&quot;];</span><br><span class="line">        if([elts count] &lt; 2) continue;</span><br><span class="line">        [params setObject:[elts lastObject] forKey:[elts firstObject]];</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    // 这里这么写主要是出于安全考虑，防止黑客通过远程方式调用本地模块。这里的做法足以应对绝大多数场景，如果要求更加严苛，也可以做更加复杂的安全逻辑。</span><br><span class="line">    NSString *actionName = [url.path stringByReplacingOccurrencesOfString:@&quot;/&quot; withString:@&quot;&quot;];</span><br><span class="line">    if ([actionName hasPrefix:@&quot;native&quot;]) &#123;</span><br><span class="line">        return @(NO);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    // 这个demo针对URL的路由处理非常简单，就只是取对应的target名字和method名字，但这已经足以应对绝大部份需求。如果需要拓展，可以在这个方法调用之前加入完整的路由逻辑</span><br><span class="line">    id result = [self performTarget:url.host action:actionName params:params shouldCacheTarget:NO];</span><br><span class="line">    if (completion) &#123;</span><br><span class="line">        if (result) &#123;</span><br><span class="line">            completion(@&#123;@&quot;result&quot;:result&#125;);</span><br><span class="line">        &#125; else &#123;</span><br><span class="line">            completion(nil);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    return result;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<h3 id="本地组件调用入口"><a href="#本地组件调用入口" class="headerlink" title="本地组件调用入口"></a>本地组件调用入口</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">/// 本地组件调用入口</span><br><span class="line">/// @param targetName 模块的方法所属类名</span><br><span class="line">/// @param actionName 方法名</span><br><span class="line">/// @param params 携带参数</span><br><span class="line">/// @param shouldCacheTarget 是否缓存该模块</span><br><span class="line">- (id)performTarget:(NSString *)targetName action:(NSString *)actionName params:(NSDictionary *)params shouldCacheTarget:(BOOL)shouldCacheTarget;</span><br></pre></td></tr></table></figure>
<p>这个就是该组件化方案的核心方法了。通过传入的模块的方法所属类名、方法名，去对应的模块代码中寻找匹配项。正是使用了反射机制、<code>runtime</code> 等特性，所以才能够彻底分离各个功能业务模块间的相互依赖。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">- (id)performTarget:(NSString *)targetName action:(NSString *)actionName params:(NSDictionary *)params shouldCacheTarget:(BOOL)shouldCacheTarget</span><br><span class="line">&#123;</span><br><span class="line">    NSString *swiftModuleName = params[kCTMediatorParamsKeySwiftTargetModuleName];</span><br><span class="line">    </span><br><span class="line">    // generate target</span><br><span class="line">    NSString *targetClassString = nil;</span><br><span class="line">    if (swiftModuleName.length &gt; 0) &#123;</span><br><span class="line">        targetClassString = [NSString stringWithFormat:@&quot;%@.Target_%@&quot;, swiftModuleName, targetName];</span><br><span class="line">    &#125; else &#123;</span><br><span class="line">        targetClassString = [NSString stringWithFormat:@&quot;Target_%@&quot;, targetName];</span><br><span class="line">    &#125;</span><br><span class="line">    // 先从缓存字典中查找</span><br><span class="line">    NSObject *target = self.cachedTarget[targetClassString];</span><br><span class="line">    if (target == nil) &#123;</span><br><span class="line">        Class targetClass = NSClassFromString(targetClassString);</span><br><span class="line">        target = [[targetClass alloc] init];</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    // generate action</span><br><span class="line">    NSString *actionString = [NSString stringWithFormat:@&quot;Action_%@:&quot;, actionName];</span><br><span class="line">    SEL action = NSSelectorFromString(actionString);</span><br><span class="line">    </span><br><span class="line">    if (target == nil) &#123;</span><br><span class="line">        // 这里是处理无响应请求的地方之一，这个demo做得比较简单，如果没有可以响应的target，就直接return了。实际开发过程中是可以事先给一个固定的target专门用于在这个时候顶上，然后处理这种请求的</span><br><span class="line">        [self NoTargetActionResponseWithTargetString:targetClassString selectorString:actionString originParams:params];</span><br><span class="line">        return nil;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    if (shouldCacheTarget) &#123;</span><br><span class="line">        self.cachedTarget[targetClassString] = target;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    if ([target respondsToSelector:action]) &#123;</span><br><span class="line">        return [self safePerformAction:action target:target params:params];</span><br><span class="line">    &#125; else &#123;</span><br><span class="line">        // 这里是处理无响应请求的地方，如果无响应，则尝试调用对应target的notFound方法统一处理</span><br><span class="line">        SEL action = NSSelectorFromString(@&quot;notFound:&quot;);</span><br><span class="line">        if ([target respondsToSelector:action]) &#123;</span><br><span class="line">            return [self safePerformAction:action target:target params:params];</span><br><span class="line">        &#125; else &#123;</span><br><span class="line">            // 这里也是处理无响应请求的地方，在notFound都没有的时候，这个demo是直接return了。实际开发过程中，可以用前面提到的固定的target顶上的。</span><br><span class="line">            [self NoTargetActionResponseWithTargetString:targetClassString selectorString:actionString originParams:params];</span><br><span class="line">            [self.cachedTarget removeObjectForKey:targetClassString];</span><br><span class="line">            return nil;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>针对这个方法实现，有几点要说的：</p>
<ul>
<li>组件的模块名是有约定规范的。对于 <code>Swift</code> ，<code>ModuleTargetName.Target_ActionName</code> 。对于 <code>Objective-C</code>，<code>Target_ActionName</code>，如果需要针对自己项目特殊化定制，则需要修改这里。</li>
<li>关于模块缓存字典 <code>@property (nonatomic, strong) NSMutableDictionary *cachedTarget;</code>，如果模块过于复杂，并且数量多，这里会造成内存上的压力，不过对于中小型项目，应该没问题。个人认为，如果把储存的 <code>Target</code> ，改为 <code>String</code> 类型的 <code>TargetName</code>，用的时候再转换，应该会好些。标记一下，这里是一个可优化的点。</li>
<li>方法中，<code>target == nil</code> 、 <code>![target respondsToSelector:action]</code> 、 <code>![target respondsToSelector:action]</code> 都是我们需要根据业务或者产品需求来自定义的地方。</li>
</ul>
<h3 id="清除缓存字典数据"><a href="#清除缓存字典数据" class="headerlink" title="清除缓存字典数据"></a>清除缓存字典数据</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">- (void)releaseCachedTargetWithTargetName:(NSString *)targetName;</span><br></pre></td></tr></table></figure>
<p>其实就是从字典中把对应的模块移除掉：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">- (void)releaseCachedTargetWithTargetName:(NSString *)targetName</span><br><span class="line">&#123;</span><br><span class="line">    NSString *targetClassString = [NSString stringWithFormat:@&quot;Target_%@&quot;, targetName];</span><br><span class="line">    [self.cachedTarget removeObjectForKey:targetClassString];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="私有方法-private-methods"><a href="#私有方法-private-methods" class="headerlink" title="私有方法 private methods"></a>私有方法 <code>private methods</code></h3><h4 id="没有-Target"><a href="#没有-Target" class="headerlink" title="没有 Target"></a>没有 <code>Target</code></h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">/// 没有找到对应的模块实现类</span><br><span class="line">/// @param targetString Target</span><br><span class="line">/// @param selectorString 方法名</span><br><span class="line">/// @param originParams 参数</span><br><span class="line">- (void)NoTargetActionResponseWithTargetString:(NSString *)targetString selectorString:(NSString *)selectorString originParams:(NSDictionary *)originParams</span><br><span class="line">&#123;</span><br><span class="line">    SEL action = NSSelectorFromString(@&quot;Action_response:&quot;);</span><br><span class="line">    NSObject *target = [[NSClassFromString(@&quot;Target_NoTargetAction&quot;) alloc] init];</span><br><span class="line">    </span><br><span class="line">    NSMutableDictionary *params = [[NSMutableDictionary alloc] init];</span><br><span class="line">    params[@&quot;originParams&quot;] = originParams;</span><br><span class="line">    params[@&quot;targetString&quot;] = targetString;</span><br><span class="line">    params[@&quot;selectorString&quot;] = selectorString;</span><br><span class="line">    </span><br><span class="line">    [self safePerformAction:action target:target params:params];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>我们要根据自己的业务和产品需求，自定义自己的响应错误页面和方法。这里可以做个性化设置。</p>
<h4 id="安全实现"><a href="#安全实现" class="headerlink" title="安全实现"></a>安全实现</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">- (id)safePerformAction:(SEL)action target:(NSObject *)target params:(NSDictionary *)params</span><br><span class="line">&#123;</span><br><span class="line">    NSMethodSignature* methodSig = [target methodSignatureForSelector:action];</span><br><span class="line">    if(methodSig == nil) &#123;</span><br><span class="line">        return nil;</span><br><span class="line">    &#125;</span><br><span class="line">    const char* retType = [methodSig methodReturnType];</span><br><span class="line"></span><br><span class="line">    if (strcmp(retType, @encode(void)) == 0) &#123;</span><br><span class="line">        NSInvocation *invocation = [NSInvocation invocationWithMethodSignature:methodSig];</span><br><span class="line">        [invocation setArgument:&amp;params atIndex:2];</span><br><span class="line">        [invocation setSelector:action];</span><br><span class="line">        [invocation setTarget:target];</span><br><span class="line">        [invocation invoke];</span><br><span class="line">        return nil;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    if (strcmp(retType, @encode(NSInteger)) == 0) &#123;</span><br><span class="line">        NSInvocation *invocation = [NSInvocation invocationWithMethodSignature:methodSig];</span><br><span class="line">        [invocation setArgument:&amp;params atIndex:2];</span><br><span class="line">        [invocation setSelector:action];</span><br><span class="line">        [invocation setTarget:target];</span><br><span class="line">        [invocation invoke];</span><br><span class="line">        NSInteger result = 0;</span><br><span class="line">        [invocation getReturnValue:&amp;result];</span><br><span class="line">        return @(result);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    if (strcmp(retType, @encode(BOOL)) == 0) &#123;</span><br><span class="line">        NSInvocation *invocation = [NSInvocation invocationWithMethodSignature:methodSig];</span><br><span class="line">        [invocation setArgument:&amp;params atIndex:2];</span><br><span class="line">        [invocation setSelector:action];</span><br><span class="line">        [invocation setTarget:target];</span><br><span class="line">        [invocation invoke];</span><br><span class="line">        BOOL result = 0;</span><br><span class="line">        [invocation getReturnValue:&amp;result];</span><br><span class="line">        return @(result);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    if (strcmp(retType, @encode(CGFloat)) == 0) &#123;</span><br><span class="line">        NSInvocation *invocation = [NSInvocation invocationWithMethodSignature:methodSig];</span><br><span class="line">        [invocation setArgument:&amp;params atIndex:2];</span><br><span class="line">        [invocation setSelector:action];</span><br><span class="line">        [invocation setTarget:target];</span><br><span class="line">        [invocation invoke];</span><br><span class="line">        CGFloat result = 0;</span><br><span class="line">        [invocation getReturnValue:&amp;result];</span><br><span class="line">        return @(result);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    if (strcmp(retType, @encode(NSUInteger)) == 0) &#123;</span><br><span class="line">        NSInvocation *invocation = [NSInvocation invocationWithMethodSignature:methodSig];</span><br><span class="line">        [invocation setArgument:&amp;params atIndex:2];</span><br><span class="line">        [invocation setSelector:action];</span><br><span class="line">        [invocation setTarget:target];</span><br><span class="line">        [invocation invoke];</span><br><span class="line">        NSUInteger result = 0;</span><br><span class="line">        [invocation getReturnValue:&amp;result];</span><br><span class="line">        return @(result);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">#pragma clang diagnostic push</span><br><span class="line">#pragma clang diagnostic ignored &quot;-Warc-performSelector-leaks&quot;</span><br><span class="line">    return [target performSelector:action withObject:params];</span><br><span class="line">#pragma clang diagnostic pop</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>简单来说，就是对于基础数据类型，拿出来安全处理后再返回，防止数据解析错误。</p>
<p>这个方法里，<code>NSMethodSignature</code> 、 <code>NSInvocation</code> 需要特殊说明。这里推荐一篇文章吧 <a href="https://blog.csdn.net/zww1984774346/article/details/78676361" target="_blank" rel="noopener">NSMethodSignature、NSInvocation进行消息转发</a>。</p>
<h4 id="多说一点"><a href="#多说一点" class="headerlink" title="多说一点"></a>多说一点</h4><p><code>NSMethodSignature</code> 的头文件实现：<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">#import &lt;Foundation/NSObject.h&gt;</span><br><span class="line"></span><br><span class="line">NS_ASSUME_NONNULL_BEGIN</span><br><span class="line"></span><br><span class="line">NS_SWIFT_UNAVAILABLE(&quot;NSInvocation and related APIs not available&quot;)</span><br><span class="line">@interface NSMethodSignature : NSObject</span><br><span class="line"></span><br><span class="line">+ (nullable NSMethodSignature *)signatureWithObjCTypes:(const char *)types;</span><br><span class="line"></span><br><span class="line">@property (readonly) NSUInteger numberOfArguments;</span><br><span class="line">- (const char *)getArgumentTypeAtIndex:(NSUInteger)idx NS_RETURNS_INNER_POINTER;</span><br><span class="line"></span><br><span class="line">@property (readonly) NSUInteger frameLength;</span><br><span class="line"></span><br><span class="line">- (BOOL)isOneway;</span><br><span class="line"></span><br><span class="line">@property (readonly) const char *methodReturnType NS_RETURNS_INNER_POINTER;</span><br><span class="line">@property (readonly) NSUInteger methodReturnLength;</span><br><span class="line"></span><br><span class="line">@end</span><br><span class="line"></span><br><span class="line">NS_ASSUME_NONNULL_END</span><br></pre></td></tr></table></figure></p>
<p><code>NSInvocation</code> 的头文件实现：<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">#import &lt;Foundation/NSObject.h&gt;</span><br><span class="line">#include &lt;stdbool.h&gt;</span><br><span class="line"></span><br><span class="line">@class NSMethodSignature;</span><br><span class="line"></span><br><span class="line">NS_ASSUME_NONNULL_BEGIN</span><br><span class="line"></span><br><span class="line">NS_SWIFT_UNAVAILABLE(&quot;NSInvocation and related APIs not available&quot;)</span><br><span class="line">@interface NSInvocation : NSObject</span><br><span class="line"></span><br><span class="line">+ (NSInvocation *)invocationWithMethodSignature:(NSMethodSignature *)sig;</span><br><span class="line"></span><br><span class="line">@property (readonly, retain) NSMethodSignature *methodSignature;</span><br><span class="line"></span><br><span class="line">- (void)retainArguments;</span><br><span class="line">@property (readonly) BOOL argumentsRetained;</span><br><span class="line"></span><br><span class="line">@property (nullable, assign) id target;</span><br><span class="line">@property SEL selector;</span><br><span class="line"></span><br><span class="line">- (void)getReturnValue:(void *)retLoc;</span><br><span class="line">- (void)setReturnValue:(void *)retLoc;</span><br><span class="line"></span><br><span class="line">- (void)getArgument:(void *)argumentLocation atIndex:(NSInteger)idx;</span><br><span class="line">- (void)setArgument:(void *)argumentLocation atIndex:(NSInteger)idx;</span><br><span class="line"></span><br><span class="line">- (void)invoke;</span><br><span class="line">- (void)invokeWithTarget:(id)target;</span><br><span class="line"></span><br><span class="line">@end</span><br></pre></td></tr></table></figure></p>
<p>其实可以看到 <code>NS_SWIFT_UNAVAILABLE</code>。这些特性在 <code>Swift</code> 中都被抛弃了。我之前尝试用 <code>Swift</code> 重写 <code>CTMediator</code> 然后放到项目中使用，因为发现很多特性不能使用了，也就作罢了。不过作者也详细说明了如何在 <code>Swift</code> 项目中使用该框架： <a href="https://casatwy.com/CTMediator_in_Swift.html" target="_blank" rel="noopener">CTMediator的Swift应用</a>。</p>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>学以致用，有些源码虽然非常精简，但是却包含了非常多值得思考的之事。譬如 <code>SwiftyJson</code> 仅仅凭借单个文件，千余行代码，就在 <code>github</code> 获取上万 <code>star</code>。 <code>CTMediator</code> 亦是如此。有些源码包罗万象，如 <code>SDWebImage</code> 等，也是学习的绝佳材料。</p>
]]></content>
      <tags>
        <tag>CTMediator,源码阅读</tag>
      </tags>
  </entry>
  <entry>
    <title>iOS内存管理分析</title>
    <url>/2019/11/11/iOS%E5%86%85%E5%AD%98%E7%AE%A1%E7%90%86%E5%88%86%E6%9E%90/</url>
    <content><![CDATA[<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>本篇博客手动 <code>copy</code> 自 <a href="https://www.jianshu.com/p/48665652e4e4" target="_blank" rel="noopener">iOS 开发：彻底理解 iOS 内存管理（MRC、ARC）</a>。谢谢原作者的详细分析。</p>
<a id="more"></a>
<h2 id="正文"><a href="#正文" class="headerlink" title="正文"></a>正文</h2><h4 id="一、什么是内存管理"><a href="#一、什么是内存管理" class="headerlink" title="一、什么是内存管理"></a>一、什么是内存管理</h4><ul>
<li>程序运行过程中，通常以下行为，会增加程序的内存使用：<ul>
<li>创建一个对象</li>
<li>定义一个变量</li>
<li>调用一个函数或者方法</li>
</ul>
</li>
<li>而一个设备的内存是有限的，程序能占用的内存也是有限的</li>
<li>当程序所占用的内存较多时，系统就会发出内存警告，这时就得回收一些不需要的内存空间。比如回收一些不需要使用的对象、变量等</li>
<li>如果程序占用系统内存过大，系统就可能强制关闭程序，造成程序崩溃、闪退现象，影响用户体验</li>
</ul>
<blockquote>
<p>综上所述，我们要对内存进行合理的内存分配、清除内存，回收哪些不需要在使用的对象，从而保证程序的稳定性。</p>
</blockquote>
<p>那么，哪些对象需要我们进行内存管理呢？</p>
<ul>
<li>任何继承了 <code>NSObject</code> 的对象，都需要进行内存管理。</li>
<li>而其他非对象类型，如 <code>int</code> 、 <code>char</code> 、 <code>float</code> 、 <code>double</code> 、 <code>struct</code> 、 <code>enum</code> 等，不需要进行内存管理。</li>
</ul>
<p>这是因为：</p>
<ul>
<li>继承了 <code>NSObject</code> 的对象，存储在操作系统到 <strong>堆</strong> 里边。</li>
<li>操作系统的 <strong>堆</strong>：一般由程序员分配释放，若程序员不释放，结束时可能由系统回收，分配方式类似数据结构的链表。</li>
<li>非 <code>OC</code> 对象一般都放在操作系统的 <strong>栈</strong> 里边。</li>
<li>操作系统的 <strong>栈</strong>：由操作系统自动分配释放内存，存放函数的参数值、局部变量值等。其操作方式类似数据结构中的 <strong>栈（先进后出）</strong>。</li>
</ul>
<p>示例：<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">int main(int argc, const char *argv []) &#123;</span><br><span class="line">    @autoreleasepool &#123;</span><br><span class="line">        int a = 10;  // 栈</span><br><span class="line">        int b = 20;  // 栈</span><br><span class="line">        // p: 栈</span><br><span class="line">        // Person 对象（计数器 == 1）：堆</span><br><span class="line">        Person *p = [[Person alloc] init];</span><br><span class="line">    &#125;</span><br><span class="line">    // 经过上面代码后，栈里的变量 a、b、p 都会被回收</span><br><span class="line">    // 但是堆里的 Person 对象仍会留在内存中，因为它的计数器依然是 1</span><br><span class="line">    return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p><img src="https://i.loli.net/2019/10/22/C2Zypj5RcaDQJtv.png" alt="iOS内存管理-1"></p>
<blockquote>
<p>上述说法过于片面，其实并非所有 继承了 <code>NSObject</code> 的对象，存储在操作系统到 <strong>堆</strong> 里边，譬如创建的字符串有时候根据创建方式、位置，也会存储到 <strong>常量区</strong>。</p>
</blockquote>
<h4 id="二、内存管理模型"><a href="#二、内存管理模型" class="headerlink" title="二、内存管理模型"></a>二、内存管理模型</h4><p>提供给 <code>Objective-C</code> 程序员的内存管理模型有一下三种：</p>
<ul>
<li>自动垃圾收集（<code>iOS</code> 运行环境不支持）</li>
<li>手工引用计数和自动释放池（<code>MRC</code>）</li>
<li>自动引用计数（<code>ARC</code>）</li>
</ul>
<h4 id="三、-MRC-手动管理内存（Manual-Reference-Counting）"><a href="#三、-MRC-手动管理内存（Manual-Reference-Counting）" class="headerlink" title="三、 MRC 手动管理内存（Manual Reference Counting）"></a>三、 <code>MRC</code> 手动管理内存（<code>Manual Reference Counting</code>）</h4><h5 id="1-引用计数器"><a href="#1-引用计数器" class="headerlink" title="1. 引用计数器"></a>1. 引用计数器</h5><p>系统就是根据一个对象的引用计数器来判断什么时候需要回收一个对象的内存。</p>
<ul>
<li>引用计数器是一个整数</li>
<li>从字面意思，可以理解为一个对象被引用的次数。也可以理解为，有多少人正在引用这个对象</li>
<li>每个 <code>OC</code> 都有自己的引用计数器</li>
<li>任何一个对象，创建初始的引用计数都是1</li>
<li>当使用 <code>alloc</code> 、<code>new</code> 、<code>copy</code> 等关键词创建一个对象时，对象的引用计数默认就是1</li>
<li>当没有任何人使用这个对象时，该对象就会被回收，也就是说：<ul>
<li>当对象的引用计数为0时，该对象的内存地址就会被回收。</li>
<li>如果对象的引用计数不为0，那么在整个程序运行过程，它占用的内存就不可能被回收（除非整个程序已经退出）</li>
</ul>
</li>
</ul>
<h5 id="2-引用计数操作"><a href="#2-引用计数操作" class="headerlink" title="2. 引用计数操作"></a>2. 引用计数操作</h5><ul>
<li>为保证对象的存在，每当创建一个该对象的引用时，需要该对象发送一条 <code>retain</code> 消息，使该对象的引用计数 +1。（<code>retain</code> 方法返回对象本身）。</li>
<li>当不再需要该对象时，通过给对象发送 <code>release</code> 消息，可使对象的引用计数器 -1。</li>
<li>给对象发送 <code>retainCount</code> 消息，可以获得当前的引用计数器值</li>
<li>当对象的引用计数器为 0 时，系统就知道该对象已经不被引用了，可以回收该对象的内存了。这时会主动调用对象的 <code>dealloc</code> 方法发起整个回收过程。</li>
<li>需要注意的是，<code>release</code> 并不代表销毁或者回收队形，仅仅是引用计数器 <code>-1</code>。</li>
</ul>
<h5 id="3-dealloc-方法"><a href="#3-dealloc-方法" class="headerlink" title="3. dealloc 方法"></a>3. <code>dealloc</code> 方法</h5><ul>
<li>当一个对象的引用计数为 0 时，该对象即将被销毁，内存被回收。</li>
<li>对象即将被销毁时，系统会给对象发送一条 <code>dealloc</code> 消息，因此，也能够通过对象是否调用 <code>dealloc</code> 方法来判断是否被销毁。</li>
<li><code>dealloc</code> 方法的重写<ul>
<li>一般都会重写 <code>dealloc</code> 方法，在这里释放相关资源。<code>dealloc</code> 就是对象的遗言。</li>
<li>一旦重写了 <code>dealloc</code> 方法，就必须调用 <code>dealloc</code> 方法，并且放到最后面。</li>
</ul>
</li>
<li>注意，不能直接调用 <code>dealloc</code> 方法。一旦对象被回收了，其占用的内存就不能使用了，坚持使用会导致程序崩溃，即野指针错误。</li>
</ul>
<h5 id="4-野指针和空指针"><a href="#4-野指针和空指针" class="headerlink" title="4. 野指针和空指针"></a>4. 野指针和空指针</h5><ul>
<li>只要一个对象被销毁了，我们就称这个对象是 <code>僵尸对象（不能再使用的对象）</code>。</li>
<li>当一个指针指向僵尸对象（不可用内存），我们就称这个指针为野指针。</li>
<li><p>只要给一个野指针发送消息，就会引发崩溃。（<code>EXC_BAD_ACCESS</code>）</p>
  <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">int main(int argc, const char * argv[]) &#123;</span><br><span class="line">    @autoreleasepool &#123;</span><br><span class="line">        Person *p = [[Person alloc] init]; // 执行完引用计数为 1</span><br><span class="line">        [p release]; // 执行完引用计数为0，实例对象被释放</span><br><span class="line">        [p release]; // 此时，p就变成了野指针，再给野指针p发送消息就会报错</span><br><span class="line">        [p release];</span><br><span class="line">    &#125;</span><br><span class="line">    return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>为了防止野指针错误，一般情况下，当一个对象被销毁时，我们就将该对象的引用指针设为 <code>空指针</code>。</p>
</li>
<li>空指针<ul>
<li>没有指向内存空间的指针。里面存的是 <code>nil</code> ，也就是 <code>0</code>。</li>
<li>给空指针发送消息是没有任何回应的。<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">int main(int argc, const char * argv[]) &#123;</span><br><span class="line">    @autoreleasepool &#123;</span><br><span class="line">        Person *p = [[Person alloc] init]; // 执行完引用计数为1</span><br><span class="line">        [p release]; // 执行完引用计数为0，实例对象被释放</span><br><span class="line">        p = nil; // 此时，p变为了空指针</span><br><span class="line">        [p release]; // 再给空指针p发送消息就不会报错了</span><br><span class="line">        [p release];</span><br><span class="line">    &#125;</span><br><span class="line">    return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
</ul>
</li>
</ul>
<h5 id="5-内存管理规律"><a href="#5-内存管理规律" class="headerlink" title="5. 内存管理规律"></a>5. 内存管理规律</h5><p><code>单个对象内存管理规律</code></p>
<ul>
<li>谁创建，谁 <code>release</code> <ul>
<li>如果通过 <code>alloc</code> 、 <code>new</code> 、 <code>copy</code> 、 <code>mutableCopy</code> 来创建一个对象，那么就必须调用 <code>release</code> 或者 <code>autorelase</code>。</li>
</ul>
</li>
<li><p>谁 <code>retain</code> ，谁 <code>release</code></p>
<ul>
<li>只要调用了 <code>retain</code>，就必须调用 <code>release</code> 。</li>
</ul>
</li>
<li><p>总结一下：</p>
<ul>
<li>有加就有减</li>
<li>曾经让对象的引用计数 <code>+1</code> ，就必须在最后将对象的引用计数 <code>-1</code> 。</li>
</ul>
</li>
</ul>
<p><code>多个对象内存管理规律</code></p>
<ul>
<li>其实就是 <code>内存管理规律</code> 的多个使用。</li>
</ul>
<h5 id="6-property-参数"><a href="#6-property-参数" class="headerlink" title="6. @property 参数"></a>6. <code>@property</code> 参数</h5><ul>
<li>在成员变量前加上 <code>@property</code> ，系统就会帮我们生成基本的 <code>getter/setter</code> 方法。</li>
<li>如果在 <code>property</code> 属性用 <code>retain</code> 修饰，系统就会帮我们生成 <code>getter/setter</code> 方法的内存管理代码，但是仍需要我们自己重写 <code>dealloc</code> 代码。</li>
<li>如果 <code>property</code> 属性用 <code>assgin</code> 修饰，系统就不会帮我们生成 <code>setter</code> 方法的内存管理代码，仅仅只会生成普通的 <code>getter/setter</code> 方法。默认什么都不写就是 <code>assgin</code> 。</li>
</ul>
<h5 id="7-自动释放池"><a href="#7-自动释放池" class="headerlink" title="7. 自动释放池"></a>7. 自动释放池</h5><p>当我们不在使用一个对象的时候应该将其内存释放掉。但是有时候我们不知道应该何时将其释放，为了解决这个问题， <code>Objective-C</code> 提供了 <code>autorelease</code> 方法</p>
<ul>
<li><code>autorelease</code> 是一种支持引用计数的内存管理方式，只要给对象发送一条 <code>autorelease</code>消息，会将对象放到一个自动释放池中，当自动释放池被销毁时，会对池子里的所有对象做一次 <code>release</code> 操作。<blockquote>
<p>需要特别注意的是：这里只是发送 <code>release</code> 消息，如果对象的引用计数不为 0，该对象依然不会被销毁。</p>
</blockquote>
</li>
<li><code>autorelease</code> 方法会返回对象自身，且调用完 <code>autorelease</code> 后，对象的计数器不变。</li>
</ul>
<h6 id="7-1-使用-autorelease-有什么好处呢？"><a href="#7-1-使用-autorelease-有什么好处呢？" class="headerlink" title="7.1 使用 autorelease 有什么好处呢？"></a>7.1 使用 <code>autorelease</code> 有什么好处呢？</h6><ul>
<li>不在关心对象的释放时间</li>
<li>不在关心什么时候调用 <code>release</code> </li>
</ul>
<h6 id="7-2-autorelease-的原理实质上是什么？"><a href="#7-2-autorelease-的原理实质上是什么？" class="headerlink" title="7.2 autorelease 的原理实质上是什么？"></a>7.2 <code>autorelease</code> 的原理实质上是什么？</h6><p><code>autorelease</code> 实际上只是把 <code>release</code> 的调用延迟了，对于每一个 <code>autorelease</code> ,系统只是把对象放入了当前的 <code>autorelease pool</code> 中，当该 <code>pool</code> 被释放时，该 <code>pool</code> 中所有对象都会调用 <code>release</code> 。</p>
<h6 id="7-3-autorelease-的创建方法"><a href="#7-3-autorelease-的创建方法" class="headerlink" title="7.3 autorelease 的创建方法"></a>7.3 <code>autorelease</code> 的创建方法</h6><ul>
<li><p>使用 <code>NSAutoreleasePool</code> 来创建：</p>
 <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">NSAutoreleasePool *pool = [[NSAutoreleasePool alloc] init]; // 创建自动释放池</span><br><span class="line">[pool release]; // [pool drain]; 销毁自动释放池</span><br></pre></td></tr></table></figure>
</li>
<li><p>使用 <code>@autoreleasepool</code> 创建：</p>
  <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">@autoreleasepool</span><br><span class="line">&#123; //开始代表创建自动释放池</span><br><span class="line">&#125; //结束代表销毁自动释放池</span><br></pre></td></tr></table></figure>
</li>
</ul>
<h6 id="7-4-autorelease-的使用方法"><a href="#7-4-autorelease-的使用方法" class="headerlink" title="7.4 autorelease 的使用方法"></a>7.4 <code>autorelease</code> 的使用方法</h6><ul>
<li><p><code>NSAutoreleasePool</code> 用法：</p>
  <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">NSAutoreleasePool *autoreleasePool = [[NSAutoreleasePool alloc] init];</span><br><span class="line">Person *p = [[[Person alloc] init] autorelease];</span><br><span class="line">[autoreleasePool drain];</span><br></pre></td></tr></table></figure>
</li>
<li><p><code>@autoreleasepool</code> 用法:</p>
  <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">@autoreleasepool</span><br><span class="line">&#123; // 创建一个自动释放池</span><br><span class="line">    Person *p = [[Person new] autorelease];</span><br><span class="line">    // 将代码写到这里就放入了自动释放池</span><br><span class="line">&#125; // 销毁自动释放池(会给池子中所有对象发送一条release消息)</span><br></pre></td></tr></table></figure>
</li>
</ul>
<h6 id="7-5-autorelease-的注意事项"><a href="#7-5-autorelease-的注意事项" class="headerlink" title="7.5 autorelease 的注意事项"></a>7.5 <code>autorelease</code> 的注意事项</h6><ul>
<li><p>并不是放到自动释放池代码中，就会自动加入自动释放池</p>
  <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">@autoreleasepool &#123;</span><br><span class="line">    // 因为没有调用 autorelease 方法,所以对象没有加入到自动释放池</span><br><span class="line">    Person *p = [[Person alloc] init];</span><br><span class="line">    [p run];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>在自动释放池的外部调用 <code>autorelease</code> 不会被加入到自动释放池中。<code>autorelease</code> 是一个方法，只有在自动释放池中调用才有效。</p>
  <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">@autoreleasepool &#123;</span><br><span class="line">&#125;</span><br><span class="line">// 没有与之对应的自动释放池, 只有在自动释放池中调用autorelease才会放到释放池</span><br><span class="line">Person *p = [[[Person alloc] init] autorelease];</span><br><span class="line">[p run];</span><br><span class="line"></span><br><span class="line">// 正确写法</span><br><span class="line">@autoreleasepool &#123;</span><br><span class="line">    Person *p = [[[Person alloc] init] autorelease];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">// 正确写法</span><br><span class="line">Person *p = [[Person alloc] init];</span><br><span class="line">@autoreleasepool &#123;</span><br><span class="line">    [p autorelease];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
</ul>
<h6 id="7-6-自动释放池的嵌套调用"><a href="#7-6-自动释放池的嵌套调用" class="headerlink" title="7.6 自动释放池的嵌套调用"></a>7.6 自动释放池的嵌套调用</h6><ul>
<li>自动释放池是以栈的形式存在。</li>
<li><p>由于栈只有一个入口，所以调用 <code>autorelease</code> 时会将对象放到栈顶的自动释放池。栈顶就是离调用 <code>autorelease</code> 最近的自动释放池。</p>
  <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">@autoreleasepool &#123; // 栈底自动释放池</span><br><span class="line">    @autoreleasepool &#123;</span><br><span class="line">        @autoreleasepool &#123; // 栈顶自动释放池</span><br><span class="line">            Person *p = [[[Person alloc] init] autorelease];</span><br><span class="line">        &#125;</span><br><span class="line">        Person *p = [[[Person alloc] init] autorelease];</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>自动释放池内不宜放占用内存比较大的对象</p>
<ul>
<li>尽量避免对大内存使用该方法，对这种延迟释放机制，还是尽量少用。</li>
<li>不要把大量循环操作放到一个 <code>autoreleasepool</code> 之间，这样会造成内存峰值的上升。<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">// 内存暴涨</span><br><span class="line">@autoreleasepool &#123;</span><br><span class="line">    for (int i = 0; i &lt; 99999; ++i) &#123;</span><br><span class="line">        Person *p = [[[Person alloc] init] autorelease];</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">// 内存不会暴涨</span><br><span class="line">for (int i = 0; i &lt; 99999; ++i) &#123;</span><br><span class="line">    @autoreleasepool &#123;</span><br><span class="line">        Person *p = [[[Person alloc] init] autorelease];</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
</ul>
</li>
</ul>
<h6 id="7-7-autorelease-错误用法"><a href="#7-7-autorelease-错误用法" class="headerlink" title="7.7 autorelease 错误用法"></a>7.7 <code>autorelease</code> 错误用法</h6><ul>
<li><p>不要连续使用 <code>autorelease</code></p>
  <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">@autoreleasepool &#123;</span><br><span class="line">    // 错误写法, 过度释放</span><br><span class="line">    Person *p = [[[[Person alloc] init] autorelease] autorelease];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>调用 <code>autorelease</code> 后又调用 <code>release</code></p>
  <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">@autoreleasepool &#123;</span><br><span class="line">    Person *p = [[[Person alloc] init] autorelease];</span><br><span class="line">    [p release]; // 错误写法, 过度释放</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
</ul>
<h5 id="8-MRC-中避免循环-retian"><a href="#8-MRC-中避免循环-retian" class="headerlink" title="8. MRC 中避免循环 retian"></a>8. <code>MRC</code> 中避免循环 <code>retian</code></h5><ul>
<li>不要让 <code>A</code> <code>retain</code> <code>B</code>，<code>B</code> <code>retain</code> <code>A</code>。</li>
<li>让其中一方不要做 <code>retain</code> 操作即可。</li>
<li>当两端互相引用时，应该一端用 <code>retain</code>，一端用 <code>assign</code> 。</li>
</ul>
<h4 id="四、ARC-自动管理内存（Automatic-Reference-Counting）"><a href="#四、ARC-自动管理内存（Automatic-Reference-Counting）" class="headerlink" title="四、ARC 自动管理内存（Automatic Reference Counting）"></a>四、<code>ARC</code> 自动管理内存（<code>Automatic Reference Counting</code>）</h4><ul>
<li><code>Automatic Reference Counting</code>，自动引用计数，即 <code>ARC</code>，<code>WWDC2011</code> 和 <code>iOS5</code> 所引用的最大变革和最激动人心的变化。<code>ARC</code> 是新的 <code>LLVM3.0</code>  编译器的一项特性，使用 <code>ARC</code>，可以说是一举解决了广大 <code>iOS</code> 开发者所憎恶的手动内存管理的麻烦。</li>
<li>使用 <code>ARC</code> 后，系统会检测出何时需要保持对象，何时需要自动释放对象，何时需要释放对象，编译器会管理好对象的内存，会在合适的地方插入<code>retain</code> 、 <code>release</code> 、 <code>autorelease</code> ，通过生成正确的代码，去自动释放或者保持对象。</li>
</ul>
<h5 id="4-1-ARC-的判断原则"><a href="#4-1-ARC-的判断原则" class="headerlink" title="4.1 ARC 的判断原则"></a>4.1 <code>ARC</code> 的判断原则</h5><p><code>ARC</code> 通过判断对象是否被 <code>强指针</code> 引用来决定引用计数是否增加。</p>
<ul>
<li>强指针<ul>
<li>默认所有对象的指针变量都是强指针</li>
<li>被 <code>__strong</code> 修饰的指针</li>
</ul>
</li>
<li>弱指针<ul>
<li>被 <code>__weak</code> 修饰的指针<h5 id="4-2-ARC-的使用"><a href="#4-2-ARC-的使用" class="headerlink" title="4.2 ARC 的使用"></a>4.2 <code>ARC</code> 的使用</h5><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">int main(int argc, const char * argv[]) &#123;</span><br><span class="line">    // 不用写release, main函数执行完毕后p会被自动释放</span><br><span class="line">    Person *p = [[Person alloc] init];</span><br><span class="line"></span><br><span class="line">    return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
</ul>
</li>
</ul>
<h5 id="4-3-ARC-的注意点"><a href="#4-3-ARC-的注意点" class="headerlink" title="4.3 ARC 的注意点"></a>4.3 <code>ARC</code> 的注意点</h5><ul>
<li>不允许调用对象的 <code>release</code> 方法</li>
<li>不允许调用 <code>autorelease</code> 方法</li>
<li>重写父类的 <code>dealloc</code> 时，不能在调用 <code>[super deallock]</code></li>
</ul>
<h5 id="4-4-ARC-下对象内存管理"><a href="#4-4-ARC-下对象内存管理" class="headerlink" title="4.4 ARC 下对象内存管理"></a>4.4 <code>ARC</code> 下对象内存管理</h5><ul>
<li><p>局部变量释放，对象随之被释放</p>
  <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">int main(int argc, const char * argv[]) &#123;</span><br><span class="line">    @autoreleasepool &#123;</span><br><span class="line">        Person *p = [[Person alloc] init];</span><br><span class="line">    &#125; // 执行到这一行局部变量p释放</span><br><span class="line">    // 由于没有强指针指向对象, 所以对象也释放</span><br><span class="line">    return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>清空指针，对象随之被释放</p>
  <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">int main(int argc, const char * argv[]) &#123;</span><br><span class="line">    @autoreleasepool &#123;</span><br><span class="line">        Person *p = [[Person alloc] init];</span><br><span class="line">        p = nil; // 执行到这一行, 由于没有强指针指向对象, 所以对象被释放</span><br><span class="line">    &#125;</span><br><span class="line">    return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>默认所有指针都是强指针</p>
  <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">int main(int argc, const char * argv[]) &#123;</span><br><span class="line">    @autoreleasepool &#123;</span><br><span class="line">        // p1和p2都是强指针</span><br><span class="line">        Person *p1 = [[Person alloc] init];</span><br><span class="line">        __strong Person *p2 = [[Person alloc] init];</span><br><span class="line">    &#125;</span><br><span class="line">    return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>弱指针需要明确说明(注意：千万不要使用弱指针保存薪创建的对象)</p>
  <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">int main(int argc, const char * argv[]) &#123;</span><br><span class="line">    @autoreleasepool &#123;</span><br><span class="line">        // p是弱指针, 对象会被立即释放</span><br><span class="line">        __weak Person *p1 = [[Person alloc] init];</span><br><span class="line">    &#125;</span><br><span class="line">    return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
</ul>
<h5 id="4-5-ARC-下多对象内存管理"><a href="#4-5-ARC-下多对象内存管理" class="headerlink" title="4.5 ARC 下多对象内存管理"></a>4.5 <code>ARC</code> 下多对象内存管理</h5><p><code>ARC</code> 和 <code>MRC</code> 一样，想拥有某个对象必须用强指针保存对象，但是不需要在 <code>dealloc</code> 中 <code>release</code>。<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">@interface Person : NSObject</span><br><span class="line">// MRC写法</span><br><span class="line">//@property (nonatomic, retain) Dog *dog;</span><br><span class="line"></span><br><span class="line">// ARC写法</span><br><span class="line">@property (nonatomic, strong) Dog *dog;</span><br><span class="line">@end</span><br></pre></td></tr></table></figure></p>
<h5 id="4-6-ARC-中的-property-参数"><a href="#4-6-ARC-中的-property-参数" class="headerlink" title="4.6 ARC 中的 @property 参数"></a>4.6 <code>ARC</code> 中的 <code>@property</code> 参数</h5><ul>
<li><code>strong</code>: 用于 <code>OC</code> 对象，相当于 <code>MRC</code> 中的 <code>retain</code></li>
<li><code>weak</code>: 用于 <code>OC</code> 对象，相当于 <code>MRC</code> 中的 <code>assign</code></li>
<li><code>assign</code>: 用于基本数据类型，跟 <code>MRC</code> 中 <code>assign</code> 一样。</li>
</ul>
<h5 id="4-7-ARC-下循环引用问题"><a href="#4-7-ARC-下循环引用问题" class="headerlink" title="4.7 ARC 下循环引用问题"></a>4.7 <code>ARC</code> 下循环引用问题</h5><p><code>ARC</code> 跟 <code>MRC</code> 一样，如果 <code>A</code> 拥有 <code>B</code>，<code>B</code> 拥有 <code>A</code>，那么必须一方使用弱指针。<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">@interface Person : NSObject</span><br><span class="line">@property (nonatomic, strong) Dog *dog;</span><br><span class="line">@end</span><br><span class="line"></span><br><span class="line">@interface Dog : NSObject</span><br><span class="line">// 错误写法, 循环引用会导致内存泄露</span><br><span class="line">//@property (nonatomic, strong) Person *owner;</span><br><span class="line"></span><br><span class="line">// 正确写法, 当如果保存对象建议使用weak</span><br><span class="line">@property (nonatomic, weak) Person *owner;</span><br><span class="line">@end</span><br></pre></td></tr></table></figure></p>
<h3 id="后记"><a href="#后记" class="headerlink" title="后记"></a>后记</h3><p>最近两年一直在使用 <code>Swift</code>，对 <code>OC</code> 的一些基础知识遗忘了不少，通过这篇博客算是温习一些。接下来打算在细读一遍 《Objective-C高级编程》，来对 <code>ARC</code> 、 <code>Blocks</code> 、 <code>GCD</code> 做一个系统的复习。</p>
]]></content>
      <tags>
        <tag>iOS,内存,面试</tag>
      </tags>
  </entry>
  <entry>
    <title>iOS内存分配：堆、栈、全局区、常量区、代码区</title>
    <url>/2019/11/01/iOS%E5%86%85%E5%AD%98%E5%88%86%E9%85%8D%EF%BC%9A%E5%A0%86%E3%80%81%E6%A0%88%E3%80%81%E5%85%A8%E5%B1%80%E5%8C%BA%E3%80%81%E5%B8%B8%E9%87%8F%E5%8C%BA%E3%80%81%E4%BB%A3%E7%A0%81%E5%8C%BA/</url>
    <content><![CDATA[<h3 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h3><p>内容参考：</p>
<p><a href="https://blog.csdn.net/zhw521411/article/details/89844273" target="_blank" rel="noopener">iOS 内存分配 栈、堆、全局区、常量区、代码区
</a></p>
<a id="more"></a>
<p><a href="https://www.jianshu.com/p/df630e78df32" target="_blank" rel="noopener">NSString存储管理–NSTaggedPointerString</a></p>
<p><a href="https://www.jianshu.com/p/746c747e7e00" target="_blank" rel="noopener">iOS开发中的内存分配（堆和栈）</a></p>
<h3 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h3><p><img src="https://i.loli.net/2019/10/23/vwNbsckyQJXUg64.png" alt="iOS内存分配.png"></p>
<h4 id="1-代码区"><a href="#1-代码区" class="headerlink" title="1. 代码区"></a>1. 代码区</h4><p>代码区是用来存放函数的二进制代码，也就是，它是可执行程序在内存中的镜像。代码段需要防止在运行时被非法修改，所以只允许读取操作，而不允许写入操作。</p>
<h4 id="2-全局（静态）区"><a href="#2-全局（静态）区" class="headerlink" title="2. 全局（静态）区"></a>2. 全局（静态）区</h4><ul>
<li>数据区：数据段用来存放可执行文件中已经初始化的全局变量，也就是用来存放静态分配的变量和全局变量。</li>
<li>BSS区：BSS段包含了程序中未初始化的全局变量</li>
</ul>
<h4 id="3-常量区"><a href="#3-常量区" class="headerlink" title="3. 常量区"></a>3. 常量区</h4><p>常量存储区，这是一块比较特殊的存储区，他们里面存放的是常量。</p>
<h4 id="4-堆（heap）区"><a href="#4-堆（heap）区" class="headerlink" title="4. 堆（heap）区"></a>4. 堆（heap）区</h4><p>堆是由程序员分配和释放，用于存放进程运行中被动态分配的内存段。它大小不固定，可动态扩张和缩减。</p>
<h4 id="5-栈（stack）区"><a href="#5-栈（stack）区" class="headerlink" title="5. 栈（stack）区"></a>5. 栈（stack）区</h4><p>栈是由编译器自动分配释放来管理内存。用户存放程序临时创建的变量、存放函数的参数值、局部变量等。由于栈的先进后出特点，所以特别适合用来做保存/恢复现场的操作。从这个吧意义上，我们可以把栈看做一个临时寄存、交换的内存区。</p>
<p>static 修饰的属性始终保存到常量区。</p>
<blockquote>
<p>上述几种内存区域中，数据段、BSS、堆通常都是被连续存储的-内存位置上的连续（并不是堆链式存储的内存区域）。而代码段和栈往往会被独立存放。</p>
</blockquote>
<blockquote>
<p>栈是向低地址扩展的数据结构，是一块连续的内存区域。堆是向高地址扩展的数据结构，是不连续的内存区域。</p>
</blockquote>
<h3 id="内存模型-Heap、Stack"><a href="#内存模型-Heap、Stack" class="headerlink" title="内存模型 Heap、Stack"></a>内存模型 Heap、Stack</h3><p>以下内容引用自阮一峰老师的 <a href="http://www.ruanyifeng.com/blog/2018/01/assembly-language-primer.html" target="_blank" rel="noopener">汇编语言入门教程</a>。</p>
<p>对于堆 <code>heap</code> 和 栈 <code>stack</code> 描述更加详细易懂。</p>
<h4 id="1-堆-Heap"><a href="#1-堆-Heap" class="headerlink" title="1. 堆 Heap"></a>1. 堆 <code>Heap</code></h4><p>寄存器只能存放少量的数据，大多数时候， <code>CPU</code> 还要指挥寄存器，直接跟内存交换数据，所以，除了寄存器，还必须了解内存怎么存储数据。</p>
<p>程序运行的时候，操作系统会给它分配一块内存，用来存储程序和运行产生的数据，比如从 <code>0x1000</code> 到 <code>0x8000</code>，起始地址是较小的那个地址，结束地址是较大的那个地址。</p>
<p><img src="https://i.loli.net/2019/10/31/LTUFinQrWfgskIM.png" alt="内存示意图"></p>
<p>程序运行过程中，对于动态的内存占用请求（比如新建对象），系统就会从预先分配好的那段内存中，划出一部分给用户，具体规则是从起始地址开始划分（实际上，起始地址会有一段静态数据，这里忽略）。举例来说，用户要求得到10个字节内存，那么从起始地址 <code>0x1000</code> 开始给他分配，一直分配到 <code>0x100A</code>，如果在要求得到 <code>22</code> 个字节，那么就分配到 <code>0x1020</code>。</p>
<p><img src="https://i.loli.net/2019/10/31/A5F9RSraq3zZy14.png" alt="内存分配"></p>
<p>这种因为用户主动请求而划分出来的内存区域，叫做 <code>Heap(堆)</code>。它由起始位置开始，从低位（地址）向高位（地址）增长。 <code>Heap</code> 的一个重要特点就是不会自动消失，必须手动释放，或者由垃圾回收机制来回收。</p>
<h4 id="2-栈-Stack"><a href="#2-栈-Stack" class="headerlink" title="2. 栈 Stack"></a>2. 栈 <code>Stack</code></h4><p>除了 <code>Heap</code> 外，其他的内存占用叫做 <code>Stack(栈)</code>。简单来说，<code>Stack</code> 是由于函数运行而临时占用的内存区域。 </p>
<p><img src="https://i.loli.net/2019/10/31/rDStKFxY2gnJQPM.png" alt="栈内存"></p>
<p>请看下面的例子：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">int main() &#123;</span><br><span class="line">    int a = 2;</span><br><span class="line">    int b = 3;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>上面代码中，系统开始执行 <code>main</code> 函数时，会为它在内存里面建立一个 <code>帧（frame）</code>，所有的 <code>main</code> 的内部变量（比如 <code>a</code> 和 <code>b</code>）都保存在这个 <code>帧</code> 里面。<code>main</code> 函数执行结束后，该帧就会被回收，释放所有的内部变量，不再占用空间。</p>
<p><img src="https://i.loli.net/2019/10/31/c1aJKyxk6AXub5G.png" alt="帧（frame）"></p>
<p>如果函数内部调用其他函数，会发生什么情况呢？</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">int main() &#123;</span><br><span class="line">    int a = 2;</span><br><span class="line">    int b = 3;</span><br><span class="line">    return add_a_and_b(a,b);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>上面代码中，<code>main</code> 函数调用了 <code>add_a_and_b</code> 函数。执行到这一行的时候，系统也会为 <code>add_a_and_b</code> 新建一个 <code>帧（frame）</code>，用来存储它的内部变量。也就是说，此时同时存在两个帧：<code>main</code> 和 <code>add_a_and_b</code>。一般来说，调用栈有多少层，就有多少帧。</p>
<p><img src="https://i.loli.net/2019/10/31/asmhyVkiEDQFNJL.png" alt="函数嵌套"></p>
<p>等到 <code>add_a_and_b</code> 运行结束，它的帧就会被回收，系统会回到刚才 <code>main</code> 函数中断执行的地方，继续往下执行。通过这种机制，就实现了函数的层层调用，并且每一层都能使用自己的本地变量。</p>
<p>所有的帧都存放在 <code>Stack</code> ，由于帧是一层层叠加的，所以 <code>Stack</code> 叫做 <strong>栈</strong>。生成新的帧，叫 <code>入栈</code>，英文单词是 <code>push</code>；栈的回收叫 <code>出栈</code>，英文是 <code>pop</code>。<code>Stack</code> 的特点就是，最晚入栈的帧最早出栈（因为最内层的函数调用，最先结束执行），这种叫做 <strong>后进先出</strong> 的数据结构。每一次函数执行结束，就自动释放一个帧，所有的函数执行结束，整个 <code>Stack</code> 就都释放了。</p>
<p><code>Stack</code> 是由内存区域的结束地址开始，从高位（地址）向地位（地址）分配。比如，内存区域的结束地址是 <code>0x8000</code>，第一帧假定是 <code>16</code> 字节，那么下一次分配的地址就会从 <code>0x7FF0</code> 开始；第二帧假定需要 <code>64</code> 字节，那么地址就会移到 <code>0x7FB0</code>。</p>
<p><img src="https://i.loli.net/2019/10/31/7h8ewTifNRcLmyr.png" alt="栈内存地址分配"></p>
<h3 id="关于-NSString-的内存分配"><a href="#关于-NSString-的内存分配" class="headerlink" title="关于 NSString 的内存分配"></a>关于 <code>NSString</code> 的内存分配</h3><p><a href="https://www.jianshu.com/p/df630e78df32" target="_blank" rel="noopener">NSString存储管理–NSTaggedPointerString</a> 文章中详细说明了问题，我们并不能单纯的通过打印内存地址，来判断数据存储的区域在常量，或者堆区、栈区。</p>
]]></content>
      <tags>
        <tag>iOS,Objective-C,内存</tag>
      </tags>
  </entry>
  <entry>
    <title>The Swift Programming Language 重读笔记</title>
    <url>/2019/10/26/The%20Swift%20Programming%20Language%20%E9%87%8D%E8%AF%BB%E7%AC%94%E8%AE%B0%C2%B7/</url>
    <content><![CDATA[<h3 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h3><p><code>SwiftGG</code> 翻译组的 <a href="https://swiftgg.gitbook.io/swift/" target="_blank" rel="noopener">《The Swift Programming Language》in Chinese</a> 我在 <code>Swift3</code> 的时候通读过一遍，在 <code>Swift4</code> 的时候只浏览了前半部分，并未通读。现在 <code>Swift5</code> 准备再重新读一遍文档，把一些遗忘的、易忘的、感觉值得记下的知识点梳理一遍，以方便记忆。</p>
<a id="more"></a> 
<h4 id="基础部分"><a href="#基础部分" class="headerlink" title="基础部分"></a>基础部分</h4><ol>
<li><p>一般来说，很少需要写类型注释(type animation)，如果在声明常量或者变量时赋了一个初始值， <code>Swift</code> 可以推断出这个常量或变量的类型。</p>
</li>
<li><p>如果需要使用 <code>Swift</code> 保留关键字相同的名称作为常量或变量名，可以使用反引号将关键字包围的方式将其作为名字使用。无论如何，我们都应当避免使用关键字作为常量或变量名，除非别无选择。</p>
</li>
<li><p>当遇到一些相关值得简单分组时，元组是很有用的。元组不适合用来创建复杂的数据结构。如果数据结构比较复杂，不要使用元组，用类或者结构体去建模。</p>
</li>
<li><p><code>if</code> 与 <code>guard</code> 用户区别：在 <code>if</code> 条件语句中使用常量或变量来创建一个可选绑定，仅在 <code>if</code> 语句的句中 <code>body</code> 中才能获取到值。在 <code>guard</code> 语句中使用常量或变量创建一个可选绑定时，仅在 <code>guard</code> 语句外且在语句后才能获取到值。</p>
</li>
<li><p>断言和先决条件的不同点是，他们什么时候进行状态检测：断言仅在调试环境运行，而先决条件则在调试环境和生产环境中运行。在生产环境中，断言的条件将不进行评估。这意味着我们可以再开发阶段使用很多断言，但是这些断言再生产环境都不会产生影响。</p>
</li>
</ol>
<h4 id="基本运算符"><a href="#基本运算符" class="headerlink" title="基本运算符"></a>基本运算符</h4><ol>
<li><p>在对负数 <code>b</code> 求余时，<code>b</code> 的符号会被忽略。这意味着 <code>a % -b</code> 和 <code>a % b</code> 是一样的结果。但是 <code>-a % b</code> 与 <code>a % b</code> 结果并不一样。</p>
</li>
<li><p>一元正负号 <code>+</code> 不做任何改变的返回操作数的值。 虽然一元操作符什么都不会改变，但当使用一元负号 <code>-</code> 来表达负数时，可以使用一元正好来表达正数，使代吗具有对称美。</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> minusSix = -<span class="number">6</span></span><br><span class="line"><span class="keyword">let</span> alsoMinusSix = +minusSix  <span class="comment">// alsoMinusSix 等于 -6</span></span><br></pre></td></tr></table></figure>
</li>
</ol>
<ol start="3">
<li><p>如果两个元组的元素相同，且长度相同的话，元组就可以被比较，比较元组大小会按照从左到右、逐值比较的方式，直到发现两个值不相等时停止，如果所有值都相等，那么我们就称这一对元组是相等的。<code>Swift</code> 标准库只能比较七个以内元素的元组。如果元组元素超过七个，则需要自己实现比较运算符。</p>
 <figure class="highlight swift"><table><tr><td class="code"><pre><span class="line">(<span class="number">1</span>, <span class="string">"zebra"</span>) &lt; (<span class="number">2</span>, <span class="string">"apple"</span>)   <span class="comment">// true，因为 1 小于 2</span></span><br><span class="line">(<span class="number">3</span>, <span class="string">"apple"</span>) &lt; (<span class="number">3</span>, <span class="string">"bird"</span>)    <span class="comment">// true，因为 3 等于 3，但是apple 小于 bird</span></span><br><span class="line">(<span class="number">4</span>, <span class="string">"dog"</span>) == (<span class="number">4</span>, <span class="string">"dog"</span>)      <span class="comment">// true，因为 4 等于 4，dog 等于 dog</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>空合运算符 <code>??</code>。ps. 其实我一直我不清除这个叫啥名字。。</p>
</li>
</ol>
<h4 id="字符串和字符"><a href="#字符串和字符" class="headerlink" title="字符串和字符"></a>字符串和字符</h4><ol>
<li><p>多行字符串字面量，是由一对三个双引号包裹着的具有固定顺序的文本字符集。也可以在行尾写一个反斜杠 \ 作为续行符。</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> softWrappedQuotation = <span class="string">""</span><span class="string">"</span></span><br><span class="line"><span class="string">   The White Rabbit put on his spectacles.  "</span><span class="type">Where</span> shall <span class="type">I</span> begin, \</span><br><span class="line">   please your <span class="type">Majesty</span>?<span class="string">" he asked.</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">   "</span><span class="type">Begin</span> at the beginning,<span class="string">" the King said gravely, "</span>and go on \</span><br><span class="line">   till you come to the end; then stop.<span class="string">"</span></span><br><span class="line"><span class="string">   "</span><span class="string">""</span></span><br></pre></td></tr></table></figure>
</li>
</ol>
<ol start="2">
<li>关于 <code>字符串字面量的特殊字符</code>，主要是转义字符、<code>Unicode</code> 标量。平时使用较少。还有转义字符反斜杠 \ 的使用。</li>
</ol>
<ol start="3">
<li><p><code>扩展字符串分隔符</code> <code>#</code>。将字符串文字放在扩展分隔符中，这样字符串中的特殊字符将会被直接包含而不是转移后的效果。如果想要转义字符效果，用等量 <code>#</code> 包裹住即可。</p>
</li>
<li><p>字符串、结构体、枚举，都是值类型。</p>
</li>
<li><p>字符串知识中，关于 <code>Unicode</code> ，比较零碎，实际开发中也很少用到，已读。</p>
</li>
<li><p>关于计算字符数量，需要注意的是通过 <code>count</code> 属性返回的字符数量并不总是与包含相同自负的 <code>NSString</code> 的 <code>length</code> 属性相同。 <code>NSString</code> 的 <code>length</code> 属性是利用 <code>UTF-16</code> 表示的十六位代码单元数字，而不是 <code>Unicode</code> 可扩展的字符群集。</p>
</li>
<li><p>不同的字符可能会占用不同数量的存储空间，要以要知道字符串中 <code>Character</code> 的确定位置，就必须从 <code>String</code> 开头遍历每一个 <code>Unicode</code> 标量到结尾。因此， <code>Swift</code> 的字符串不能整数 <code>integer</code> 做索引。</p>
</li>
<li><p>子字符串 <code>SubString</code> 和 <code>String</code> 区别在于性能优化上。<code>SubString</code> 可以重用原 <code>String</code> 的内存空间，或者另一个 <code>SubString</code> 的内存空间（<code>String</code> 也有同样的优化，但如果两个 <code>String</code> 共享内存的话，它们就会相等）。这一优化意味着你在修改 <code>String</code> 和 <code>SubString</code> 之前都不需要消耗性能去复制内存。就像前面说的那样，<code>SubString</code> 不适合长期存储 —— 因为它重用了原 <code>String</code> 的内存空间，原 <code>String</code> 的内存空间必须保留直到它的 <code>SubString</code> 不再被使用为止。</p>
</li>
</ol>
<h4 id="集合类型"><a href="#集合类型" class="headerlink" title="集合类型"></a>集合类型</h4><ol>
<li><p>关于数组，每天都在用，要强调的不多。这里说三个方法及区别：<code>removeLast()</code> ，移除数组最后一个元素并返回该元素，数组必须不为空，否则会引发运行时崩溃； <code>popLast()</code> ，移除数组最后一个可选类型的元素，也就是说，数组可以为空； <code>dropLast()</code> ，移除掉最后几个元素，并返回移除后的原数组。</p>
 <figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> digitArr = [<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>,<span class="number">5</span>]</span><br><span class="line"><span class="keyword">var</span> last1 = digitArr.removeLast()</span><br><span class="line"><span class="keyword">var</span> last2 = digitArr.popLast()</span><br><span class="line"><span class="keyword">var</span> remain = digitArr.<span class="built_in">dropLast</span>(<span class="number">2</span>)</span><br><span class="line"><span class="built_in">print</span>(last1, last2 ?? <span class="string">"-2"</span>, remain)</span><br><span class="line"><span class="comment">// 输出： 5 4 [1]</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>集合 <code>Set</code>。用来存储相同类型并且没有顺序的值。当集合元素顺序不重要并且确保每个元素只出现一次时，可以使用集合而不是数组。可自己定义类型作为集合的值类型，但是定义的类型要遵循 <code>Hasshable</code> 协议，而 <code>Hashable</code> 协议遵循了 <code>Equatable</code> 协议，所以定义的类型符合这些协议即可。</p>
</li>
<li><p>集合提供了一些方法可以高效完成对集合的操作。</p>
<ul>
<li>使用 <code>intersection(_:)</code> 方法根据两个集合的交集创建一个新的集合。</li>
<li>使用 <code>symmetricDifference(_:)</code> 方法根据两个集合不相交的值创建一个新的集合。</li>
<li>使用 <code>union(_:)</code> 方法根据两个集合的所有值创建一个新的集合。</li>
<li>使用 <code>subtracting(_:)</code> 方法根据不在另一个集合中的值创建一个新的集合。</li>
</ul>
</li>
<li><p>集合还提供了方法来判断集合之间的关系：</p>
<ul>
<li>使用“是否相等”运算符 <code>==</code> 来判断两个集合包含的值是否全部相同。</li>
<li>使用 <code>isSubset(of:)</code> 方法来判断一个集合中的所有值是否也被包含在另外一个集合中。</li>
<li>使用 <code>isSuperset(of:)</code> 方法来判断一个集合是否包含另一个集合中所有的值。</li>
<li>使用 <code>isStrictSubset(of:)</code> 或者 <code>isStrictSuperset(of:)</code> 方法来判断一个集合是否是另外一个集合的子集合或者父集合并且两个集合并不相等。</li>
<li>使用 <code>isDisjoint(with:)</code> 方法来判断两个集合是否不含有相同的值（是否没有交集）。</li>
</ul>
</li>
</ol>
<ol start="4">
<li><p>字典 <code>Dictionary</code> 是一个无序集合，存储键值之间的关系。为了以特定的顺序遍历字典的键或值，可以对字典的 <code>keys</code> 或 <code>values</code> 使用 <code>sorted()</code> 方法。如果想要移除字典中的相关元素，可以可以使用如下两种方式：</p>
 <figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> ages = [<span class="string">"Tom"</span>: <span class="number">20</span>, <span class="string">"jack"</span>: <span class="number">12</span>, <span class="string">"Lilei"</span>: <span class="number">18</span>]</span><br><span class="line">ages.removeValue(forKey: <span class="string">"Tom"</span>)</span><br><span class="line"><span class="built_in">print</span>(ages)</span><br><span class="line"><span class="comment">/// ["Lilei": 18, "jack": 12]</span></span><br><span class="line">ages[<span class="string">"KK"</span>] = <span class="literal">nil</span></span><br><span class="line"><span class="built_in">print</span>(ages)</span><br><span class="line"><span class="comment">/// ["Lilei": 18, "jack": 12]</span></span><br><span class="line">ages[<span class="string">"jack"</span>] = <span class="literal">nil</span></span><br><span class="line"><span class="built_in">print</span>(ages)</span><br><span class="line"><span class="comment">/// ["Lilei": 18]</span></span><br></pre></td></tr></table></figure>
</li>
</ol>
<h4 id="控制流"><a href="#控制流" class="headerlink" title="控制流"></a>控制流</h4><ol>
<li><p><code>Swift</code> 提供了多种流程控制结构，包括可以多次执行任务的 <code>While</code> 循环，基于特定条件选择执行不同代码分支的 <code>if</code> 、 <code>guard</code> 、 <code>Switch</code> 语句，还有控制流程跳转到其他位置的 <code>break</code> 和 <code>continue</code> 语句。</p>
<p> <code>Swift</code> 还提供了 <code>for-in</code> 循环，用来更简单的遍历数组、字典、区间、字符串和其他序列类型。</p>
</li>
<li><p><code>for-in</code> 循环中，值得注意的有一点： <code>stride(from:to:by:)</code> 用于半开半闭区间， <code>stride(from:through:by:)</code> 用于闭区间，都是间隔循环。示例如下：</p>
 <figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> minutes = <span class="number">15</span></span><br><span class="line"><span class="keyword">let</span> minuteInterval = <span class="number">5</span></span><br><span class="line"><span class="keyword">for</span> tickMark <span class="keyword">in</span> <span class="built_in">stride</span>(from: <span class="number">0</span>, to: minutes, by: minuteInterval) &#123;</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">"tickMark = "</span>, tickMark)</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">/// 输出： 0  5  10</span></span><br><span class="line"><span class="keyword">for</span> tickMark <span class="keyword">in</span> <span class="built_in">stride</span>(from: <span class="number">0</span>, through: minutes, by: minuteInterval) &#123;</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">"tickMark2 = "</span>, tickMark)</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">/// 输出： 0  5  10  15</span></span><br></pre></td></tr></table></figure>
</li>
<li><p><code>while</code> 循环从计算一个条件开始，如果条件为 <code>true</code> ，会重复运行一段代码，直到条件变为 <code>false</code>。<code>repeat-while</code> 和 <code>while</code> 的区别在于判断循环条件之前，先执行一次循环的代码块。然后重复循环直到条件为 <code>false</code>。</p>
</li>
<li><p><code>Switch</code> 在 <code>Swift</code> 中是非常强大、灵活的。支持区间匹配、元组匹配、值绑定、<code>where</code> 条件、复合型 <code>case</code> 以及包含相同值绑定的复合型 <code>case</code> 等。</p>
</li>
<li><p><code>Swift</code> 共有五种控制转移语句：<br> <code>continue</code>，<code>break`</code>fallthrough<code>，</code>return<code>，</code>throw`。</p>
<ul>
<li><p><code>continue</code> ：告诉一个循环立即停止本次循环，重新开始下一次的循环。但是不会离开整个循环。</p>
</li>
<li><p><code>break</code>：立刻结束整个控制流的执行。 <code>break</code> 在循环中，是结束该循环。在 <code>Switch</code> 中，是结束该 <code>Switch</code>。</p>
</li>
<li><p><code>fallthrough</code>：在 <code>switch</code> 中使用，该关键字不会检查它下一个将会落入执行的 <code>case</code> 中的匹配条件。<code>fallthrough</code> 简单地使代码继续连接到下一个 <code>case</code> 中的代码。这和 <code>C</code> 语言标准中的 <code>switch</code> 特性是一样的。</p>
</li>
</ul>
</li>
</ol>
<ol start="6">
<li><p>带标签的语句。这个我个人确实没有用到过。这里记一下。简单来说，就是给语句命名，然后控制转移语句直接针对特性语句做处理，下面是代码：</p>
 <figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> finalSquare = <span class="number">25</span></span><br><span class="line"><span class="keyword">var</span> board = [<span class="type">Int</span>](repeating: <span class="number">0</span>, <span class="built_in">count</span>: finalSquare + <span class="number">1</span>)</span><br><span class="line">board[<span class="number">03</span>] = +<span class="number">08</span>; board[<span class="number">06</span>] = +<span class="number">11</span>; board[<span class="number">09</span>] = +<span class="number">09</span>; board[<span class="number">10</span>] = +<span class="number">02</span></span><br><span class="line">board[<span class="number">14</span>] = -<span class="number">10</span>; board[<span class="number">19</span>] = -<span class="number">11</span>; board[<span class="number">22</span>] = -<span class="number">02</span>; board[<span class="number">24</span>] = -<span class="number">08</span></span><br><span class="line"><span class="keyword">var</span> square = <span class="number">0</span></span><br><span class="line"><span class="keyword">var</span> diceRoll = <span class="number">0</span></span><br><span class="line"></span><br><span class="line">gameLoop: <span class="keyword">while</span> square != finalSquare &#123;</span><br><span class="line">    diceRoll += <span class="number">1</span></span><br><span class="line">    <span class="keyword">if</span> diceRoll == <span class="number">7</span> &#123; diceRoll = <span class="number">1</span> &#125;</span><br><span class="line">    <span class="keyword">switch</span> square + diceRoll &#123;</span><br><span class="line">    <span class="keyword">case</span> finalSquare:</span><br><span class="line">        <span class="comment">// 骰子数刚好使玩家移动到最终的方格里，游戏结束。</span></span><br><span class="line">        <span class="keyword">break</span> gameLoop</span><br><span class="line">    <span class="keyword">case</span> <span class="keyword">let</span> newSquare <span class="keyword">where</span> newSquare &gt; finalSquare:</span><br><span class="line">        <span class="comment">// 骰子数将会使玩家的移动超出最后的方格，那么这种移动是不合法的，玩家需要重新掷骰子</span></span><br><span class="line">        <span class="keyword">continue</span> gameLoop</span><br><span class="line">    <span class="keyword">default</span>:</span><br><span class="line">        <span class="comment">// 合法移动，做正常的处理</span></span><br><span class="line">        square += diceRoll</span><br><span class="line">        square += board[square]</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">print</span>(<span class="string">"Game over!"</span>)</span><br></pre></td></tr></table></figure>
</li>
<li><p>提前退出语句 <code>guard</code> 。</p>
</li>
<li><p>检测 <code>API</code> 可用性，只要是针对不同系统版本的兼容。通用写法：</p>
 <figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> #available(平台名称 版本号, ..., *) &#123;</span><br><span class="line">    <span class="type">APIs</span> 可用，语句将执行</span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="type">APIs</span> 不可用，语句将不执行</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p> 实际案例展示：</p>
 <figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> #available(iOS <span class="number">10</span>, macOS <span class="number">10.12</span>, *) &#123;</span><br><span class="line">    <span class="comment">// 在 iOS 使用 iOS 10 的 API, 在 macOS 使用 macOS 10.12 的 API</span></span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="comment">// 使用先前版本的 iOS 和 macOS 的 API</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
</ol>
<h4 id="函数"><a href="#函数" class="headerlink" title="函数"></a>函数</h4><ol>
<li><p>函数的可变参数。一个可变参数 <code>variadic parameter</code> 可以接受零个或多个值。通过在变量类型名后面加入 <code>...</code> 的方式来定义可变参数。传入值在函数体中变为此类型的一个数组。一个函数中最多只能有一个可变参数。</p>
 <figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">arithmeticMean</span><span class="params">(<span class="number">_</span> numbers: Double...)</span></span> -&gt; <span class="type">Double</span> &#123;</span><br><span class="line">    <span class="keyword">var</span> total: <span class="type">Double</span> = <span class="number">0</span></span><br><span class="line">    <span class="keyword">for</span> number <span class="keyword">in</span> numbers &#123;</span><br><span class="line">        total += number</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> total / <span class="type">Double</span>(numbers.<span class="built_in">count</span>)</span><br><span class="line">&#125;</span><br><span class="line">arithmeticMean(<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>)</span><br><span class="line"><span class="comment">// 返回 3.0, 是这 5 个数的平均数。</span></span><br><span class="line">arithmeticMean(<span class="number">3</span>, <span class="number">8.25</span>, <span class="number">18.75</span>)</span><br><span class="line"><span class="comment">// 返回 10.0, 是这 3 个数的平均数。</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>定义输入输出函数，在参数定义前加 <code>inout</code> 关键字即可。注意，只能传变量给输入输出参数，不能传常量或字面量，因为这些量是不能被修改的。输入输出参数不能有默认值，而且可变参数不能使用 <code>inout</code> 标记。</p>
</li>
</ol>
<h4 id="闭包"><a href="#闭包" class="headerlink" title="闭包"></a>闭包</h4><ol>
<li><p>关于闭包表达式的进化。虽然越变越简单，但是，还是应该以清晰明了为主。示例只是对闭包语法的一个说明：</p>
 <figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="comment">/// 排序数组</span></span><br><span class="line"><span class="keyword">let</span> names = [<span class="string">"Chris"</span>, <span class="string">"Alex"</span>, <span class="string">"Ewa"</span>, <span class="string">"Barry"</span>, <span class="string">"Daniella"</span>]</span><br><span class="line"><span class="comment">/// 1.0 版本</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">backward</span><span class="params">(<span class="number">_</span> s1: String, <span class="number">_</span> s2: String)</span></span> -&gt; <span class="type">Bool</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> s1 &gt; s2</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">var</span> reversedNames = names.sorted(by: backward)</span><br><span class="line"><span class="built_in">print</span>(reversedNames)</span><br><span class="line"><span class="comment">// reversedNames 为 ["Ewa", "Daniella", "Chris", "Barry", "Alex"]</span></span><br><span class="line"></span><br><span class="line"><span class="comment">/// 2.0 版本</span></span><br><span class="line">reversedNames = names.sorted(by: &#123; (s1: <span class="type">String</span>, s2: <span class="type">String</span>) -&gt; <span class="type">Bool</span> <span class="keyword">in</span></span><br><span class="line">    <span class="keyword">return</span> s1 &gt; s2</span><br><span class="line">&#125;)</span><br><span class="line"></span><br><span class="line"><span class="comment">/// 3.0 版本</span></span><br><span class="line">reversedNames = names.sorted(by: &#123; (s1: <span class="type">String</span>, s2: <span class="type">String</span>) -&gt; <span class="type">Bool</span> <span class="keyword">in</span> <span class="keyword">return</span> s1 &gt; s2 &#125; )</span><br><span class="line"></span><br><span class="line"><span class="comment">/// 4.0 版本</span></span><br><span class="line">reversedNames = names.sorted(by: &#123; s1, s2 <span class="keyword">in</span> <span class="keyword">return</span> s1 &gt; s2 &#125; )</span><br><span class="line"></span><br><span class="line"><span class="comment">/// 5.0 版本</span></span><br><span class="line">reversedNames = names.sorted(by: &#123; s1, s2 <span class="keyword">in</span> s1 &gt; s2 &#125; )</span><br><span class="line"></span><br><span class="line"><span class="comment">/// 6.0 版本</span></span><br><span class="line">reversedNames = names.sorted(by: &#123; $<span class="number">0</span> &gt; $<span class="number">1</span> &#125; )</span><br><span class="line"></span><br><span class="line"><span class="comment">/// 最终版</span></span><br><span class="line">reversedNames = names.sorted(by: &gt;)</span><br></pre></td></tr></table></figure>
</li>
<li><p>闭包是引用类型！！</p>
</li>
<li><p>在逃逸闭包内必须显示的调用 <code>self</code> ，非逃逸闭包则必须要。所谓逃逸闭包，就是指函数返回后才会执行的闭包。典型的就是网络闭包，在请求结束后才会执行。还有自动闭包 <code>autoclosure</code> ，但是这个并不建议使用过多。</p>
 <figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">override</span> <span class="function"><span class="keyword">func</span> <span class="title">viewDidLoad</span><span class="params">()</span></span> &#123;</span><br><span class="line">    <span class="keyword">super</span>.viewDidLoad()</span><br><span class="line">    </span><br><span class="line">    someFunctionWithNonescapingClosure &#123;</span><br><span class="line">        x = <span class="number">50</span></span><br><span class="line">    &#125;</span><br><span class="line">    someFunctionWithEscapingClosure &#123;</span><br><span class="line">        <span class="keyword">self</span>.x = <span class="number">100</span></span><br><span class="line">    &#125;</span><br><span class="line">    someFuncationWithAutoclosureClosure(closure: <span class="string">"Hello"</span>)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/// 非逃逸闭包</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">someFunctionWithNonescapingClosure</span><span class="params">(closure: <span class="params">()</span></span></span> -&gt; <span class="type">Void</span>) &#123;</span><br><span class="line">     closure()</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">/// 逃逸闭包</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">someFunctionWithEscapingClosure</span><span class="params">(closure: @escaping <span class="params">()</span></span></span> -&gt; <span class="type">Void</span>) &#123;</span><br><span class="line">    closure()</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">/// 自动转化闭包</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">someFuncationWithAutoclosureClosure</span><span class="params">(closure: @autoclosure <span class="params">()</span></span></span> -&gt; <span class="type">String</span>) &#123;</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">"<span class="subst">\(closure()</span>)"</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
</ol>
<h4 id="枚举"><a href="#枚举" class="headerlink" title="枚举"></a>枚举</h4><ol>
<li><p>令枚举遵循 <code>CaseIterable</code> 协议。<code>Swift</code> 会生成一个 <code>allCases</code> 属性，用于一个包含枚举所有成员的集合。如：</p>
 <figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">enum</span> <span class="title">Beverage</span>: <span class="title">CaseIterable</span> </span>&#123;</span><br><span class="line">    <span class="keyword">case</span> coffee, tea, juice</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">let</span> numberOfChoices = <span class="type">Beverage</span>.allCases.<span class="built_in">count</span></span><br><span class="line"><span class="built_in">print</span>(<span class="string">"<span class="subst">\(numberOfChoices)</span> beverages available"</span>)</span><br><span class="line"><span class="comment">// 打印“3 beverages available”</span></span><br></pre></td></tr></table></figure>
</li>
</ol>
<ol start="2">
<li><p>递归枚举是一种枚举类型，它由一个或多个枚举成员使用该枚举类型的实例作为关联值。用递归枚举时，编译器会插入一个间接层。可以在枚举成员前加上 <code>indirect</code> 来标识该成员可递归。</p>
<p> 例如，下面的例子，枚举存储了简单的算术表达式：</p>
 <figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">enum</span> <span class="title">ArithmeticExpression</span> </span>&#123;</span><br><span class="line">    <span class="keyword">case</span> number(<span class="type">Int</span>)</span><br><span class="line">    <span class="keyword">indirect</span> <span class="keyword">case</span> addition(<span class="type">ArithmeticExpression</span>, <span class="type">ArithmeticExpression</span>)</span><br><span class="line">    <span class="keyword">indirect</span> <span class="keyword">case</span> multiplication(<span class="type">ArithmeticExpression</span>, <span class="type">ArithmeticExpression</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
</ol>
<pre><code>也可以再枚举类型开头加上 `indirect` 关键字来表明它的所有成员都是可递归的：

<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">indirect</span> <span class="class"><span class="keyword">enum</span> <span class="title">ArithmeticExpression</span> </span>&#123;</span><br><span class="line">    <span class="keyword">case</span> number(<span class="type">Int</span>)</span><br><span class="line">    <span class="keyword">case</span> addition(<span class="type">ArithmeticExpression</span>, <span class="type">ArithmeticExpression</span>)</span><br><span class="line">    <span class="keyword">case</span> multiplication(<span class="type">ArithmeticExpression</span>, <span class="type">ArithmeticExpression</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</code></pre><h4 id="类和结构体"><a href="#类和结构体" class="headerlink" title="类和结构体"></a>类和结构体</h4><ol>
<li><p><code>Swift</code> 中机构体和类有很多共同点，两者都可以：</p>
<ul>
<li>定义属性有存储值</li>
<li>定义方法用于提供功能</li>
<li>定义下标操作用于通过下标访问它们的值</li>
<li>定义构造器用于生成初始值</li>
<li>通过扩展增加默认实现以外的功能</li>
<li><p>遵循协议提供某些标准功能</p>
<p>与结构体相比，类还有如下的附加功能：</p>
</li>
<li><p>继承，允许一个类继承另一个类的特征</p>
</li>
<li>类型转换，允许在运行时检查和解释一个类实例的类型</li>
<li>析构器，允许一个类实例释放任务其所被分配的资源</li>
<li><p>引用计数，允许对一个类多次引用</p>
<p>类增加的附加功能是以增加复杂性为代价的，作为一般准则，优先使用结构体，因为它们更容易理解，仅在适当或必要时使用类。实际上，这意味着我们大多数自定义的数据类型都应该是结构体和枚举。</p>
</li>
</ul>
</li>
<li><p>恒等运算符</p>
<p>因为类是引用类型，所以多个常量或变量幕后可能引用同一个类实例。对于结构体和枚举来说，这并不成立，因为他们都是值类型，再被赋予到常量、变量或者传递到函数时，其值总是会被拷贝。</p>
<p>判定两个常量或者变量是否引用同一个类实例又是会非常有用。为了达到这个目的，<code>Swift</code> 提供了两个恒等运算符，使用这两个运算符检测两个常量或者变量是否引用同一个类实例：</p>
<ul>
<li>相同 <code>===</code></li>
<li>不相同 <code>!==</code></li>
</ul>
<p>请注意，<strong>相同</strong> (用三个等号标识 <code>===</code>) 与 <strong>相等</strong> (用两个等号表示 <code>==</code>) 的不同，<strong>相同</strong> 表示两个类类型(<code>class type</code>) 的常量或变量是否引用同一个类实例，<strong>相等</strong> 用于表示两个值是否相等或等价，判定时要遵循设计者定义的评判标准。</p>
</li>
</ol>
<h4 id="属性"><a href="#属性" class="headerlink" title="属性"></a>属性</h4><ol>
<li><p>全局的常量或者变量都是延迟计算的，跟延时加载存储属性相似，不同的地方在于，全局的常量或者变量不需要标记 <code>lazy</code> 修饰符。而局部范围的常量或变量从不延迟计算。</p>
</li>
<li><p><strong>类型属性</strong>，不论创建多少一个类的类实例，类型属性都只有唯一一份，所有实例共享该数据。使用 <code>static</code> （用于类和结构体），或者 <code>class</code>（用于类）关键字作为修饰符。</p>
</li>
</ol>
<h4 id="方法"><a href="#方法" class="headerlink" title="方法"></a>方法</h4><ol>
<li>方法是与某些特性类型相关联的函数。譬如实例方法、类方法等。</li>
</ol>
<ol start="2">
<li><p>在实例方法中修改值类型</p>
<p>结构体和枚举都是值类型，默认情况下，值类型的属性不能在它的实例方法中被修改。</p>
<p>但是，如果确定需要在某个特定的方法中修改结构体或者枚举的属性，可以为这个方法选择 <strong>可变</strong> <code>mutating</code> 行为，然后就可以在方法内部改变它的属性。</p>
<p>在可变方法中可以给 <code>self</code> 重新赋值：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">enum TriStateSwitch &#123;</span><br><span class="line">    case off, low, high</span><br><span class="line">    mutating func next() &#123;</span><br><span class="line">        switch self &#123;</span><br><span class="line">        case .off:</span><br><span class="line">            self = .low</span><br><span class="line">        case .low:</span><br><span class="line">            self = .high</span><br><span class="line">        case .high:</span><br><span class="line">            self = .off</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line">var ovenLight = TriStateSwitch.low</span><br><span class="line">ovenLight.next()</span><br><span class="line">// ovenLight 现在等于 .high</span><br><span class="line">ovenLight.next()</span><br><span class="line">// ovenLight 现在等于 .off</span><br></pre></td></tr></table></figure>
</li>
</ol>
<h4 id="下标"><a href="#下标" class="headerlink" title="下标"></a>下标</h4><ol>
<li><p>下标允许通过在实例名称后面的方括号中传入一个或者多个索引值来对实例进行查询。它的语法类似于实例方法语法和计算型属性语法。定义下标使用 <code>subscipt</code> 关键字，与定义实例方法类似，都是指定一个或多个输入参数和一个返回类型。与实例方法不同的是，下标可以设定为读写或只读。这种行为由 <code>getter</code> 和 <code>setter</code> 实现，类似计算型属性：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">subscript(index: Int) -&gt; Int &#123;</span><br><span class="line">    get &#123;</span><br><span class="line">         // 返回一个适当的 Int 类型的值</span><br><span class="line">     &#125;</span><br><span class="line">    set(newValue) &#123;</span><br><span class="line">        // 执行适当的赋值操作</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
</ol>
<ol start="2">
<li><p>实例下标是在特定类型的一个实例上调用的下标。也可以定义一种在这个类型自身上调用的下标。这种下标被称作类型下标。可以通过在 <code>subscript</code> 关键字之前写下 <code>static</code> 关键字的方式来表示一个类型下标。类类型可以使用 <code>class</code> 关键字来代替 <code>static</code>，它允许子类重写父类中对那个下标的实现。下面的例子展示了如何定义和调用一个类型下标：</p>
 <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">enum Planet: Int &#123;</span><br><span class="line">    case mercury = 1, venus, earth, mars, jupiter, saturn, uranus, neptune</span><br><span class="line">    static subscript(n: Int) -&gt; Planet &#123;</span><br><span class="line">        return Planet(rawValue: n)!</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line">let mars = Planet[4]</span><br><span class="line">print(mars)</span><br></pre></td></tr></table></figure>
</li>
<li><p>补充一个 <code>static</code> 与 <code>class</code> 的区别：</p>
<ul>
<li><code>static</code> 可以修饰属性、函数，可用于类或者结构体中。但是被 <code>static</code> 修饰的对象不支持重写 <code>override</code>。</li>
<li><code>class</code> 只用于类对象中，可以重写。</li>
</ul>
</li>
</ol>
<h4 id="继承"><a href="#继承" class="headerlink" title="继承"></a>继承</h4><ol>
<li>在合适的地方，可以通过使用 <code>super</code> 前缀来访问超类版本的方法、属性或者下标：<ul>
<li>在方法 <code>someMetho()</code> 的重写实现中，可以通过 <code>super.someMethod</code> 来调用超类版本的 <code>someMethod()</code> 方法。</li>
<li>在属性 <code>someProperty</code> 的 <code>getter</code> 或 <code>setter</code> 的重写实现中，可以通过 <code>super.someProperty</code> 来访问超类版本的 <code>someProperty</code> 属性。</li>
<li>在下标的重写实现中，可以通过 <code>super[someIndex]</code> 来访问超类版本中相同的下标。 </li>
</ul>
</li>
</ol>
<ol start="2">
<li>可以把方法、属性、下标标记为 <code>final</code> 来防止它们被重写。只需要在声明关键字前加上 <code>final</code> 修饰符即可。也可以在关键字 <code>class</code> 前添加 <code>final</code> 修饰符，来将整个类标记为 <code>final</code> 。这样的类是不可以被继承的。</li>
</ol>
<h4 id="构造过程"><a href="#构造过程" class="headerlink" title="构造过程"></a>构造过程</h4><ol>
<li>类和结构体在创建实例时，必须为所有存储属性设置合适的初始值。存储属性不能处于一个未知的状态。当为存储属性设置默认值或者在构造器中设置初始值时，他们的值是被直接设置的，不会触发任何属性观察者。</li>
</ol>
<ol start="2">
<li><p>关于 <strong>形参命名</strong> 和 <strong>实参标签</strong></p>
<p>下面代码块中， <code>fromFahrenheit</code> 是实参标签，<code>fahrenheit</code> 是形参命名。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">struct Celsius &#123;</span><br><span class="line">    var temperatureInCelsius: Double</span><br><span class="line">    init(fromFahrenheit fahrenheit: Double) &#123;</span><br><span class="line">        temperatureInCelsius = (fahrenheit - 32.0) / 1.8</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
</ol>
<ol start="3">
<li><p>可以再构造过程中的任意点给常量属性赋值，只要在构造过程结束时它设置成正确的值。一旦长两属性被赋值，它将永远不能更改。对于类实例来说，它的常量属性只能在类的构造过程中被修改，不能在子类中修改。</p>
</li>
<li><p>结构体如果没有定义任何自定义构造器，它将自动获得一个 <strong>逐一成员构造器</strong>（memberwise initializer）。不像默认构造器，空手存储型属性有默认值，结构体也会获得逐一成员构造器。</p>
</li>
<li><p>构造器可以调用其他构造器来完成实力部分的构造过程。这一过程成为构造器代理。</p>
</li>
<li><p><code>Swift</code> 为类提供了两种构造器来确保实例中所有的存储型属性都能获得初始值，它们被称为指定构造器和便利构造器。指定构造器是类中最主要的构造器。其中：</p>
<ul>
<li>指定构造器必须总是 <strong>向上代理</strong></li>
<li>便利构造器必须总是 <strong>横向代理</strong></li>
</ul>
</li>
<li><p>安全检查的两段式构造过程展示：</p>
<p><strong>阶段 1</strong></p>
<ul>
<li>类的某个指定构造器或便利构造器被调用。</li>
<li>完成类的新实例的内存分配，但此时内存还没有被初始化。</li>
<li>指定构造器确保其所在的类引入的所有存储型属性都已赋初值。存储型属性的内存完成初始化。</li>
<li>指定构造器切换到父类的构造器，对其存储属性完成同样的任务。</li>
<li>这个过程沿着类的继承链一直往上执行，直到达到继承链的最顶部。</li>
<li>当达到了继承链的最顶部，而且继承链的最后一个类已经确保其所有的存储型属性已赋初值，这个实例的内存被认为已经完全初始化，此时，阶段1完成。</li>
</ul>
<p><strong>阶段 2</strong></p>
<ul>
<li>从继承链的顶部往下，继承链上每个类的指定构造器都有机会进一步自定义实例。构造器此时可访问 <code>self</code> ，修改它的属性并调用实例方法等。</li>
<li>最终，继承链中任意的便利构造器都有机会自定义其实例和使用 <code>self</code> 。</li>
</ul>
</li>
<li><p>可失败构造器的参数名和参数类型，不能与其他非可失败构造器的参数名，及参数类型相同。严格来说，构造器都不支持返回值。因为构造器本身的作用，只是确保对象能被正确的构造。因此只是用 <code>return nil</code> 来可失败构造器构造失败，而不要用关键字 <code>return</code> 来表明构造成功。</p>
 <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">struct Animal &#123;</span><br><span class="line">    let species: String</span><br><span class="line">    init?(species: String) &#123;</span><br><span class="line">        if species.isEmpty &#123;</span><br><span class="line">            return nil</span><br><span class="line">        &#125;</span><br><span class="line">        self.species = species</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
</ol>
<h4 id="析构过程"><a href="#析构过程" class="headerlink" title="析构过程"></a>析构过程</h4><ol>
<li>析构器只适用于类类型，析构器是在实例被释放前自动调用的。不能主动调用析构器。子类继承父类的构造器，并且在子类的构造器实现的最后，父类构造器会被自动调用。</li>
</ol>
<h4 id="可选链"><a href="#可选链" class="headerlink" title="可选链"></a>可选链</h4><ol>
<li>嗯，就是那个 <code>?</code>。这里只是说明了怎么调用属性、方法、下标等。</li>
</ol>
<h4 id="错误处理"><a href="#错误处理" class="headerlink" title="错误处理"></a>错误处理</h4><ol>
<li><code>Swift</code> 中的错误处理和其他语言中用 <code>try</code> 、 <code>catch</code> 、 和 <code>throws</code> 进行异常处理很像。和其他语言（包括 <code>Objective-C</code>）的异常处理不同的是， <code>Swift</code> 中的错误处理不涉及解除调用栈，这是一个计算代价高昂的过程。就此而言， <code>throw</code> 语句的性能特性可以和 <code>return</code> 语句媲美。</li>
</ol>
<ol start="2">
<li>为了表示一个函数、方法或构造器可以抛出错误，在函数声明后加上 <code>throws</code> 关键字。一个标有 <code>throws</code> 的函数称为 <code>throwing</code> 函数。如果这个函数指明了返回类型，<code>throws</code> 关键字要写在返回箭头 <code>-&gt;</code> 的前面。只有 <code>throwing</code> 函数可以传递错误。任何在非 <code>throwing</code> 函数内部抛出的错误都只能在函数内部处理。</li>
<li><code>do-catch</code> 可以对错误进行匹配。 <code>defer</code> 是将代码的执行延迟到当前作用域退出之前。</li>
</ol>
<h4 id="类型转换"><a href="#类型转换" class="headerlink" title="类型转换"></a>类型转换</h4><ol>
<li>类型转换在 <code>Swift</code> 中使用 <code>as</code> 和 <code>is</code> 操作符实现。</li>
<li>类型检查操作符 <code>is</code> 来检查一个实例是否属于特定子类型。 <code>as?</code> 或者 <code>as!</code> 用来转换。</li>
<li><code>Swift</code> 为不确定的类型提供了两种类型别名：<ul>
<li><code>Any</code> 标识任何类型，包括函数类型。</li>
<li><code>AnyObject</code> 可以表示任何类类型的实例。</li>
</ul>
</li>
</ol>
<h4 id="嵌套类型"><a href="#嵌套类型" class="headerlink" title="嵌套类型"></a>嵌套类型</h4><ol>
<li>所谓 <strong>嵌套类型</strong>，是指可以在支持的类型中定义嵌套的枚举、类和结构体。要在一个类型中嵌套另一个类型，将嵌套类型定义在其外部类型的 <code>{ }</code> 内即可，而且根据需要可以定义多级嵌套。（根据 <code>SwiftLint</code> 不建议嵌套过多）</li>
</ol>
<h4 id="扩展"><a href="#扩展" class="headerlink" title="扩展"></a>扩展</h4><ol>
<li>扩展可以给一个现有类、结构体、枚举，还有协议添加新的功能。它拥有在不访问扩展类型源码实现的基础上就完成功能扩展的能力，即逆向建模。但需要注意的是，扩展可以添加新的功能，但是不能重写已经存在的功能。<code>Swift</code> 中的扩展可以：<ul>
<li>添加计算型实力属性和类属性。</li>
<li>定义实例方法和类方法。</li>
<li>提供新的构造器。</li>
<li>定义下标。</li>
<li>定义和使用新的嵌套类型。</li>
<li>使已经存在的类型遵循某个协议。</li>
</ul>
</li>
</ol>
<ol start="2">
<li>扩展可以添加新的计算属性，但是不能添加存储属性，或着向现有属性添加属性观察者。</li>
<li>扩展可以添加便利构造器，但是不能添加指定构造器。</li>
</ol>
<h4 id="协议"><a href="#协议" class="headerlink" title="协议"></a>协议</h4><ol>
<li>有时候需要在方法中<strong>改变</strong>（或<strong>异变</strong>）方法所属的实例，对值类型，则需要加 <code>mutating</code> 关键字。如果在协议中定义了一个实例方法，该方法会改变遵循该协议的类型的实例，那么在定义协议时需要在方法前添加 <code>mutating</code> 关键字。这使得结构体和枚举能够遵循该协议并满足此方法要求。</li>
<li><strong>协议合成</strong>，即多个协议合一通过 <code>&amp;</code> 符号结合起来。用的并不是很多。</li>
<li>协议扩展，可以更优雅的实现 <code>可选协议</code> 。</li>
</ol>
<h4 id="泛型"><a href="#泛型" class="headerlink" title="泛型"></a>泛型</h4><ol>
<li>请始终以大写字母开头的驼峰命名法来为类型参数命名，以表明它是一个占位<strong>类型</strong>，而不是一个值。</li>
<li><p>类型约束指定类型参数必须继承自指定类、遵循特定的协议或协议组合。如下：</p>
 <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">func someFunction&lt;T: SomeClass, U: SomeProtocol&gt;(someT: T, someU: U) &#123;</span><br><span class="line">    // 这里是泛型函数的函数体部分</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>关联类型</strong></p>
<p> 定义一个协议时，声明一个或多个关联类型作为协议定义的一部分将会非常有用。关联类型为协议中某个类型提供了一个占位符名称，其代表的实际类型在协议被遵循时才会指定。关联类型通过  <code>associatedtype</code> 关键字来指定。</p>
 <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">protocol Container &#123;</span><br><span class="line">    associatedtype Item</span><br><span class="line">    mutating func append(_ item: Item)</span><br><span class="line">    var count: Int &#123; get &#125;</span><br><span class="line">    subscript(i: Int) -&gt; Item &#123; get &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
</ol>
<pre><code>下面用一个结构体来遵循这个协议：

<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">struct IntStack: Container &#123;</span><br><span class="line">    // IntStack 的原始实现部分</span><br><span class="line">    var items = [Int]()</span><br><span class="line">    mutating func push(_ item: Int) &#123;</span><br><span class="line">        items.append(item)</span><br><span class="line">    &#125;</span><br><span class="line">    mutating func pop() -&gt; Int &#123;</span><br><span class="line">        return items.removeLast()</span><br><span class="line">    &#125;</span><br><span class="line">    // Container 协议的实现部分</span><br><span class="line">    typealias Item = Int</span><br><span class="line">    mutating func append(_ item: Int) &#123;</span><br><span class="line">        self.push(item)</span><br><span class="line">    &#125;</span><br><span class="line">    var count: Int &#123;</span><br><span class="line">        return items.count</span><br><span class="line">    &#125;</span><br><span class="line">    subscript(i: Int) -&gt; Int &#123;</span><br><span class="line">        return items[i]</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>


或者使用一个泛型 `Stack` 来遵循该协议：
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">struct Stack&lt;Element&gt;: Container &#123;</span><br><span class="line">    // Stack&lt;Element&gt; 的原始实现部分</span><br><span class="line">    var items = [Element]()</span><br><span class="line">    mutating func push(_ item: Element) &#123;</span><br><span class="line">        items.append(item)</span><br><span class="line">    &#125;</span><br><span class="line">    mutating func pop() -&gt; Element &#123;</span><br><span class="line">        return items.removeLast()</span><br><span class="line">    &#125;</span><br><span class="line">    // Container 协议的实现部分</span><br><span class="line">    mutating func append(_ item: Element) &#123;</span><br><span class="line">        self.push(item)</span><br><span class="line">    &#125;</span><br><span class="line">    var count: Int &#123;</span><br><span class="line">        return items.count</span><br><span class="line">    &#125;</span><br><span class="line">    subscript(i: Int) -&gt; Element &#123;</span><br><span class="line">        return items[i]</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>


也可以在协议里给关联类型添加约束来要求遵循的类型满足协议：

<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">protocol Container &#123;</span><br><span class="line">    associatedtype Item: Equatable</span><br><span class="line">    mutating func append(_ item: Item)</span><br><span class="line">    var count: Int &#123; get &#125;</span><br><span class="line">    subscript(i: Int) -&gt; Item &#123; get &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>


或者在关联类型约束里使用协议：

<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">protocol SuffixableContainer: Container &#123;</span><br><span class="line">    associatedtype Suffix: SuffixableContainer where Suffix.Item == Item</span><br><span class="line">    func suffix(_ size: Int) -&gt; Suffix</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</code></pre><ol start="2">
<li><p><strong> 泛型 <code>Where</code> 语句</strong></p>
<p> 需要灵活使用 <code>where</code> 进行条件约束即可。</p>
</li>
</ol>
<h4 id="不透明类型"><a href="#不透明类型" class="headerlink" title="不透明类型"></a>不透明类型</h4><ol>
<li><p>虽然使用不透明类型作为返回值，看起来和返回协议类型非常相似，但两者有个主要区别，就是是否需要保持类型一致性。一个不透明类型只能对应一种具体的类型，即便函数调用者并不知道是哪一种类型。协议类型可以对应多个类型，只要他们都遵循统一协议。总得来说，协议类型更具灵活性，底层类型可以存储更多的值，而不透明类型对这些底层类型有更强的限定性。用法如下：</p>
 <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">protocol Shape &#123;</span><br><span class="line">    func draw() -&gt; String;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">struct Quare: Shape &#123;</span><br><span class="line">    var size: Int</span><br><span class="line"></span><br><span class="line">    func draw() -&gt; String &#123;</span><br><span class="line">        return size.description;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">class ViewController: UIViewController &#123;</span><br><span class="line"></span><br><span class="line">    override func viewDidLoad() &#123;</span><br><span class="line">        super.viewDidLoad()</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    /// 返回协议类型</span><br><span class="line">    func makeOneQuare() -&gt; Shape &#123;</span><br><span class="line">        let q1 = Quare(size: 10)</span><br><span class="line">        print(q1.draw())</span><br><span class="line">        return q1;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    /// 返回不透明类型</span><br><span class="line">    func makeSecondQuare() -&gt; some Shape &#123;</span><br><span class="line">        let q2 = Quare(size: 10)</span><br><span class="line">        print(q2.draw())</span><br><span class="line">        return q2;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>说一下自己的看法吧：不透明类型是可以被协议类型替代使用的，这个就好像 <code>weak</code> 、 <code>unowned</code> 两个关键词一样，我们有知道区别的必要性，但是即使只用 <code>weak</code>，就可以解决循环引用的问题了。可能真的有只能使用 <code>unowned</code> 的情况，如果以后遇到，会在这里补充。 </p>
</li>
</ol>
<h4 id="自动引用计数"><a href="#自动引用计数" class="headerlink" title="自动引用计数"></a>自动引用计数</h4><ol>
<li>引用计数仅仅用于类的实例。结构体和枚举都是值类型，不是引用类型，也不是通过引用的方式存储和传递。</li>
<li><strong>弱引用</strong>不会对其引用的实例保持强引用，因而不会阻止 <code>ARC</code> 销毁被引用的实例。这个特性阻止了引用变为循环引用。关键字 <code>weak</code>。另外，当 <code>ARC</code> 设置弱引用为 <code>nil</code> 时，属性观察不会被触发。</li>
<li>和<strong>弱引用</strong>类似，<strong>无主引用</strong>不会牢牢保持引用的实例。和弱引用不同的是，无主引用在其他实例有相同或更长声明周期时使用。关键字是 <code>unowned</code> 。无主引用通常都被期望有值。不过 <code>ARC</code> 无法再实例被销毁后将无主引用设为 <code>nil</code> ，因为非可选类型的实例不允许被置为 <code>nil</code>。并且，使用无主引用，必须确保引用始终指向一个未被销毁的实例。如果在该实例被销毁后，访问该实例的无主引用，会触发运行时崩溃。</li>
<li>闭包和类相似，都是引用类型。循环引用可以通过弱引用或者无主引用来解决。</li>
</ol>
<h4 id="内存安全"><a href="#内存安全" class="headerlink" title="内存安全"></a>内存安全</h4><h5 id="1-内存访问冲突的实质："><a href="#1-内存访问冲突的实质：" class="headerlink" title="1. 内存访问冲突的实质："></a>1. 内存访问冲突的实质：</h5><ol>
<li>内存访问冲突时，要考虑内存访问上下文的三个性质：访问是读还是写，访问的时长，以及访问的地址。特别是，冲突会发生在有两个访问符合下列情况时：<ul>
<li>至少有一个是写访问</li>
<li>他们的访问是同一个内存地址</li>
<li>他们的访问在时间线上有部分的重叠。</li>
</ul>
</li>
</ol>
<h5 id="2-Swift-中两种长期访问类型："><a href="#2-Swift-中两种长期访问类型：" class="headerlink" title="2. Swift 中两种长期访问类型："></a>2. <code>Swift</code> 中两种长期访问类型：</h5><ul>
<li><code>In-Out</code> 参数的访问冲突。</li>
<li>结构体 <code>mutating</code> 方法里。</li>
</ul>
<h5 id="3-这个小节里举的例子值得回头再看看。"><a href="#3-这个小节里举的例子值得回头再看看。" class="headerlink" title="3. 这个小节里举的例子值得回头再看看。"></a>3. <a href="https://swiftgg.gitbook.io/swift/swift-jiao-cheng/25_memory_safety" target="_blank" rel="noopener">这个小节</a>里举的例子值得回头再看看。</h5><p>主要是针对长期内存访问的分析，和解决方法。</p>
<h4 id="访问控制"><a href="#访问控制" class="headerlink" title="访问控制"></a>访问控制</h4><h5 id="1-访问级别"><a href="#1-访问级别" class="headerlink" title="1. 访问级别"></a>1. 访问级别</h5><p><code>Swift</code> 为代码中的实体提供了五种不同的访问级别：</p>
<ul>
<li><code>open</code> 和 <code>public</code> 级别可以让实体被同一模块的所有实体访问。在模块外也可以通过导入该模块来访问原文件里的所有实体。通常情况下，使用 <code>open</code> 或者 <code>public</code> 来定义模块对外访问接口。<blockquote>
<p><code>open</code> 只能作用于类或者类的成员，它和 <code>public</code> 的区别主要在于 <code>open</code> 限定的类或者类的成员能够在模块外被继承和重写。将类的访问级别显示指定为 <code>open</code> 表明已经设计好了类的代码，并且充分考虑到了这个类在其他模块作为父类时的影响。</p>
</blockquote>
</li>
<li><code>internal</code> 级别让实体被同一模块源文件的任何实体访问，但是不能被模块外的实体访问。通常情况下，某个实体只在应用程序或者框架内部使用，可以将其设置为 <code>internal</code> 级别。同时，这个也是默认访问级别。</li>
<li><code>fileprivate</code> 限制实体只能在其定义的文件内部访问。如果功能代码的实现细节只需要在文件内部访问时，可以使用 <code>fileprivate</code> 来将其隐藏。</li>
<li><code>private</code> 限制实体只能在其定义的作用域，以及同一文件的 <code>extension</code> 中访问。非同一个源文件的 <code>extension</code> ，也是无法访问的。</li>
</ul>
<p>以上， <code>open</code> 的访问级别最高，限制最少。<code>private</code> 的访问级别最低，限制最多。</p>
<h5 id="2-访问级别规则"><a href="#2-访问级别规则" class="headerlink" title="2. 访问级别规则"></a>2. 访问级别规则</h5><p><code>Swift</code> 中访问级别遵循一个原则：<strong>实体不能定义在具有更低级别访问的实体中</strong>。例如：</p>
<ul>
<li>一个 <code>public</code> 的变量，其所在类型的访问级别不能是 <code>internal</code>、<code>fileprivate</code> 或者 <code>private</code> 。因为无法保证变量的类型在使用变量的地方也具有访问权限。</li>
<li>函数的访问级别不能高于它的参数类型和返回类型的访问级别。因为这样就会出现函数可以在任何地方被访问，而参数和返回类型不可以的情况。</li>
</ul>
<p>注意，即使违反上面的规则，正常情况下，也不会编译或者运行错误…</p>
<h4 id="高级运算符"><a href="#高级运算符" class="headerlink" title="高级运算符"></a><a href="https://swiftgg.gitbook.io/swift/swift-jiao-cheng/27_advanced_operators" target="_blank" rel="noopener">高级运算符</a></h4><p>开发中比较少需要使用高级运算符，这里留着记录，具体使用时在参考，点击标题即可。</p>
<h5 id="1-位运算符"><a href="#1-位运算符" class="headerlink" title="1. 位运算符"></a>1. 位运算符</h5><ul>
<li><p>按位取反运算符 <code>Bitwise NOT Operator</code> </p>
<ul>
<li>按位取反运算符 <code>~</code> 对一个数值的全部比特位进行取反</li>
<li>按位取反运算符是一个前缀运算符，直接放在运算数之前，并且他们之间不能添加任何空格。<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">let age = 18</span><br><span class="line">let invertAge = ~age</span><br><span class="line">print(invertAge)  // -19</span><br></pre></td></tr></table></figure>
</li>
</ul>
</li>
<li><p>按位与运算符 <code>Bitwise AND Operator</code> </p>
<ul>
<li>按位与运算符 <code>&amp;</code> 对两个数的比特位进行合并。它返回一个新的数。只有当两个数的全部对应为都为 <code>1</code> 的时候，新数的对应位才为 <code>1</code> 。<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">let one = 10</span><br><span class="line">let sec = 10</span><br><span class="line">let third = 20</span><br><span class="line">let and1 = one &amp; sec</span><br><span class="line">let and2 = one &amp; third</span><br><span class="line">print(and1, and2)   // 10         0</span><br></pre></td></tr></table></figure>
</li>
</ul>
</li>
<li><p>按位或运算符 <code>Bitwise OR Operaor</code></p>
<ul>
<li>按位或运算符 <code>|</code> 可以对两个数的比特位进行比较。它返回一个新的数，只要两个数的对应位中任意一个为 <code>1</code> ，新数的对应位就为 <code>1</code> 。</li>
</ul>
</li>
<li><p>按位异或运算符 <code>Bitwise XOR Operator</code> </p>
<ul>
<li>按位异或运算符，或<em>称排外的运算符</em> <code>^</code>，可以对两个数的比特位进行比较。它返回一个新的数，当两个数的对应为不相同时，新数的对应位就为 <code>1</code>，对应位相同时，则为 <code>0</code> 。</li>
</ul>
</li>
<li><p>按位左移、按位右移运算符 <code>Bitwise Left Right Shift Operators</code> </p>
<ul>
<li>按位左移运算符 <code>&lt;&lt;</code> 和按位右移运算符 <code>&gt;&gt;</code> 可以对一个数的所有位指定位数的左移或者右移，但是需要遵循下面定义的规则。</li>
<li>对一个数进行按位左移或者按位右移，相当于对这个数进行乘以 <code>2</code> 或者除以 <code>2</code> 的运算。将一个整数左移一位，等价于将这个数乘以 <code>2</code> 。同样的，将一个整数右移一位，等价于将这个数除以 <code>2</code> 。</li>
<li>无符号整数的移位运算（简单）</li>
<li>有符号整数的移位运算（复杂）</li>
</ul>
</li>
</ul>
<h5 id="2-溢出运算符"><a href="#2-溢出运算符" class="headerlink" title="2. 溢出运算符"></a>2. 溢出运算符</h5><ul>
<li>溢出加法 <code>&amp;+</code></li>
<li>溢出减法 <code>&amp;-</code></li>
<li>溢出乘法 <code>&amp;*</code></li>
</ul>
<h5 id="3-优先级和结合性"><a href="#3-优先级和结合性" class="headerlink" title="3. 优先级和结合性"></a>3. 优先级和结合性</h5><p>运算时，加上必要的括号，更具可读性。</p>
<h5 id="4-运算符函数"><a href="#4-运算符函数" class="headerlink" title="4. 运算符函数"></a>4. 运算符函数</h5><p>类和结构体可以为现有的运算符提供自定义的实现，这通常被称为 <code>运算符重载</code> 。</p>
<ul>
<li>前缀和后缀运算符，如 <code>-a</code> 、<code>b!</code> </li>
<li>符合赋值运算符，如 <code>=</code> 、 <code>+=</code></li>
<li>等价运算符，如 <code>==</code> 、 <code>!=</code> </li>
</ul>
<h5 id="5-自定义运算符"><a href="#5-自定义运算符" class="headerlink" title="5. 自定义运算符"></a>5. 自定义运算符</h5><p>自定义运算符的优先级。</p>
<h3 id="后记"><a href="#后记" class="headerlink" title="后记"></a>后记</h3><p>本来是想一星期补完的， 结果断断续续持续了两周多。这里仅仅是记录自己日常中容易忽视的，或者不太常用的一些点。语言参考只是让我们对这个语言有个大概的了解，想要加深对 <code>Swift</code> 的理解，还是应该更多的去看优秀的源码，自己动手去实践和总结。</p>
]]></content>
      <tags>
        <tag>ios swift 基础</tag>
      </tags>
  </entry>
  <entry>
    <title>iOS面试宝典 开发工具-Swift</title>
    <url>/2019/01/16/iOS%E9%9D%A2%E8%AF%95%E5%AE%9D%E5%85%B8-%E5%BC%80%E5%8F%91%E5%B7%A5%E5%85%B7-Swift/</url>
    <content><![CDATA[<p>####1. Swift]</p>
<p>####2. Objective-C</p>
<p>####3. Swift VS Objective-C</p>
<p>####4. Xcode 使用</p>
 <a id="more"></a> 
<!----------- 哈哈哈哈 ------------>
<h2 id="1-Swift"><a href="#1-Swift" class="headerlink" title="1. Swift"></a>1. Swift</h2><p>在iOS开发中，语言选择是最初的一步，现在苹果公司主推的语言是 Swift。</p>
<p>Swift 自 2014年发布以来，已经历经 4 个版本的迭代。其在 TIOBE 编程语言排行榜上，一度位列第 12 位，超过 Ruby 并远远甩开上一代语言 Objective-C。从性能上来说，它的速度是 Objective-C 的 2.6 倍，是 Python 的 8.4 倍。更重要的是，Swift 是一门开源的语言，它的性能和优化接受者整个业界的监督和关注。无论从哪个角度讲，Swift 都将取代 Objective-C，成为 iOS 开发的主流语言。</p>
<p>所以，在面试中，关于 Swift 的问题越来越多。对于这门语言的熟悉程度，成为判断一名软件工程师水准的重要标准之一。下面的内容会从实战和理论连个角度来探讨 Swift 的特性。</p>
<h3 id="Swift-面试理论题"><a href="#Swift-面试理论题" class="headerlink" title="Swift 面试理论题"></a>Swift 面试理论题</h3><h4 id="类（class）和结构体（struct）有什么区别"><a href="#类（class）和结构体（struct）有什么区别" class="headerlink" title="类（class）和结构体（struct）有什么区别"></a>类（class）和结构体（struct）有什么区别</h4><p><strong>关键词</strong>：<font color="red"><figure class="highlight plain"><figcaption><span>```#值类型```</span></figcaption></figure></font><table><tr><td class="code"><pre><span class="line"> </span><br><span class="line">在 Swift 中，class 是引用类型，struct 是值类型。值类型在传递和赋值时将进行复制，而引用类型只会适用引用对象的一直“指向”。所以，两者之间的区别就是两个类型的区别。</span><br><span class="line"></span><br><span class="line">举一个简单的例子，代码如下：</span><br></pre></td></tr></table></p>
<p>class Temperature {<br>    var value: Float = 30.0<br>}</p>
<p>class Person {<br>    var temp: Temperature?</p>
<pre><code>func sick() {
    temp?.value = 40
}
</code></pre><p>}</p>
<p>let A = Person()<br>let B = Person()<br>let temp = Temperature()</p>
<p>A.temp = temp<br>B.temp = temp</p>
<p>A.sick()<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">在上面这段代码，Temperature 是 class，为引用类型，故 A 的 temp 和 B 的 temp 指向同一个对象。A 的 temp 值被修改了，B 的 temp 值也会随着改变。如果将 Temperature 改为 struct，为值类型，则 A 的 temp 值的修改不会影响到 B 的 temp 值。</span><br><span class="line"></span><br><span class="line">在内存中，引用类型，诸如类，是在堆（heap）上进行存储和操作的；而值类型，诸如结构体，是在栈（stack）上进行存储和操作的，相比栈上的操作，堆上的操作更加复杂和耗时，所以苹果官方推荐使用结构体，这样可以提高 APP 的运行效率。</span><br><span class="line"></span><br><span class="line">**加分回答：**</span><br><span class="line"></span><br><span class="line">class 的如下功能是 struct 没有的：</span><br><span class="line"></span><br><span class="line">- 可以继承，这样子类可以使用父类的特性和方法。</span><br><span class="line">- 类型转换可以再运行时检查和解释一个实例的类型。</span><br><span class="line">- 可以用 deinit 来释放资源。</span><br><span class="line">- 一个类可以被多次引用。</span><br><span class="line"></span><br><span class="line">struct 也有如下优势：</span><br><span class="line"></span><br><span class="line">- 结构较小，适用于复制操作，相比一个 class 的实例被多次引用，struct 更加安全。</span><br><span class="line">- 无须担心内存泄漏或者多线程冲突问题。</span><br><span class="line"></span><br><span class="line">**类似问题：**</span><br><span class="line"></span><br><span class="line">引用类型和值类型有什么区别？struct 相比 class，在使用上有什么优势？</span><br><span class="line"></span><br><span class="line">#### Swift 是面向对象还是函数式的编程语言</span><br><span class="line"></span><br><span class="line">**关键词**：&lt;font color=&quot;red&quot;&gt;```#面向对象```  ```#函数式编程```&lt;/font&gt;</span><br><span class="line"></span><br><span class="line">Swift 既是面向对象的编程语言，又是函数式的编程语言。</span><br><span class="line"></span><br><span class="line">说 Swift 是面向对象的编程语言，是因为 Swift 支持类的封装、继承和多态，从这一点来看，Swift 与 Java 这类纯面向对象的编程语言几乎毫无差别。</span><br><span class="line"></span><br><span class="line">说 Swift 是函数是的编程语言，是因为 Swift 支持 map，reduce，filter，flatmap 这类除去中间状态、数学函数式的方法，更加强调运算结果而不是中间过程。</span><br><span class="line"></span><br><span class="line">**类似问题：**</span><br><span class="line"></span><br><span class="line">为什么说 Swift 是函数式的编程语言？</span><br><span class="line"></span><br><span class="line">#### 在 Swift 中，什么是可选型（optional）</span><br><span class="line"></span><br><span class="line">**关键词**：&lt;font color=&quot;red&quot;&gt;```#Optional```  ```#nil```&lt;/font&gt;</span><br><span class="line"></span><br><span class="line">在 Swift 中，可选型是为了表达当一个变量值为空的情况。当一个变量值为空时，它就是 nil。在 Swift 中，无论变量是引用类型还是值类型，都可以是可选型变量。下面举一个例子：</span><br></pre></td></tr></table></figure></p>
<p>/// 值类型为 Float，value 默认值是 30.0<br>var value: Float = 30.0<br>/// 值类型为 String ,key 默认值为 nil<br>var key: String?<br>/// 引用类型为 UIImage， image 默认值为 nil<br>var image: UIImage?<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">**加分回答：**</span><br><span class="line"></span><br><span class="line">在 Objective-C 中没有明确提出可选型的概念，然而，其引用类型却可以为 nil，以此来标志其变量值为空的情况。而 Swift 将这一概念扩大到值类型，并且明确提出了可选性的概念。</span><br><span class="line"></span><br><span class="line">#### 在 Swift 中，什么是泛型（Generics）</span><br><span class="line">**关键词**：&lt;font color=&quot;red&quot;&gt;```#泛型```&lt;/font&gt;</span><br><span class="line"></span><br><span class="line">在 Swift 中，泛型主要是为了增加代码的灵活性而生的：它可以使对应的代码满足任意类型的变量或方法。</span><br><span class="line"></span><br><span class="line">举一个简单的例子：加入要写出一种方法可以交换两个 Int 值，其中一种写法如下：</span><br></pre></td></tr></table></figure></p>
<p>func swap(_ a: inout Int, _ b: inout Int) {<br>    (a, b) = (b, a)<br>}<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">上面这种写法有用但并不高效。因为，加入需要实现一个方法用于交换两个 Float 值，那么又得重写一遍。泛型就是为了解决这类问题而来的：希望有个一般性的方法，可以交换任意类型的值。</span><br></pre></td></tr></table></figure></p>
<p>func swap<t>(_ a: inout T, _ b: inout T) {<br>    (a, b) = (b, a)<br>}<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">**注意**，Swift 是类型安全的语言，所以这里交换两个变量的类型必须一致。</span><br><span class="line"></span><br><span class="line">#### 说明比较关键词：Open，Public，Internal，File-private和private</span><br><span class="line"></span><br><span class="line">**关键词**：&lt;font color=&quot;red&quot;&gt;```#访问控制权限```&lt;/font&gt;</span><br><span class="line"></span><br><span class="line">Swift 有五个级别的访问控制权限，从高到底一次为空 Open，Public，Internal，File-private 和 private。</span><br><span class="line"></span><br><span class="line">它们遵循的基本原则是：高级别的变量不允许被定义为低级别变量的成员变量。比如，一个 Private 的 class 中不能含有 Public 的 String。反之。低级别的变量却可以定义在高级别的变量中。比如，Public 的 class 中可以含有 Private 的 Int 值。</span><br><span class="line"></span><br><span class="line">- **Open** 具备最高的访问权限。其修饰的的类和方法可以在任意 Module 中被访问和重写；它是 Swift3 中新添加的访问权限。</span><br><span class="line">- **Public** 的权限仅次于 Open。与 Open 唯一的区别在于，它修饰的对象可以在任意 Module 中被访问，但不能重写。</span><br><span class="line">- **Internal** 是默认的权限。它表示只能在当前定义的 Module 中被访问和重写。它可以被一个 Module 中的多个文件访问，但不能被其他 Module 访问。</span><br><span class="line">- **File-private** 也是 Swift3 中新添加的权限。其修饰的对象只能在当前文件中使用。例如，它可以被一个文件中的 class、extension 和 struct 共同使用。</span><br><span class="line">- **Private** 是最低级别的访问权限。它的对象只能在定义的作用域内使用。离开了这个作用域，即使是同一个文件中的其他作用域，也无法访问。</span><br><span class="line"></span><br><span class="line">**类似问题：**</span><br><span class="line"></span><br><span class="line">Swift3 中新引入的 Open 和 File-private 关键词有什么用？</span><br><span class="line"></span><br><span class="line">#### 说明比较关键词：Strong，Weak 和 Unowned</span><br><span class="line"></span><br><span class="line">**关键词**：&lt;font color=&quot;red&quot;&gt;```#引用类型```    ```#内存管理``` &lt;/font&gt;</span><br><span class="line"></span><br><span class="line">Swift 的内存管理机制与 Objective-C 一样，都为 ARC（Automatic Reference Counting）。其基本原理是，一个对象在没有任何强引用指向它时，所占用的内存就会被回收。反之，只要有任何一个强引用指向该对象，它就会一直存在于内存中。</span><br><span class="line"></span><br><span class="line">- Strong 代表强引用，是默认属性。当一个对象被声明为 Strong 时，表示父层级对该对象有一个强引用的指向，此时，该对象的引用计数会增加1。</span><br><span class="line">- Weak 代表弱引用。当一个对象被声明为 Weak 时，表示父层级对该对象没有指向，该对象的引用计数不会增加1。在该对象被释放后，弱引用页随即消失。继续访问该对象，程序会到的 nil，不会崩溃。</span><br><span class="line">- Unowned 与弱引用的本质一样。唯一不同的是，该对象被释放后，依然有个无效的引用指向对象，它不是 Optional，也不指向 nil。如果继续访问该对象，则程序就会崩溃。</span><br><span class="line"></span><br><span class="line">**加分回答：**</span><br><span class="line"></span><br><span class="line">引入 Weak 和 Unowned，是为了解决由 Strong 带来的循环引用问题。简单地说，当两个对象互相有一个强引用指向对方时，就会导致两个对象在内存中无法被释放。</span><br><span class="line"></span><br><span class="line">Weak 和 Unowned de 使用场景有如下差别：</span><br><span class="line"></span><br><span class="line">- 当访问对象可能已经被释放时，则用 weak，比如 delegate 的修饰。</span><br><span class="line">- 当访问对象确定不可能被释放时，则用 unowned，比如 self 的引用。</span><br><span class="line"></span><br><span class="line">实际上，为了安全，很多公司规定任何时候都用 Weak 修饰。</span><br><span class="line"></span><br><span class="line">#### 在 Swift 中，如何理解 copy-on-write</span><br><span class="line"></span><br><span class="line">**关键词**：&lt;font color=&quot;red&quot;&gt;```#内存管理``` &lt;/font&gt;</span><br><span class="line"></span><br><span class="line">当值类型（比如 struct）在复制时，复制的对象和原对象实际上在内存中指向同一个对象。而且仅当修复制后的对象时，才会在内存中重新创建一个新的对象。下面举一个例子：</span><br></pre></td></tr></table></figure></t></p>
<p>/// arrayA 是一个数组，为值类型<br>let arrayA = [1, 2, 3]<br>/// arrayB 这个时候与 arrayA 在内存中是同一个数组，内存中没有生成新的数组<br>var arrayB = arrayA<br>/// arrayB 被修改了，此时 arrayB 在内存中变成了一个新的数组，而不是原来的 arrayA 了<br>arrayB.append(4)<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">从上面的代码可以看出，复制的数组与原数组共享一个地址，直到其中一个发生改变。这样的设计使得值类型可以被多次复制而无须耗费多余的内存，只有变化的时候才会增加开销。**因此内存的使用更加高效**。</span><br><span class="line"></span><br><span class="line">#### 什么是属性观察器（Property Observer）</span><br><span class="line"></span><br><span class="line">**关键词**：&lt;font color=&quot;red&quot;&gt;```#willSet```    ```#didSet``` &lt;/font&gt;</span><br></pre></td></tr></table></figure></p>
<p>var title: String {<br>    willSet {<br>        print(“将标题从(title)设置到(newValue)”)<br>    }<br>    didSet {<br>        print(“将标题从(oldValue)设置到(title)”)<br>    }<br>}<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">上面这段代码对 title 进行了监听。在 title 发生改变前，willSet 对应的作用域将被执行，新的值是 newValue。在 title 发生改变后，didSet 对应的作用域将被执行，原来的值为 oldValue。这就是属性观察。</span><br><span class="line"></span><br><span class="line">**加分回答：**</span><br><span class="line"></span><br><span class="line">初始化方法对属性的设定，以及在 willSet 和 didSet 中对属性的再次设定，都不会触发调用属性观察器。</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">### Swift 面试实战题</span><br><span class="line"></span><br><span class="line">#### 在结构体中如何修改成员变量的方法</span><br><span class="line"></span><br><span class="line">**关键词**：&lt;font color=&quot;red&quot;&gt;```#mutating``` &lt;/font&gt;</span><br><span class="line"></span><br><span class="line">请问下面的代码存在什么问题？</span><br></pre></td></tr></table></figure></p>
<p>protocol Pet {<br>    var name: String { get set }<br>}</p>
<p>struct MyDog: Pet {<br>    var name: String</p>
<pre><code>func changeName(name: String) {
    self.name = name
}
</code></pre><p>}<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">应该在 ```func changeName(name: String)``` 的前面加上关键词 ```mutating```,表示该方法会修改结构体中自己的成员变量。</span><br><span class="line"></span><br><span class="line">**注意**，再设计协议的时候，由于 protocol 可以被 class 或 struct 或 enum 实现，故而要考虑是否用 `mutating` 来修饰方法。</span><br><span class="line"></span><br><span class="line">类中不存在这个问题，因为类可以随意修改自己的成员变量。</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">#### 如何用 Swift 实现 或（||）操作</span><br><span class="line"></span><br><span class="line">**关键词**：&lt;font color=&quot;red&quot;&gt;```#autoclosure``` &lt;/font&gt;</span><br><span class="line"></span><br><span class="line">这道题的解法很多，下面给出一种最直接的解法：</span><br></pre></td></tr></table></figure></p>
<p>func ||(left: Bool, right: Bool) -&gt; Bool {<br>    if left {<br>        return true<br>    } else {<br>        return right<br>    }<br>}<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">上面这种解法虽然正确，当时并不高效。或（||）操作的本质是，当表达式左边的值为真的时候，无需计算表达式右边的值。而上面这种解法是将表达式右边的值准备好，在传入进行操作。当表达式右边的值十分复杂时，就会造成性能上的浪费。所以，上面的这种做法违反了或（||）操作的本质，正确的实现方法如下：</span><br></pre></td></tr></table></figure></p>
<p>func ||(left: Bool, right: @autoclosure () -&gt; Bool) -&gt; Bool {<br>    if left {<br>        return true<br>    } else {<br>        return right()<br>    }<br>}<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">autoclosure 可以将表达式右边值得计算推迟到判定 left 值为 false 时，这样就可以避免第一种方法带来的不必要的计算开销了。</span><br><span class="line"></span><br><span class="line">#### 实现一个函数：输入任意一个整数，输出为输入的整数 +2</span><br><span class="line"></span><br><span class="line">**关键词**：&lt;font color=&quot;red&quot;&gt;```#柯里化``` &lt;/font&gt;</span><br><span class="line"></span><br><span class="line">这道题看似简单，如果直接写成下面的代码：</span><br></pre></td></tr></table></figure></p>
<p>func addTwo(_ num: Int) -&gt; Int {<br>    return num + 2<br>}<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">接下来面试官会问：“假如要实现输入为输出的整数+6或+8呢？能不能只定义一次方法呢？”正确的写法是利用 Swift 的 Currying 特性：</span><br></pre></td></tr></table></figure></p>
<p>func add(_ num: Int) -&gt; (Int) -&gt; Int {<br>    return { val in<br>        return num + val<br>    }<br>}<br>let addTwo = add(2), addFour = add(4), addTen = add(10)<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">Swift 的柯里化特性是函数式编程思想的体现。它接受多个参数的方法进行变形，并用高阶函数的方式进行处理，是整个代码更加灵活。</span><br><span class="line"></span><br><span class="line">#### 实现一个函数：求 0~100（包括 0 和 100）中为偶数并且恰好是其他数字方法的数字</span><br><span class="line"></span><br><span class="line">**关键词**：&lt;font color=&quot;red&quot;&gt;```#函数式编程``` &lt;/font&gt;</span><br><span class="line"></span><br><span class="line">这套题十分简单。最简单、最粗暴的写法如下：</span><br></pre></td></tr></table></figure></p>
<p>func evenSquereNums(from: Int, to: Int) -&gt; [Int] {<br>    var res = <a href="">Int</a><br>    for num in from … to where num % 2 == 0 {<br>        if (from … to).contains(num <em> num) {<br>            res.append(num </em> num)<br>        }<br>    }<br>    return res<br>}<br>evenSquereNums(from: 0, to: 100)<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">上面代码的写法虽然正确，但是不够优雅。首先，这个方法完全可以利用泛型进行优化，同时，可以在创建 res 数组时加上 reserveCapacity，以保证其性能。其实这个题目最简单、直接的写法使用函数式编程的思想，一行代码即可解决问题：</span><br></pre></td></tr></table></figure></p>
<p>(0 … 10).map { $0 * $0 }.filter { $0 % 2 == 0 }<br><code>`</code></p>
<p>Swift 有函数式编程的思想。其中 flatMap，map，reduce 和 filter 是其代表的方法。本题考察了 map 和 filter 的组合使用。相比一般的 for 循环，这种写法更加简洁。</p>
]]></content>
      <tags>
        <tag>ios 面试 swift</tag>
      </tags>
  </entry>
</search>
